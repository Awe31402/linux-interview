# 《奔跑吧 Linux內核》（第二版）卷1 第5章 內存管理之高級主題 高頻面試題

> **說明**：收錄自第 5 章開篇「本章的高頻面試題」，共計 48 題。
> 答案結合書本理論、Linux 內核原始碼（本專案樹，版本 6.1.115）、
> 以及 Radxa ROCK 5B（RK3588）實機（`radxa@192.168.68.57`）的實測資料。

---

### 1. page数据结构中的_refcount和_mapcount有什么区别？

- **`_refcount`（`atomic_t`）**：该 `struct page` 的**总引用计数**，代表"内核中有多少处代码/数据结构正持有对这个物理页的引用，认为它当前『正在被使用、不能被释放/回收』"，涵盖范围很广：被页表映射、被 page cache 索引、被 `get_user_pages()` 钉住、被内核某处代码临时 `get_page()` 持有等都会增加它。**只要 `_refcount` 归零，该页才真正可以被释放回伙伴系统**，因此它是"这个物理页是否可以被回收/释放"的最终判据。
- **`_mapcount`（`atomic_t`）**：专门统计**该页当前被多少个用户态页表项（PTE）实际映射着**（即出现在多少个进程的地址空间中），不涉及"page cache 是否引用它""内核是否临时借用它"这类非页表映射的引用。`page_mapcount(page) = atomic_read(&page->_mapcount) + 1`（内部从 -1 起算，未被任何 PTE 映射时为 -1，读出的 `mapcount` 是 0）。

关系：**`_mapcount` 是 `_refcount` 的一个子集来源**——每多一个 PTE 映射该页，`_mapcount` 加一的同时，也会让 `_refcount` 相应加一（因为"被页表映射"本身也算一种引用）；但 `_refcount` 还包含很多 `_mapcount` 不统计的引用（如刚从伙伴系统分配出来、还未建立任何映射的页 `_refcount=1` 但 `_mapcount=-1`(0)；被 page cache 索引但尚无进程 `mmap` 它）。因此 `_refcount >= _mapcount 相关的映射引用之和`，两者服务于不同层面的判断：`_mapcount` 回答"有多少地址空间在用它（决定是否需要 COW，见第4章第42题）"，`_refcount` 回答"这个物理页整体是否还有任何存在的理由（决定能否释放）"。

### 2. 匿名页面和高速缓存页面有什么区别？

- **匿名页面（Anonymous Page）**：**不对应任何文件**，其内容"凭空产生"（如进程堆/栈、`malloc`/`mmap(MAP_ANONYMOUS)` 分配的内存），生命周期完全由进程地址空间决定。`page->mapping` 指向一个 `anon_vma`（并在低位打上 `PAGE_MAPPING_ANON` 标记以区分，见第6题），没有底层文件/存储对应；一旦被换出，其数据只能存到 **swap 分区/文件**才能保留，进程退出（且没有共享者）后数据即彻底丢弃。
- **页面高速缓存（Page Cache）**：**对应某个文件（或块设备）的某段内容**，作为磁盘数据在内存中的缓存副本，`page->mapping` 指向该文件的 `address_space`，`page->index` 表示是文件内第几页。回收该页时，若内容干净（未被修改）可以直接丢弃（下次访问重新从文件读取即可，无需 swap）；若是脏页（被写过、`MAP_SHARED` 文件映射写入或普通 `write()` 系统调用写入）则需要先**回写（writeback）**到原文件所在的存储设备，而不是写入 swap。

两者共用同一套 `struct page`/LRU 框架管理（见第3~5、38~48题的页面分配器与回收讨论），但"数据的最终归宿"不同——匿名页靠 swap，文件页靠原文件——这个区别直接决定了 `kswapd`/直接回收路径处理它们时的不同策略（详见第16~19题）。

### 3. page数据结构中有一个锁，我们称为页锁，请问trylock_page()和 lock_page()有什么区别？

`PG_locked` 是 `page->flags` 中的一个标志位，作为该页的一把**互斥锁（page lock）**，用于串行化对该页内容/状态的并发操作（如同时避免多个路径同时对同一页做 I/O、迁移、回收等）。

- **`lock_page()`**：**阻塞式**获取页锁——若该页当前已被其它路径锁住，调用者会**睡眠等待**（放入等待队列，被唤醒后重试），直到成功拿到锁才返回。适用于允许睡眠的上下文，且调用者确实需要独占访问该页才能继续（如必须要等这一页的 I/O 完成）。
- **`trylock_page()`**：**非阻塞式**尝试获取页锁——若锁已被占用，**立即返回失败**（不睡眠、不等待），由调用者自行决定后续处理（如跳过这一页、稍后重试、或退回到其它策略）。适用于不允许睡眠的上下文（如某些内存回收的快速路径、中断相关代码），或者调用者只是"能拿到就处理，拿不到就算了"的场景（如 `kswapd`/直接回收扫描 LRU 时，若某页正被别的路径锁住，通常选择跳过它而非死等，以避免大范围阻塞）。

### 4. 请画出page数据结构中flags成员的布局示意图。

`page->flags` 是一个 `unsigned long`，按位划分为**状态标志位区**与**元数据编码区**（section/node/zone/kasan-tag 等，具体位宽因 `CONFIG_SPARSEMEM`/`CONFIG_NUMA`/位宽而异），从最高位到最低位大致为：

```
高位                                                                        低位
+-----------+-----------+-----------+---------------------------------------+
| SECTION   |   NODE    |   ZONE    |         狀態旗標位 (PG_xxx)             |
| (可能與LAST_CPUPID共用) | (若NUMA)  | (區分該page屬於哪個zone)  | PG_locked, PG_error,     |
|           |           |           | PG_referenced, PG_uptodate,           |
|           |           |           | PG_dirty, PG_lru, PG_active,          |
|           |           |           | PG_slab, PG_writeback,                |
|           |           |           | PG_reserved, PG_swapcache, ...        |
+-----------+-----------+-----------+---------------------------------------+
```

具体每个字段占多少位由编译期常量决定（`SECTIONS_WIDTH`、`NODES_WIDTH`、`ZONES_WIDTH` 等，取决于开启了 `SPARSEMEM`/`SPARSEMEM_EXTREME`/`NUMA` 与否，以及物理地址位宽），最低若干位固定留给一组 `enum pageflags`（`include/linux/page-flags.h`）中定义的位标志，通过 `PageLocked(page)`、`SetPageDirty(page)`、`ClearPageActive(page)` 等宏访问；本设备因 `# CONFIG_NUMA is not set`（单节点 UMA），`NODE` 字段实际宽度为 0（无需编码 node id），`SECTION`（`CONFIG_SPARSEMEM`）字段用于配合第3章第6题所述的、DTB 给出的 3 段不连续物理内存做 `pfn_to_page()`/`page_to_pfn()` 的换算。

### 5. 请列举page数据结构中_refcount和_mapcount计数的使用案例。

**`_refcount` 使用案例**：
- 从伙伴系统 `alloc_pages()` 分配出一个新页时初始化为 1；
- `get_page()`/`put_page()`：内核任意代码路径临时需要"保证这个页在我用完之前不会被释放"时的标准做法（如 `get_user_pages()` 钉住用户页供 DMA 使用）；
- page cache 索引该页时持有一份引用（被加入 `address_space` 的 XArray 时 `_refcount` +1）；
- `_refcount` 归零触发 `free_pages()` 真正释放回伙伴系统。

**`_mapcount` 使用案例**：
- 每次 `page_add_anon_rmap()`/`page_add_file_rmap()`（在缺页处理、`mmap`、`fork` 建立子进程页表映射时调用）让某个 PTE 指向该页时 `_mapcount` +1；对应的 `page_remove_rmap()`（`munmap`、进程退出回收页表、页被换出前拆除映射时）则 -1；
- 第4章第42题的写时复制判断：`page_mapcount(page) == 1` 才允许"复用"而非复制；
- 第35题所述 KSM 的 `write_protect_page()` 中 `page_mapcount(page) + 1 + swapped != page_count(page)` 这类一致性检查；
- 内存回收扫描 LRU 时，`page_mapcount(page) > 0` 用于判断该页当前是否仍被某进程实际映射（是则需要先做反向映射 unmap，才能真正回收，见第5章 rmap 相关题）。

### 6. 请简述page数据结构中mapping成员的作用。

`page->mapping` 是一个被"打了标记复用"的指针，依据其**最低两位**决定含义：

- 最低位为 **0** 且指针非空：`page->mapping` 指向一个 **`struct address_space *`**，表示这是一个**文件映射页/page cache 页**，该 `address_space` 通常挂在某个 `inode` 下，代表该页属于哪个文件、结合 `page->index` 确定是文件内哪一页；
- 最低位为 **1**（即 `PAGE_MAPPING_ANON` 标记）：实际存的是一个 **`struct anon_vma *`** 指针（需要先清除最低位才能得到真实地址），表示这是一个**匿名页面**，通过它可以找到反向映射所需的 `anon_vma`（进而找到所有映射该页的 VMA，见第7、8题）；
- `page->mapping == NULL`：表示该页当前未被任何文件/匿名地址空间"拥有"（如刚从伙伴系统分配、尚未建立任何映射的页，或是某些内核内部专用页）。

这种"用指针最低若干位（因为指针本身按字对齐，低位天然是 0）编码额外类型信息"的技巧在内核中很常见，`page_mapping()`/`PageAnon()` 等辅助函数负责正确解析这个字段，使内存管理的通用代码路径（如缺页、回收、迁移）可以用统一的 `page->mapping` 字段、通过类型判断分别导向"匿名页反向映射路径"或"文件页地址空间路径"。

### 7. 在Linux 2.4.x内核中，如何从一个页面中找到所有映射该页面的VMA？ RMAP可以带来哪些便利？

**Linux 2.4.x（旧版本，无 RMAP）**：内核**没有为匿名页维护任何"反向"索引**——要找出"哪些 VMA/进程映射了这个物理页"，唯一的办法是**从进程侧正向遍历**：拿到系统中所有进程的 `mm_struct`，逐个遍历其所有 VMA，再逐个遍历 VMA 覆盖的每一个虚拟地址、查页表，检查该 PTE 是否恰好指向目标物理页——即**O(所有进程 × 所有 VMA × 所有页表项)** 的全局暴力扫描，在需要"回收/换出某个具体物理页之前，必须先拆除所有映射它的 PTE"这类场景（`try_to_unmap`）下开销极其高昂，是当时内存回收性能的一大瓶颈（尤其系统中进程数、内存量增大后几乎不可行）。

**RMAP（Reverse Mapping，反向映射）带来的便利**：为每个匿名页/文件页额外维护一份**从"页"直接指向"哪些 VMA 映射了它"的反向索引**（匿名页通过 `anon_vma`/`anon_vma_chain`，文件页通过 `address_space` 的区间树/`i_mmap`），使得"给定一个物理页，找出所有映射它的 VMA"从 **O(全局进程/页表规模)** 降为 **O(实际映射该页的 VMA 数量)**，与系统总体规模无关，只与"这个页实际被共享的程度"成正比。这让内存回收（尤其是要精确回收特定几个页而非整体扫尽）、页面迁移、`munmap` 局部清理等操作的性能变得可控，是现代 Linux 内存管理能够在大内存、大量进程场景下依然高效工作的关键基础设施之一。

### 8. 阅读Linux 5.0内核RMAP机制的代码，画出父子进程之间VMA、AVC、 AV以及page等数据结构之间的关系图。

```
                    ┌─────────────┐                  ┌─────────────┐
                    │ 父進程 mm_struct │              │ 子進程 mm_struct │
                    └──────┬──────┘                  └──────┬──────┘
                           │ vma                             │ vma
                           v                                 v
                    ┌─────────────┐                  ┌─────────────┐
                    │  VMA (parent) │                │  VMA (child)  │  (fork 後各自獨立的 vm_area_struct，
                    └──────┬──────┘                  └──────┬──────┘   但邏輯上映射同一組匿名頁)
                           │ anon_vma_chain (AVC)            │ anon_vma_chain (AVC)
                           v                                 v
                    ┌─────────────┐                  ┌─────────────┐
                    │  AVC (parent) │ ──same_vma──── │  AVC (child)  │
                    └──────┬──────┘   鏈到各自vma的avc_lock    └──────┬──────┘
                           │ anon_vma                        │ anon_vma
                           v                                 v
                    ┌─────────────────────── AV (anon_vma) 樹 ───────────────────────┐
                    │  root AV (最初 fork 之前，父進程自己的 anon_vma)                    │
                    │     ├── child AV (fork 時為子進程新建，parent 指向 root)            │
                    │  透過 AV.rb_root（紅黑樹）串連所有子孫 AV，方便統一遍歷              │
                    └─────────────────────────────┬───────────────────────────────────┘
                                                    │ (AV->rb_root 中每個 avc 又反過來
                                                    │  掛在對應 page 的 anon_vma 之下)
                                                    v
                                            ┌───────────────┐
                                            │  page (匿名頁)  │  page->mapping 指向其中一個 AV(root)
                                            └───────────────┘
```

要点：`fork()` 时，子进程的每个匿名 VMA 会创建一个**新的 `anon_vma`（AV）**，但通过 `AV->parent`/`AV->root` 指针链接回父进程原来的 AV（形成一棵 AV 树，`root` 始终指向最初那个"祖先" AV）；父、子各自的 VMA 都通过 `anon_vma_chain`（AVC，一个"多对多"关系的中间节点，既链在其所属 VMA 的 `vma->anon_vma_chain` 链表上、也链在其所属 AV 的 `avc->rb`/`same_anon_vma` 红黑树上）与自己的 AV 关联；页面的 `page->mapping` 只指向**根 AV**，但要找"映射该页的所有 VMA"时，通过根 AV 的 `rb_root` 遍历到所有子孙 AV，再经每个 AV 下挂的 AVC 找到各自的 VMA——这样即使父子进程各有独立的 AV，也能统一、完整地找到所有共享该匿名页的 VMA。

### 9. 在Linux 2.6.34内核中，RMAP机制采用了新的实现，Linux 2.6.33内核和 之前的版本中的RMAP机制称为旧版本RMAP机制。那么在旧版本RMAP机制 中，如果父进程有1000个子进程，每个子进程都有一个VMA，每个VMA里面有 1000个匿名页面，当所有的子进程的VMA同时发生写时复制时会是什么情况 呢？

**旧版本 RMAP（2.6.33 及之前）**：每个匿名页的反向映射是通过 `page->mapping` 直接指向**唯一一个共享的 `anon_vma`**、并把所有映射该 `anon_vma` 的 VMA 直接挂在这**同一个 `anon_vma` 的链表**上实现的——即无论有多少个子进程 `fork()` 出来，只要它们的匿名 VMA 是从同一个父 VMA 复制而来，就都共享**同一把 `anon_vma`**（及其保护锁），VMA 直接以链表形式全部挂在其下，没有第8题所述"每个进程各自独立 AV、再用 AVC/AV 树间接关联"的分层结构。

在题述场景（1000 个子进程，每个各有一个 VMA，且发生 COW）下：
- 所有 1000 个子进程的 VMA 全部挂在**同一个 `anon_vma`** 之下，任何一次需要遍历"哪些 VMA 映射了某个匿名页"的操作（如内存回收扫描、页面迁移，或本题的 COW 处理路径中检查共享状态），都必须**线性遍历这条长达 1000 项的链表**，即使实际只关心其中一两个 VMA；
- 更严重的是：这个共享的 `anon_vma` 只有**一把锁**（`anon_vma->lock`），1000 个子进程各自的缺页/COW 处理若都需要访问、修改与这个 `anon_vma` 相关的反向映射信息，会在这一把全局锁上产生**激烈的锁竞争（lock contention）**——多核同时对不同子进程的不同页面做 COW，本来逻辑上互不相干，却会因为共享同一把 `anon_vma` 锁而互相阻塞、串行化，导致本应可以并行的操作性能急剧下降（这就是官方 changelog 中提到的、促使 2.6.34 重新设计 RMAP 加入 `anon_vma_chain` 分层结构的直接动机——新方案让每个子进程拥有自己独立的 `anon_vma`，只在真正需要"遍历所有共享者"这种低频操作时才通过 AV 树把它们关联起来，日常的缺页/COW 处理各自只需碰自己的 AV，互不阻塞）。

### 10. kswapd内核线程何时会被唤醒？

`kswapd`（每个 NUMA 节点一个，本设备单节点即一个 `kswapd0`）在**页面分配路径检测到某个 zone 的空闲页低于该 zone 的 `low` 水位线**时被唤醒（`wakeup_kswapd()`，通常在 `__alloc_pages_slowpath()` 分配请求发现快速路径不满足水位而进入慢速路径的早期阶段调用）。被唤醒后 `kswapd` 转入**异步的后台回收（background reclaim）**：从当前分配请求的 zone 开始，尝试把该 zone 的空闲页量从 `low`（或更低）回收提升到 `high` 水位线，回收足够后重新进入睡眠；如果回收速度跟不上分配速度（`low`/`min` 水位持续被击穿），发起分配的进程自身则会转入**同步的直接回收（direct reclaim）**路径，亲自参与扫描回收（见第39题快速/慢速路径的讨论）。

实测本设备 `/proc/vmstat` 中 `kswapd_low_wmark_hit_quickly = 76`、`kswapd_high_wmark_hit_quickly = 6` 说明系统运行以来 `kswapd` 已被唤醒并成功／快速地把 zone 从低水位拉回高水位的次数；同时 `pgsteal_kswapd = 335187` 远大于 `pgsteal_direct = 23254`，说明绝大多数内存回收工作是由 `kswapd` 在后台异步完成的，只有少部分（约 6.5%）落到了更影响应用延迟的直接回收路径，反映系统整体内存压力可控。

### 11. LRU链表如何知道页面的活动频繁程度？

Linux 用**双 LRU 链表（Active/Inactive）**近似实现"访问频繁程度"的排序，而非精确统计每个页面的访问次数（那样开销太大），核心信号是页面的 **`PG_referenced` 标志位**与**页表项的 Accessed 位（硬件维护，ARM64 对应 `PTE_AF`）**：

- 每当一个页被访问（读/写命中，硬件自动置位对应 PTE 的 Access Flag），或某些路径显式调用 `mark_page_accessed()`，就会设置该页的 `PG_referenced` 位（如果已经设置过，再次被访问会被视为"较活跃"的信号，推动它从 Inactive 链表提升到 Active 链表）；
- 当 `kswapd`/直接回收扫描到某页时，会检查它的 `PG_referenced` 以及对应 PTE 的 Accessed 位（并顺带清除，为下一轮统计重新计时），据此判断：**最近被访问过 → 判定为活跃，移到/留在 Active 链表（优先保留）；长期未被访问 → 留在/移入 Inactive 链表，成为优先被回收的候选**；
- 页从 Active 移到 Inactive 需要"经过一次扫描发现未被再次访问"，是一种**近似 LRU（Least Recently Used）而非严格计数**的机制——本质上是"最近是否被用过一次"的粗粒度冷热判断，而非记录精确的访问时间戳/频次，兼顾了实现开销与回收效果。

### 12. kswapd按照什么原则来换出页面？

总体原则是"**优先换出最不可能很快被再次用到的页面**"，具体体现为：

1. **优先从 Inactive 链表（而非 Active）挑选**：Inactive 链表本身就代表"较冷、近期未被访问"的候选集合；
2. **按 `swappiness` 权衡匿名页 LRU 与文件页 LRU 各自贡献多少扫描比例**（见第16题）——决定这一轮回收在匿名页和文件页两条 LRU 上各扫描多大比重；
3. 同一条 LRU 链表内部，从**链表尾部（最久未被移动到头部/最久未被标记 referenced 的一端）**开始扫描，越靠尾部代表越久没有活动迹象；
4. 对遇到的每一页做进一步检查（是否 `mlock` 不可回收、是否正被钉住 `_refcount`/`_mapcount` 异常、是否是脏页需要先回写等，见第18、19题），符合条件才真正回收，否则跳过或移至链表另一端。

本质上是"先分类（匿名/文件、Active/Inactive），再按 LRU 顺序、结合 swappiness 权重、结合页面自身状态（脏/干净、锁定与否）"这一套组合策略，而非单一维度的简单排序。

### 13. kswapd按照什么方向来扫描zone？

与第1章第3题页面分配器的扫描方向相反：`kswapd` 是**从"最低（最需要保护/最珍贵，如 DMA）"的 zone 开始，还是从与之前分配请求相关的目标 zone 开始向下**？准确地说，`kswapd` 会为其管辖的 **node 中所有 zone**，按照与分配请求相同的 `zonelist` 顺序（即从"更高级/更常用"的 zone，如 `ZONE_MOVABLE`/`ZONE_NORMAL`，到"更基础/更受限"的 zone，如 `ZONE_DMA`）**逐一检查每个 zone 是否达到 `high` 水位**，对未达标的 zone 依次进行回收，目标是让"当前分配请求锚定的那个最高 zone 及其下所有更低 zone"整体都恢复到健康水位——这与页面分配器"从高 zone 向低 zone 找空闲页"的方向是一致的（因为 `kswapd` 存在的目的就是提前替分配请求把水位准备好），并非独立的另一套扫描顺序。

### 14. kswapd以什么标准来退出扫描LRU链表？

`kswapd`（`balance_pgdat()`/`kswapd_shrink_node()`）持续扫描回收，直到满足以下**任一**退出条件：

1. **该 node 下所有 zone 的空闲页都已达到（或超过）各自的 `high` 水位线**——回收目标已达成，恢复到充裕状态，可以安心睡眠；
2. **达到本轮设定的最大扫描/回收页数上限（`nr_to_reclaim`/`SWAP_CLUSTER_MAX` 等相关限制）仍未达标，但已经扫描了足够多轮（`priority` 从高优先级数值降到 0，即已经尝试了最"用力"的扫描强度）**——说明当前内存压力下已经尽力而为，避免无限扫描占用 CPU；
3. 扫描发现**基本没有可回收的页面了**（如可回收 LRU 长度过短、或者 `sc.nr_scanned` 相对 `sc.nr_to_reclaim` 的比例显示继续扫描性价比很低），提前放弃本轮；
4. 收到**更高优先级的中断/需要让路**（如 `kthread_should_stop()` 表示该内核线程要被停止）。

一句话：**"达到目标水位（成功）" 或 "已尽最大努力仍无法达到（放弃本轮，等待下次被唤醒再试）"** 两种情况都会导致退出当前扫描，回到睡眠等待下一次被唤醒。

### 15. 移动设备操作系统（如Android操作系统），没有交换分区或者交换文 件，kswapd会扫描匿名页面LRU吗？

一般**不会**（或即使扫描也基本不会真正回收）。因为**匿名页要被回收，必须先能够换出到 swap**（把数据保存下来，否则直接丢弃即意味着数据永久丢失），如果系统**根本没有配置任何 swap（分区/文件/zram）**，内核的回收逻辑（`get_scan_count()`）在计算匿名页与文件页各自应该分配多少扫描比重时，会检测到 `total_swap_pages == 0`（无可用 swap 空间），从而**把匿名页 LRU 的扫描比重直接设为 0**，本轮回收全部压力都放在文件页 LRU 上（文件页无论如何都可以直接丢弃、下次访问重新从文件读回，不依赖 swap）。

不过要注意：现代 Android 实际上通常会配置 **zRAM（压缩内存作为"swap"设备）** 或近年的 **内存压缩 + 匿名页回写（如 `ZSWAP`）**等机制间接提供"匿名页可以被换出"的能力，此时 `total_swap_pages > 0`，`kswapd` 才会正常按 `swappiness` 权重去扫描匿名页 LRU；如果连 zRAM 都没有配置（较老的、极简的嵌入式系统），匿名页确实完全不会被 LRU 扫描/换出，只能通过 `oom_killer` 直接杀进程来释放内存。

实测本设备 `free -h` 显示 `Swap: 3.9Gi`（`/proc/meminfo`：`SwapTotal: 4064756 kB`），确实配置了较大的 swap 空间（很可能是 zRAM 或普通 swapfile），且 `vm.swappiness = 100`（见第16题），说明本设备的回收策略会积极地对匿名页 LRU 进行扫描与换出。

### 16. swappiness的含义是什么？kswapd如何计算匿名页面和页面高速缓存之 间的扫描比重？

`vm.swappiness`（`0~200`，`/proc/sys/vm/swappiness`）是一个**倾向性调节旋钮**，表示"内核在选择回收匿名页 vs 回收文件页（page cache）之间的相对偏好程度"：数值越**高**，越倾向于**更早、更多地换出匿名页**（把宝贵的物理内存优先留给 page cache，认为磁盘 I/O 缓存收益更大）；数值越**低**，越倾向于**优先回收文件页 cache、尽量少动匿名页**（认为匿名页/进程内存更重要，宁可牺牲文件缓存命中率）；`0` 表示尽量不回收匿名页（除非文件页已经无法满足回收目标），`100` 是传统意义上的"两者同等对待"基准点，`>100`（较新内核支持到 200）则可以进一步表达"比文件页更愿意回收匿名页"的倾向。

`get_scan_count()` 的大致计算逻辑：以 `swappiness` 与 `(200 - swappiness)` 分别作为匿名页、文件页两条 LRU 的**基础权重比例**，再结合两者各自当前的 Active/Inactive 比例、最近的 `refault`（换出后又马上被换入，说明回收"回收错了"，用作动态修正信号，抑制过度回收某一类）等因素做调整，最终得到"本轮回收总共要扫描 N 个页面时，其中大约分配给匿名页 LRU 多少、文件页 LRU 多少"的具体扫描数量。

实测本设备 `vm.swappiness = 100`（并非常见桌面/服务器默认的 60），说明该嵌入式系统的配置策略是"匿名页和文件页同等对待、都愿意积极换出"，结合前述 `SwapTotal` 较大（约 4GB），符合内存相对有限（8GB）、需要更积极利用 swap（很可能是低开销的 zRAM）来换取更大可用内存的嵌入式设备典型调优思路。

### 17. 当系统中充斥着大量只访问一次的文件访问时，kswapd如何来规避这 种访问？

这类"扫一遍就不再访问"的文件访问模式（如大文件顺序读取、`cp`/备份等一次性 I/O）如果按普通 LRU 逻辑处理，会把这些只用一次的页面推到 Active 链表头部，**挤占（thrash）掉真正的热点数据**，造成"有意义的缓存被无意义的一次性数据冲刷掉"的问题。内核用以下机制规避：

- **`workingset` 检测（refault distance）机制**：页面被回收前会在其对应的基数树/XArray 位置留下一个"影子（shadow）"记录，包含"当时它在 Inactive 链表中的大致位置（能推算出经过了多久/多少次回收才轮到它）"；当（若）该页后来又被重新访问（refault）时，内核比较"这次访问距离上次被回收经历了多长的 LRU 距离"，据此判断：如果这个距离远远超过当前 Inactive 链表容量，说明它属于典型的"扫一遍就不会再碰、纯粹一次性"的访问模式，**不应该被提升进 Active 链表**（避免污染热点数据集）；只有 refault 距离较短（说明确实是真正的热点、只是暂时被挤出去了）时，才认为是"值得提升为活跃页"的信号；
- 该机制的统计即体现在 `/proc/vmstat` 的 `workingset_refault_*`/`workingset_activate_*`/`workingset_restore_*` 等计数上——实测本设备 `workingset_refault_file = 79979` 而 `workingset_activate_file` 同为 `79979`（几乎相等），说明目前发生的文件页 refault 大多数确实被判定为"真正的工作集、应当重新激活"，尚未观测到大规模"一次性扫描污染"的情况。

### 18. 在回收页面高速缓存时，对于脏的页面高速缓存，kswapd会马上回写 吗？

**通常不会立即同步回写**。原因是磁盘/存储 I/O 相对内存操作极其缓慢，如果 `kswapd` 扫描到一个脏页就同步等待其写回完成，会严重拖慢整个回收扫描的推进速度（阻塞在单个慢速 I/O 上）。因此常见做法是：

- 扫描到脏页时，**优先跳过**（留在 LRU 中，或移动位置），转而继续扫描寻找**干净、可以直接回收**的页面，优先完成本轮回收目标；
- 真正需要处理脏页回写的，通常交由**独立的回写机制（如 `writeback` 相关的 `wb_workqueue`、定期的 `pdflush`/`flusher` 线程，或文件系统日志相关机制）异步进行**，与 LRU 回收扫描解耦；
- 只有在**回收压力很大、干净页已经不够用、且脏页比例过高**的情况下，`kswapd`/直接回收路径才会退而求其次，**主动发起（或等待）少量脏页的同步/异步回写**，但会尽量克制（内核有 `nr_immediate`/`vmscan_immediate_reclaim` 等统计和限流逻辑，避免大量脏页同时触发 I/O 风暴）。

实测本设备 `/proc/vmstat` 中 `nr_vmscan_immediate_reclaim = 330`（相对 `pgscan_kswapd` 逾百万次扫描而言占比很小），说明绝大多数回收扫描过程中遇到脏页时确实是"绕开、不硬等"，只有少数情况才触发这种"立即回收（含等待回写）"的较重路径。

### 19. 内核中有哪些页面会被kswapd回写到交换分区？

只有**匿名页（Anonymous Page）**及其等价物（如 `tmpfs`/`shmem` 页——这类页虽然挂在 `address_space` 下、行为上更接近文件页，但本质没有真实磁盘文件，回收时同样需要写入 swap 才能保留内容）会被写入**交换分区/交换文件（swap）**；普通的**文件映射页面高速缓存（page cache）**即使是脏页，也是回写到它们**各自对应的原始文件**（通过文件系统的 writeback 机制），而**不会**被写到 swap——因为文件页天生就有一个"真实的家"（磁盘上的原文件），没有必要（也不应该）额外占用 swap 空间。

因此，"回写到交换分区"这个动作专属于**匿名页 LRU** 一侧的回收路径（`shrink_page_list()` 处理匿名页分支时，若该页是脏的/需要换出，调用 `pageout()` → 交换子系统的 `swap_writepage()`），文件页分支走的是完全不同的 `->writepage`（文件系统提供的回写回调）路径。

### 20. 请简述匿名页面的生命周期。

1. **产生**：进程通过 `malloc`/堆增长（`brk`）/`mmap(MAP_ANONYMOUS)`/栈增长等方式建立匿名 VMA；首次访问触发缺页，分配（清零的）物理页并建立 PTE 映射（见第21题）；
2. **活跃使用**：被进程正常读写访问，`PG_referenced`/PTE Access Flag 被置位，倾向留在/晋升到 **Active Anon LRU**；
3. **变冷 / 被回收候选化**：一段时间未被访问，随扫描逐步从 Active 降到 **Inactive Anon LRU**；
4. **（可选）加入 swap cache 并换出**：当发生内存回收且被选中，先分配 swap slot、把内容写入 swap 设备，同时该页短暂存在于 **swap cache** 中（作为"内存与 swap 设备之间的过渡态"）；写回完成后，拆除页表映射（PTE 被替换为记录 swap 位置的 swap entry），最终真正释放该 `struct page`（`_refcount` 归零）回伙伴系统；
5. **（可选）换入**：进程之后又访问了该虚拟地址，触发 `do_swap_page()`（主缺页，见第4章第38、39题），从 swap 设备读回数据，重新分配物理页、加入 swap cache、建立新 PTE 映射，恢复到"活跃"状态（此时该 swap slot 若仍被引用可能暂不释放，直到确认没有其它路径还需要它）；
6. **最终回收/释放**：进程退出、`munmap`、或该 VMA 段被 `madvise(MADV_DONTNEED)` 等主动丢弃时，拆除映射、若在 swap 中也一并释放对应 swap slot，`_refcount` 归零，物理页归还伙伴系统。

整个生命周期是"分配 → 活跃 ⇄ 不活跃（LRU 间往返，取决于是否被访问）→（可能反复经历换出/换入）→ 最终释放"的循环/往返过程，不是一条单向直线。

### 21. 在什么情况下会产生匿名页面？

- 进程堆区增长（`brk()`/`sbrk()`）；
- `mmap()` 使用 `MAP_ANONYMOUS`（不关联文件的匿名映射，常用于线程栈、大块动态内存如 glibc `malloc` 的大对象走 `mmap`）；
- 进程（用户态）栈自动向下增长（内核检测到访问地址落在栈 VMA 之下但在允许自动扩展的范围内）；
- **写时复制（COW）**产生的新页：`fork()` 后子进程对共享的只读页发生写访问、或私有文件映射（`MAP_PRIVATE`）发生写访问时复制出的私有副本，即使原页是文件页，COW 复制出的这份私有副本此后也按**匿名页**管理（内容已与原文件脱钩）；
- `shmem`/`tmpfs`（内存文件系统）本质上也是一种特殊管理的匿名页（虽然挂靠在 `address_space` 下，但没有真实磁盘文件，回收时同样走 swap 路径，行为上更接近匿名页，见第19题）。

### 22. 在什么条件下会释放匿名页面？

- 该页的 `_refcount` 归零，即**再没有任何页表映射、也没有 swap cache/其它内核路径持有引用**——通常由以下操作触发这一系列引用递减：
  - 进程 `munmap()` 显式取消映射对应区域；
  - 进程整体退出（`exit_mmap()` 拆除所有 VMA 及其页表映射）；
  - `madvise(MADV_DONTNEED)`/`MADV_FREE` 等主动告知内核"这段内存不再需要"；
  - 内存回收（`kswapd`/直接回收）扫描到该页且判定应当回收：先换出到 swap（如果尚未在 swap 中）、拆除所有映射它的 PTE（通过 rmap 找到所有共享者，逐一 `unmap`），确认没有其它引用后释放；
- 若是被多个进程共享的匿名页（`fork()` 后父子共享、尚未 COW），需要**所有共享者**都不再映射它（`_mapcount` 归零，且没有其它 `_refcount` 来源如 swap cache 临时引用）才会真正释放物理页——单个进程 `munmap`/退出只是让自己这一份映射消失，`_mapcount`/`_refcount` 相应递减，但只要还有其它进程仍映射着它，物理页依然存活。

### 23. 页面迁移是基于什么原理来实现的？

页面迁移（`migrate_pages()`，`mm/migrate.c`）的基本原理是：**在不改变"逻辑上这段虚拟地址指向的数据内容"的前提下，把数据从一个物理页搬到另一个物理页，并同步更新所有指向它的映射关系**，对上层（进程）完全透明。实现步骤大致为：

1. **隔离（isolate）**：先把目标页从其所在的 LRU 链表（或其它内核数据结构如 page cache）中摘除，防止迁移过程中被并发的分配器/回收器/其它路径干扰；
2. **在目标位置分配一个新的空闲页**（大小、属性与源页一致）；
3. **拆除映射并搬运内容**：通过反向映射（rmap，见第7~9题）找到所有映射源页的 PTE，先把它们统一替换成一种特殊的**迁移中间态（migration entry，一种特殊编码的"临时失效"PTE）**，暂时阻止新的访问（访问会触发缺页、被阻塞等待迁移完成）；然后把源页的实际数据内容**拷贝（`copy_page`/`copy_highpage`）**到新分配的目标页；
4. **重新建立映射**：把之前替换成 migration entry 的所有 PTE，改指向新页（同时恢复正常的访问权限位），并更新该页在 page cache（若是文件页）等结构中的索引指向新页；
5. **释放源页**：确认所有映射与索引都已切换完毕后，源页的引用计数归零，释放回伙伴系统；
6. 重新把目标页加入 LRU 等常规内核结构，迁移完成、恢复正常可访问状态。

整个过程通过"先暂时冻结访问（migration entry）→ 拷贝数据 → 切换所有引用指向新页 → 解除冻结"来保证迁移过程中不会有任何路径读到"一半旧一半新"的不一致数据，也不会发生数据丢失。

### 24. 内核中有哪些页面可以迁移？

- 标记为 **`__GFP_MOVABLE`** 分配出来的页面，典型是**用户态的匿名页与（大部分）文件页面高速缓存**（属于 `MIGRATE_MOVABLE` 迁移类型，可参与内存规整/迁移，见第4章第4题、本章第45题）；
- 部分**可迁移的内核对象**，如某些通过专门接口注册了迁移回调（`address_space_operations->migrate_page` 或更新的 `migrate_folio`）的特殊页面（如某些文件系统的元数据页、`zsmalloc`/`zswap` 使用的压缩页在支持的情况下）；
- **CMA（Contiguous Memory Allocator）区域内的页面**：CMA 区平时可以被 `MIGRATE_MOVABLE` 类型的普通分配占用，但当真正需要该区域做大块连续 DMA 分配时，会强制把当前占用它的可迁移页迁移走，腾出连续空间（这正是 CMA 依赖页面迁移机制才能工作的原因，实测本设备 `/proc/pagetypeinfo` 中 `DMA` zone 有 `128` 个 `CMA` 类型的 pageblock，对应 `/proc/meminfo` 的 `CmaTotal: 262144 kB`）；
- 用于 **NUMA 负载均衡（AutoNUMA）** 场景下把页面迁移到访问它更频繁的 CPU 所在节点更近的内存（本设备因 `CONFIG_NUMA` 未开启，此用途不适用）。

### 25. 内核本身使用的页面是否可以迁移？为什么？

**通常不可以（默认属于 `MIGRATE_UNMOVABLE` 类型）**。原因：内核内部大量数据结构（如页表页本身、`slab`/`slub` 分配出的各种内核对象、`kmalloc` 出来的缓冲区等）往往通过**裸的内核虚拟地址（线性映射区地址）、甚至直接的物理地址/PFN**被内核代码中大量、分散、且未必都能被统一追踪的位置直接引用（不像用户态页面那样，所有引用都规范地通过 VMA/页表这一套受控机制暴露、可以用统一的 rmap 机制找出所有引用者）——内核没有一个类似"反向映射"的通用机制能保证枚举出"当前有哪些指针正指向这个内核页"，因此**无法安全地做『暂停访问 → 拷贝 → 切换所有引用 → 恢复』这一整套迁移流程**，贸然迁移会导致某些遗漏更新的指针仍指向旧（已被释放/挪作他用）的物理地址，造成极其隐蔽且危险的内存错误。

因此像页表本身占用的页、`slab` 对象所在的页等，默认都分配自 **`MIGRATE_UNMOVABLE`** 迁移类型（见第45题），伙伴系统会尽量把这类"钉死在原地"的分配都集中放在专门的 `pageblock` 中，避免与可迁移页混杂在一起、影响可迁移区域的连续性。少数**特别设计过、显式实现了迁移回调**的内核对象（如部分文件系统的元数据、`zsmalloc` 池中的页）例外，可以支持受控的迁移。

### 26. 在页面迁移的过程中需要注意些什么？

- **一致性与原子性**：迁移过程中不能出现"部分调用者已经看到新页、部分还停留在旧页"的中间不一致状态，需要通过 migration entry（第23题）等机制确保迁移期间的并发访问被正确阻塞/重定向；
- **引用计数处理**：迁移开始前要正确隔离（防止被 LRU 扫描/其它路径并发操作），迁移过程中要小心处理源页/目标页的 `_refcount`/`_mapcount`，避免计数错乱导致提前释放或永久泄漏；
- **脏页/回写状态**：若源页是脏页（尤其是文件页），需要保证内容被完整、正确地搬到新页，且后续回写机制（`writeback`）能正确地对新页生效，不能丢失"待回写"这一状态；
- **锁的正确获取顺序**：涉及页锁（`PG_locked`）、`i_pages`/`address_space` 的锁、`anon_vma` 锁、页表锁等多把锁，必须按照内核规定的顺序获取，避免死锁；
- **失败回滚**：迁移过程中若目标页分配失败、或发现该页正被其它不兼容的操作（如正在做 direct I/O、被钉住 `_refcount` 异常升高）占用，需要能够干净地**回滚**——恢复源页的正常映射与状态，不能让页面停留在"迁移到一半"的损坏状态；
- **不可迁移的页要提前排除**（第25题），迁移框架本身需要先判断该页是否属于可迁移类型，避免对不支持迁移的页盲目尝试。

### 27. 什么是传统LRU页面和非LRU页面？

- **传统 LRU 页面（LRU page）**：会被正式挂入某个 zone/`memcg` 的 **Active/Inactive Anon/File LRU 链表**、参与标准的 `kswapd`/直接回收扫描逻辑的页面，即前述第2、11~19题讨论的匿名页与文件页 cache 主体，占内核管理的"可正常按 LRU 冷热规律回收"的绝大多数页面。
- **非 LRU 页面（Non-LRU page）**：**不挂在标准 LRU 链表上**、有着自己独立生命周期/回收规则管理的一类特殊页面，典型如：`slab`/`slub` 分配出的内核对象所在页（由 slab 分配器自己的机制管理增减，见第4章11题）、页表本身占用的页、大部分 `MIGRATE_UNMOVABLE` 类型的内核专用页、以及一些通过 `PG_slab` 等专用标志位区分、明确"不参与常规页面回收算法"的页面。内核用页标志（如 `PageLRU()`）来判断一个页当前是否处于 LRU 管理体系内，只有 `PageLRU()` 为真的页才会被 `shrink_page_list()` 这类标准回收路径处理，非 LRU 页面依赖各自子系统自身的回收/收缩钩子（如 `shrinker` 接口，slab 收缩、`shrink_slab()`）参与整体内存压力响应。

### 28. 内存规整是基于什么原理来实现的？

**内存规整（compaction）**用于解决**外部碎片化（第47题）**——即总的空闲页数量够，但都是分散的小块、缺乏足够大的连续空闲块可用。其原理与经典的"磁盘碎片整理"思路类似，但更巧妙地利用了页面迁移机制（第23题）：在一个 zone 内同时维护**两个扫描指针**：

- **迁移扫描指针（migrate scanner）**：从 zone 的**低地址端**向高地址扫描，寻找**已被占用、且属于可迁移类型（`MIGRATE_MOVABLE`）**的页面（这些页可以被安全地挪走）；
- **空闲扫描指针（free scanner）**：从 zone 的**高地址端**向低地址扫描，寻找**当前空闲**的页面，作为迁移的目标位置；

两个指针相向而行：每找到一批"可迁移的占用页"与相应数量的"空闲目标页"，就调用页面迁移机制，把低地址端的占用页**迁移搬运到高地址端的空闲页**中去，这样一来，低地址端原本"占用页与空闲页犬牙交错"的区域会被逐渐清空、腾出连续的大块空闲空间，而高地址端则相应地被占用页填满——整体效果是把空闲空间从"分散在各处的小碎片"**规整聚拢**为"集中在低地址端的大块连续空闲区间"，从而重新有能力满足高阶（大块连续物理内存）分配请求。两指针相遇（或提前达到预设的规整目标/时间预算）时本轮 compaction 结束。

### 29. 如何触发内存规整？

- **直接（同步）触发**：在页面分配的慢速路径中，当发现请求的高阶（`order > 0`）连续内存无法满足、而水位检查显示总空闲页数其实是够的（说明问题出在碎片化而非真正缺内存）时，`__alloc_pages_slowpath()` 会调用 `try_to_compact_pages()` 现场发起一次同步的内存规整，规整完成后立即重试分配；
- **后台异步触发（`kcompactd`）**：类似 `kswapd`，系统为每个 node 启动一个 **`kcompactd`** 内核线程，当检测到某 zone 的**碎片化指数（fragmentation index）**偏高、或高阶水位（`min_free_kbytes`/`watermark_boost` 结合的高阶专属检查）不满足时被唤醒，在后台异步做规整，尽量赶在真正的分配请求到来之前把碎片提前整理好，减少分配路径同步等待规整的延迟；实测本设备 `/proc/vmstat` 中 `compact_daemon_wake = 100`，说明 `kcompactd` 确实在系统运行过程中被唤醒过多次；
- **用户主动触发**：写 `/proc/sys/vm/compact_memory`（触发全系统所有 zone 的一次同步规整）、或针对具体 node 的 `/sys/devices/system/node/nodeN/compact`，常用于运维/调试场景手动整理内存碎片。

### 30. 哪些页面适合做内存规整？哪些页面不适合做内存规整？

- **适合**：属于 **`MIGRATE_MOVABLE`** 迁移类型的页面——典型是用户态匿名页、大部分文件页面高速缓存，这些页面的所有引用者都规范地通过页表/`address_space` 暴露、有完整的反向映射信息，可以安全地执行"迁移扫描指针"所做的搬运操作（本质就是调用第23题所述的页面迁移）；
- **不适合**：
  - **`MIGRATE_UNMOVABLE`** 类型的页面（第25题所述的内核内部页表页、`slab` 对象页等）——没有通用的反向映射机制，无法安全迁移；
  - 被 **`mlock()`** 锁定、标记为 `PG_mlocked`/`VM_LOCKED` 的页面——用户显式要求"不允许被换出/挪动"，规整也应尊重这一约束（除非配置了 `vm.compact_unevictable_allowed`，允许规整触及不可回收但可迁移的页，本设备实测 `vm.compact_unevictable_allowed = 1` 即开启了这一放宽）；
  - 正在进行 **direct I/O**（如某驱动正对该物理页做 DMA 传输）等场景下被临时钉住（`_refcount` 异常升高、`PageWriteback()`/相关标志位表明正处于关键 I/O 状态）的页面——此时迁移它风险很高（DMA 硬件并不知道页面被挪动了，硬件仍会往旧的物理地址读写），规整逻辑会检测并跳过这类页面；
  - `MIGRATE_CMA` 类型的页虽然本身设计为"总是可迁移"（第24题），但要额外遵守 CMA 区域自身的分配约束。

### 31. KSM是基于什么原理来合并页面的？

KSM（Kernel Samepage Merging）的原理：**扫描被应用主动标记为"可合并"（通常通过 `madvise(MADV_MERGEABLE)`）的匿名页，找出内容完全相同的多个物理页，将它们合并为一份只读共享的物理页，原来分别指向各自独立副本的多个 PTE 全部改为指向这唯一一份共享页（并置为只读），从而节省物理内存**（典型受益场景：虚拟机场景下多个客户机操作系统内存中大量相同的内核代码/常见库/全零页；或桌面浏览器多个标签页/多进程共享大量相同的渲染缓存内容）。当其中任何一个引用者之后尝试写入这份共享页时，走标准的**写时复制（COW，第4章41、42题）**路径，复制出一份私有副本再写入——因此 KSM 合并页在使用体验上与普通"多个进程通过 COW 共享同一份只读数据"完全一致，唯一区别是这份共享关系是内核**主动扫描发现相同内容后建立**的，而非天然来自 `fork()`/共享映射。

### 32. 内容相同的页面在KSM里是如何被扫描和合并的？其工作流程是怎么 样的？

`ksmd` 内核线程周期性（由 `sleep_millisecs`、`pages_to_scan` 两个 sysfs 参数控制扫描节奏与每轮扫描量，实测本设备 `sleep_millisecs=20`、`pages_to_scan=100`）遍历所有注册为 `MADV_MERGEABLE` 的匿名页，工作流程大致：

1. 对候选页计算一个**校验和（checksum）**，先做低成本的初步比较；
2. 到 **不稳定树（unstable tree，一棵按内容/校验和排序的红黑树，`ksm_rmap_item->node`）**中查找是否已有内容"疑似相同"的页——不稳定树中的页可能后续还会被修改（"不稳定"因此得名），比较时需要逐字节核实内容确实完全一致；
3. 若在不稳定树中找到内容完全相同的页，则触发合并：把两者都变为只读，将其中一个提升为 **稳定树（stable tree，`ksm_stable_node`，内容一经进入即视为"已确认可长期共享、不会再变"）**中的一个节点，两个原页面的映射关系（`ksm_rmap_item`）都挂到这一个 `stable_node` 的 `hlist` 下，其中一份物理页被真正释放，所有相关 PTE 改为指向另一份；
4. 若稳定树中本来就已存在内容相同的页（更常见的情况——只要系统运行一段时间，稳定树里已经积累了不少"热门重复内容"），新扫到的候选页直接与稳定树中的对应节点比较，相同则直接合并进去（追加到该 `stable_node` 的 `hlist`），不必再经过不稳定树；
5. 后续若某个共享页被写入（COW），对应的 `ksm_rmap_item` 从该 `stable_node` 的 `hlist` 摘除，恢复为普通独立的匿名页，重新参与后续的扫描（可能之后又与其它内容相同的页再次被合并）。

### 33. 若稳定的节点的hlist中堆积了几百万个rmap_item，那么对系统会产生 什么影响？

- **稳定树本身的比较开销增大**：`stable_node` 挂载在稳定树（红黑树）中，若某几个"超级热门"内容对应的 `hlist` 极其庞大，虽然不直接影响红黑树本身按内容比较/查找单个节点的复杂度（那仍是 O(log N)，N 是不同"内容"的种类数，与某一节点下挂了多少 `rmap_item` 无关），但**任何需要遍历该 `stable_node->hlist`（如该共享页需要被回收/迁移/规整时，必须通过 rmap 机制找出所有引用它的 VMA/`rmap_item` 逐一处理，见第7~9题反向映射的开销讨论）的操作，其时间复杂度会随 `hlist` 长度线性增长**——如果 `hlist` 长达数百万项，任何这类操作都会变得极其缓慢，长时间占用相关的锁（如 `stable_node` 或对应页锁），阻塞其它并发路径；
- **内存迁移/规整（第26、28~30题）的代价急剧上升**：若这个被大量共享的物理页恰好需要参与页面迁移或内存规整，需要逐一更新数百万个 PTE 映射，可能导致长时间的锁持有与延迟尖峰；
- **正是这一问题**促使后续内核版本引入了 **`stable_node_chain`/`stable_node_dup`**（"节点链"机制，见第34题）：把原本"一个 `stable_node` 挂一条超长 `hlist`"的结构，拆分/限制为多个"重复节点（dup）"、每个 dup 下的 `hlist` 长度被 `max_page_sharing`（实测本设备该 sysfs 值为 `256`）上限约束，超出上限后新的相同内容页不再无限制地往同一个物理页合并，而是允许保留少量"冗余"的独立副本，用可控的内存节省幅度换取避免上述性能灾难。

### 34. 新版本的KSM对稳定的节点做了哪些优化？

针对第33题所述问题，新版本 KSM 引入了：

1. **`max_page_sharing` 上限**（`/sys/kernel/mm/ksm/max_page_sharing`，本设备实测值 `256`）：限制单个共享物理页最多能被多少个 `rmap_item`/映射关系共享，一旦某个 `stable_node` 下的 `hlist` 长度达到这个上限，后续再发现内容相同的新页时，**不再继续往这唯一一个物理页合并**，而是分裂/建立新的"重复节点（dup）"；
2. **`stable_node_chain`/`stable_node_dup` 结构**（第32题结构体定义中可见 `STABLE_NODE_CHAIN` 特殊标记、`hlist_dup`/`list` 字段）：把"内容相同"这一组关系组织成一条**节点链（chain）**，链上挂着多个**重复节点（dup）**，每个 dup 各自独立维护一份不超过 `max_page_sharing` 长度的 `hlist`——原来"一个内容对应恰好一个物理页、一条可能无限长的 hlist"，变成"一个内容可以对应多个（数量随需要动态增长的）物理页副本，分摊在多个 dup 上，每个 dup 的 hlist 长度都被上限约束"，把原本 O(N) 的最坏情况操作拆分成多个规模可控的小块；
3. **`stable_node_chains_prune_millisecs` 周期性垃圾回收**（本设备实测 `2000` 毫秒）：定期检查/合并这些 dup 链，清理已经不再需要的中间结构，避免链本身随时间无限膨胀；
4. 代价：这一优化本质是"用适度增加的物理内存占用（允许同一内容存在少量副本，而非强制唯一）"来换取"避免单一共享页 hlist 无限增长导致的严重性能劣化"，体现了 KSM 在"内存节省"与"系统响应性/可控性"之间做出的权衡取舍。

### 35. 在KSM机制里，合并过程中把页面设置成写保护的函数 write_protect_page()中有如下判断。 if (page_mapcount(page) + 1 + swapped != page_count(page)) { goto out_unlock; } 这个判断的依据是什么？

这是一个**一致性核实（sanity check）**：确保"当前这个页面除了我们已知的、正准备参与合并的这些映射/引用之外，没有任何其它意料之外的引用者"，否则贸然把它设为只读并继续合并流程可能会破坏那些"未知引用者"原本的语义（如它们本以为自己独占该页、可以随时写入）。

- `page_mapcount(page)`：当前有多少个页表项（PTE）映射着这个页；
- `+ 1`：`write_protect_page()` 自己在核实这一刻，通常也临时持有/即将持有一份对该页的引用（如通过 `page_lock`/`get_page` 或调用路径本身），这个 `+1` 是把"当前这次检查操作自身占用的那一份引用"也算进预期值里；
- `swapped`：如果该页当前同时存在于 **swap cache** 中（尚未被完全从换出状态清理干净），swap cache 本身也会持有一份额外的 `_refcount` 引用，这里作为"已知、可以解释"的引用一并加进预期总数；
- `page_count(page)`（即 `_refcount`）：该页**实际的总引用计数**。

如果 `page_mapcount + 1 + swapped == page_count`，说明"能想到的所有引用来源（页表映射 + 本次操作自身 + swap cache）加起来，正好等于实际总引用数"，**没有遗漏、没有多出来的神秘引用者**，可以放心继续把该页设为写保护、纳入合并流程；如果**不相等**（通常是 `page_count` 更大），说明还有别的代码路径也正持有对这个页的引用（比如正在被 `get_user_pages()` 钉住做 DMA、或被其它子系统临时引用），此时贸然合并/写保护可能与那个"不知情"的引用者产生冲突或数据不一致，因此保守地 `goto out_unlock` 放弃本次合并，等下次扫描再重新尝试。

### 36. 如果多个VMA的虚拟页面同时映射了同一个匿名页面，那么此时page>index应该等于多少？

对于**匿名页**，`page->index` 记录的是：**该页在它所属的那个 `anon_vma` 对应"虚拟地址区间"里的（以页为单位的）偏移量**，具体计算为该页第一次建立映射时所在 VMA 内的相对页偏移（`(address - vma->vm_start) >> PAGE_SHIFT`，再结合 VMA 自身的 `vm_pgoff` 基准做统一换算），**它是以最初建立该匿名页映射的那个 VMA/地址关系为准记录的一个固定值，即使后来这个页又被其它 VMA（比如 `fork()` 产生的子进程 VMA，各自 `vm_start` 可能不同但相对偏移一致，因为是同一段地址区间在父子间的复制）共享映射，`page->index` 依然沿用最初确定的这个值不变**——因为 `fork()` 后子进程的匿名 VMA 虽然是独立的 `vm_area_struct`，但通常与父 VMA 描述的是"同一段相对地址关系"（子进程的地址空间布局是从父进程复制而来，只是 `mm_struct`/页表不同，虚拟地址数值本身通常保持一致，或至少相对偏移一致），所以 `page->index` 这个"页在其虚拟地址区间内的相对位置"标识，对于所有共享该匿名页的 VMA 而言依然是一致、有意义的，可以配合 `anon_vma`/rmap 机制推算出该页在任意一个共享者 VMA 中对应的具体虚拟地址（第3章第3题所述"由 page 和 VMA 找到 vaddr"的转换关系，正是依赖 `page->index` 这个记录）。

### 37. 页面和KSM页面的区别是什么？

- **普通（未合并）匿名/文件页面**：`page->mapping` 指向 `anon_vma`（匿名）或 `address_space`（文件），`_mapcount` 反映的是"实际有多少 PTE 直接映射它"，没有经过 KSM 的内容比较/合并流程，是"天然"产生（分配/COW/文件读入）的页面。
- **KSM 页面**：是被 KSM 扫描发现"内容与另一个/另一批页完全相同"后，经过合并流程产生的**特殊只读共享匿名页**——`PageKsm(page)` 为真，其反向映射不再是简单的"一个 `anon_vma`"，而是通过 `stable_node`（第32~34题）关联着**一组内容相同、原本分别属于不同虚拟地址/不同 `anon_vma` 的 `rmap_item`**；该页始终保持**只读**，任何写入企图都会触发 COW（第31题）先分裂出一份独立副本，写入后该副本便"退出" KSM 管理、变回普通匿名页。

一句话：**KSM 页面是"内容驱动、事后被内核主动发现并人为建立共享关系"的一类特殊匿名页，普通页面的共享关系（若有）则是"天然"由 `fork()`/`mmap(MAP_SHARED)` 等操作预先决定的**，两者在使用层面（读共享、写 COW）表现一致，区别主要体现在反向映射数据结构与产生原因上。

### 38. 页面分配器如何管理空闲页面和分配请求之间的关系？

页面分配器（`mm/page_alloc.c`）本质上是在**"当前各 zone 各 order 的空闲页块存量（伙伴系统空闲链表 + per-CPU 缓存 pcplist）"**与**"源源不断到来的、大小（order）、属性（GFP 掩码/迁移类型/zone 上限）各异的分配请求"**之间做撮合与调度：

- **空闲侧**：伙伴系统按 zone、按 order、按迁移类型（第45题）分门别类地组织空闲页块链表，另外每个 CPU 还维护一份 order-0（单页）的**per-CPU 缓存（pcplist）**，把最常见的小额高频分配/释放请求拦截在 per-CPU 层面处理，减少直接碰全局伙伴系统链表（及其锁）的频率；
- **请求侧**：每个分配请求携带 `order`（要几个连续页）、`gfp_mask`（决定最高允许到哪个 zone、是否允许睡眠回收、是否走紧急预留等）、隐含的迁移类型（决定优先从哪个迁移类型的空闲链表找）；
- **撮合逻辑**：优先走**快速路径**（第39题）直接在合适的 zone/pcplist 找现成空闲块；找不到则触发**唤醒 kswapd（第10题）**、必要时**内存规整（第28、29题）**、**直接回收**、甚至**牺牲预留内存（第43题）/OOM**等一系列"创造更多可用空闲页/块"的手段，反复重试，直到满足请求或最终失败（返回 `NULL`/触发 OOM）。

可以把它类比为一个"资源池管理器"：空闲页是资源池存量，分配请求是取用需求，分配器通过分层缓存（pcplist）、分类组织（zone/order/migratetype）、以及在存量不足时主动触发多种"补货"机制（回收、规整、唤醒后台线程），尽量在**性能（大多数请求走最快路径）**与**成功率（存量紧张时仍有办法满足关键请求）**之间取得平衡。

### 39. 什么是页面分配器的快速路径？什么是慢速路径？

- **快速路径（fast path，`get_page_from_freelist()`）**：假设当前内存较为充裕的"乐观路径"——直接按 `zonelist` 顺序遍历候选 zone，对每个 zone 做水位检查（`zone_watermark_fast()`，一般以 `high` 或 `low` 水位为准，取决于是否已经唤醒过 `kswapd`），一旦找到某个 zone 满足水位且有所需 order/迁移类型的空闲块（优先查 per-CPU pcplist，其次伙伴系统链表），立即分配成功返回——全程**不涉及内存回收、不触发睡眠等待**，开销小、延迟低，是绝大多数（内存充裕时）分配请求的实际路径；
- **慢速路径（slow path，`__alloc_pages_slowpath()`）**：当快速路径未能找到满足条件的 zone/空闲块时才会进入，会依次尝试一系列"更费力"的补救手段——**唤醒 `kswapd`**、放宽/调低水位检查标准（`ALLOC_HARDER` 等，若调用者身份允许）、**内存规整**、**直接回收**（自己参与扫描 LRU 释放内存）、必要时**多次重试**上述过程（配合优先级/水位逐步放宽），最终仍不行则依据 `gfp_mask` 决定是**返回失败（`NULL`）**还是触发 **OOM Killer**。慢速路径涉及可能的睡眠等待（回收、规整期间），开销和延迟远高于快速路径，只在内存紧张时才会被触发。

### 40. 当一个普通的进程恶意占用内存时，页面分配器是如何处理的？

页面分配器本身**不直接"惩罚"某个进程**，它的职责只是按照水位、回收、规整等标准机制尽力满足或拒绝分配请求；真正针对"内存被过度占用、系统整体承压"的应对，主要体现在慢速路径触发的一系列连锁反应：

1. **持续唤醒 `kswapd` 后台回收**（试图腾出内存供其它进程使用）；
2. 该恶意进程自身继续申请内存时，也会被迫走**直接回收（direct reclaim）**——即它自己发起的分配请求会因为内存紧张而被拖慢（在自己的系统调用路径中被迫参与扫描回收，相当于"谁申请内存申请得凶，谁自己就要承担更多用于回收的 CPU 开销"，一定程度上自我抑制）；
3. 若持续到水位即使经过回收、规整也无法恢复、系统整体陷入内存耗尽的僵局，最终触发 **OOM Killer**：内核根据每个进程的 `oom_score`（主要由其占用的内存量、`oom_score_adj` 等因素决定，占用内存越多通常 `oom_score` 越高）选择"最应该被牺牲"的进程直接 `SIGKILL` 掉，强制释放其占用的全部内存——这正是应对"某进程恶意/异常地持续占用大量内存导致系统整体不可用"这一场景的最终手段。

也就是说，页面分配器体系提供的是"回收（迫使占用者自身分担开销）→ 规整 → 拒绝新请求（超过 `overcommit` 限制则 `mmap`/`malloc` 直接失败）→ OOM 杀死重罪进程"这样一套层层递进、最终有底线的应对机制。

### 41. 若一个普通进程处于内存承压的情况下，页面分配器尝试哪些努力来 保证分配成功？

（即慢速路径 `__alloc_pages_slowpath()` 依次尝试的手段，按大致顺序）：

1. **唤醒 `kswapd`**，寄望其后台回收能在本次分配重试时已经腾出足够空间；
2. **放宽本次检查用的水位/分配标志**（如某些场景允许降级使用 `ALLOC_HARDER`），让原本卡在 `low`/`high` 水位线的请求有机会以更低的门槛（如降到接近 `min`）通过；
3. **尝试内存规整（compaction）**：若判断问题主要是外碎片化（总空闲够、缺大块连续），先规整腾出连续大块再重试分配；
4. **发起直接回收（direct reclaim）**：调用者自己参与扫描、回收 LRU 页面，回收出一定数量的空闲页后重新尝试分配；
5. **反复重试上述 2~4 步骤若干轮**（每轮可能进一步调整策略/放宽条件），期间也会检查是否有信号/致命条件需要提前退出；
6. 若调用者具备特殊身份（如 `PF_MEMALLOC`、`TIF_MEMDIE` 等标志，常见于正在响应内存压力本身的路径、或已经被 OOM 选中即将退出的进程），可能被允许使用**紧急预留内存**（`ALLOC_HARDER`/`ALLOC_HIGH`/`ALLOC_OOM`，第43题）；
7. 若以上均告失败，根据 `gfp_mask` 是否允许（如是否 `__GFP_NOFAIL`/是否可以直接返回失败）决定**放弃并返回 `NULL`**，还是触发 **OOM Killer** 后再重试一次。

对于"普通进程"（不具备上述特殊豁免身份）而言，主要能依靠的就是 1~5 这几步"公平参与回收/规整"的常规努力，通常不会被给予访问紧急预留内存的特权。

### 42. 在什么情况下页面分配器可以访问系统预留内存？

系统会为每个 zone 预留一部分"平时不动用、专供极端紧急场景使用"的内存（体现为水位线 `min` 以下、以及第43题所述通过降低有效水位间接"借用"到的部分），只有满足以下条件之一时，分配请求才被允许突破正常水位限制去动用这部分预留：

- **`__GFP_ATOMIC`/`__GFP_HIGH`（对应 `ALLOC_HIGH`）**：调用者明确声明"这是原子/高优先级分配，不能失败也不能等待回收"（如网络协议栈在中断/软中断上下文中的关键分配路径），允许适度突破水位（`min -= min/2`）；
- **`ALLOC_HARDER`**（常与 `__GFP_ATOMIC` 一起、或某些实时性要求高的场景）：进一步放宽水位要求（`min -= min/4`）；
- **进程正处于 OOM 流程中、即将退出（`ALLOC_OOM`/被标记为 OOM 受害者）**：允许它自己在退出前所需的少量内存分配更容易成功（更快地释放它占用的其它内存，尽快让系统脱离危机），此时 `min -= min/2`（与 `ALLOC_HIGH` 同等幅度，但语义不同、且常与 `ALLOC_HARDER` 一起生效）；
- **`ALLOC_NO_WATERMARKS`**：完全不检查水位（用于极特殊的、必须成功的核心路径，如某些内存回收自身运作所需的关键分配、`PF_MEMALLOC` 标记的进程），本质上就是被允许"想用多少预留就用多少"；
- 具备 **`CAP_SYS_ADMIN`** 等特权、或被 cpuset/memcg 配置显式豁免的场景，也可能被授予接近 `ALLOC_NO_WATERMARKS` 的待遇。

普通、不带任何特殊标志的一般分配请求（如典型的 `GFP_KERNEL`/`GFP_HIGHUSER_MOVABLE`）**始终不能**触碰这部分预留，必须老老实实排队等待回收/规整腾出正常空闲内存。

### 43. 在页面分配器中，分配掩码ALLOC_HIGH、ALLOC_HARDER、 ALLOC_OOM以及ALLOC_NO_WATERMARKS之间有什么区别？它们各自能访 问系统预留内存的百分比是多少？思考为什么页面分配器需要做这样的安排。

实测本设备内核源码 `mm/page_alloc.c` 的 `__zone_watermark_ok()` 给出精确的判定逻辑（`min` 是本来应满足的水位线数值）：

```c
if (alloc_flags & ALLOC_HIGH)
    min -= min / 2;                 // ALLOC_HIGH：水位門檻降為原本的 50%

if (unlikely(alloc_harder)) {       // alloc_harder = ALLOC_HARDER | ALLOC_OOM
    if (alloc_flags & ALLOC_OOM)
        min -= min / 2;             // ALLOC_OOM：在 alloc_harder 分支內再打對折
    else
        min -= min / 4;             // ALLOC_HARDER（非OOM）：只降低 25%
}
```

- **`ALLOC_HIGH`**（对应 `__GFP_HIGH`）：门槛降为原来的 **50%**（`min -= min/2`），即最多可以额外动用相当于"正常门槛一半"的预留部分；
- **`ALLOC_HARDER`**（不含 OOM，如部分 `__GFP_ATOMIC` 场景）：在（可能已经被 `ALLOC_HIGH` 降过一次的）门槛基础上再降低 **25%**（`min -= min/4`）；由于 `__GFP_ATOMIC` 实际上会**同时**设置 `ALLOC_HARDER` 与 `ALLOC_HIGH` 两个标志（源码注释明确说明），两次削减是**复合叠加**的：先变为 `min/2`，再在此基础上减去 `(min/2)/4`，最终门槛降为原始 `min` 的 **3/8（37.5%）**，即可以动用高达 **62.5%** 的原本"预留"部分；
- **`ALLOC_OOM`**：属于 `alloc_harder` 判断范围内的更激进分支，直接再打对折（`min -= min/2`），若与 `ALLOC_HIGH` 叠加（`min/2` 基础上再 `-min/2`）门槛可降至原始的 **1/4（25%）**，即可动用高达 **75%** 的预留——比普通 `ALLOC_HARDER` 更激进，因为 OOM 受害进程"即将退出、这次分配是为了让它能尽快释放更多内存、缓解危机"，值得给予最大让步；
- **`ALLOC_NO_WATERMARKS`**：**完全不检查水位**（直接跳过整个 `__zone_watermark_ok` 判断，见 `mm/page_alloc.c` 中 `if (alloc_flags & ALLOC_NO_WATERMARKS) return true;` 一类的短路逻辑），相当于可以动用 **100%** 的预留（乃至理论上榨干 zone 里最后一页），是最高等级的豁免。

**为什么要这样分级安排**：内存耗尽是一个"越接近临界点、越需要区别对待不同紧迫程度的请求"的场景——完全不加区分地对所有请求一视同仁（要么都严格守住水位、要么都放开），要么会让本可以通过牺牲少量预留就能解决的紧急/关键分配（如网络中断处理、内存回收自身运作所需的分配）在最需要成功的时候反而失败、引发连锁故障；要么会让预留内存被普通请求轻易耗尽，起不到"留给关键场景兜底"的作用。分级、按比例递进地放宽水位，本质上是一种**优先级调度**：让"越关键、越不能失败、越有助于让系统尽快脱离危机（如 OOM 受害者退出释放内存）"的请求，被允许拿到更大比例的"最后储备"，而普通请求则被要求老实排队、通过回收/规整等正常手段自行争取内存，从而在系统濒临崩溃的边缘尽量保证最重要的操作还能继续运作，避免"雪崩式"的全面失败。

### 44. 伙伴系统算法是如何减少内存碎片的？

- **合并（coalescing）机制**（第7题）：释放页面时主动检测并合并相邻伙伴块，持续把小的空闲碎片聚合回大块，是对抗碎片化最基本的"自愈"能力；
- **按 order 分级管理**：不同大小的空闲请求各自在对应 order 的链表中查找，避免了"不按大小分类、随意查找"可能导致的大块被不断切碎、小块却难以拼凑回大块的乱象；
- **分裂时优先切割大块的高位部分**：保证每次分裂产生的伙伴仍然是"规整对齐"的地址（`buddy_pfn = pfn XOR (1<<order)`），使得日后释放时依然能高效地按位运算找回伙伴、大概率能重新合并回去，而不是产生难以再拼接利用的"歪斜"地址范围；
- **迁移类型分区**（第45、46题）：把可迁移、可回收、不可迁移的页分开存放在不同的 `pageblock` 中，避免"生命周期/可挪动性差异很大"的页面混杂在一起，防止长期存在的不可迁移小对象"钉"在一大片区域中间，阻碍相邻大量可迁移页合并成大块；
- 结合**内存规整（compaction，第28~30题）**：当纯粹依赖"合并"仍然不足以应对外碎片化时（存在无法直接合并、但可以迁移挪动的占用页），主动迁移腾挪，进一步提升大块连续内存的可获得性，是对伙伴系统"被动合并"能力的主动补强。

### 45. 为什么要把内存分成不同的迁移类型？这些迁移类型有什么区别？

**原因**：不同用途的内存分配，其"生命周期长短"与"是否可以被安全挪动（迁移/回收）"差异巨大——如果把"可能长期占用、无法迁移"的内核对象（如页表页、slab 对象）与"随时可能被回收/迁移走"的用户态页面混杂分配在同一片物理区域中，当需要通过合并或规整腾出大块连续内存时，会因为这片区域中夹杂着几个"钉死不能动"的页而始终无法把周围一大片本可迁移的空闲/占用页凑成连续大块——即所谓"一颗老鼠屎坏了一锅粥"。为了避免这种情况，Linux 引入**迁移类型（migratetype）**，尽量让"同类生命周期/迁移特性"的页面聚集分配在同一批 `pageblock`（本设备实测 `Page block order: 9`，即每 512 个连续页为一个 `pageblock` 单位）中，隔离不同类型，最大化规整/合并的效果。

各迁移类型区别（对应 `enum migratetype`，`include/linux/mmzone.h`）：

- **`MIGRATE_UNMOVABLE`（不可移动）**：如页表页、大部分内核 `slab` 对象、`kmalloc` 大块内存——一旦分配位置固定，就不能被迁移/挪动，是内存规整时唯一真正"绕不开、无法处理"的类型；
- **`MIGRATE_MOVABLE`（可移动）**：用户态匿名页、大部分文件页面高速缓存——可以被页面迁移机制自由搬动，是内存规整的主要操作对象，占实测本设备 `pagetypeinfo` 中数量最多的 pageblock 类型（`DMA: 1589`、`Normal: 1933`）；
- **`MIGRATE_RECLAIMABLE`（可回收）**：内容可以被直接丢弃、之后按需重新生成/重新读取的内核对象（如部分可回收的 `slab` cache、某些文件系统元数据缓存），不需要真正"迁移"，回收压力大时可以直接释放腾出空间；
- **`MIGRATE_HIGHATOMIC`**：为满足**原子（不可睡眠）、高优先级但又不适合走紧急预留水位机制**的高阶分配请求而预留的一小部分页块，避免这类请求因为找不到合适大小的连续块而失败；
- **`MIGRATE_CMA`**：CMA（Contiguous Memory Allocator）专属类型（第24题），平时可被普通可迁移分配借用，但需要时会强制腾空、专供大块连续 DMA 使用，本设备实测 `DMA` zone 有 128 个此类型 pageblock（对应 256MB CMA 保留）；
- **`MIGRATE_ISOLATE`**：临时"隔离"状态，用于内存热插拔（下线某段物理内存前）、`CMA`/规整过程中临时标记"这段区域暂时禁止被任何新分配触及，正在被特殊处理"的过渡态，不是一种"正常使用"的分类，而是一种操作过程中的临时标记。

### 46. 假设请求分配order为4的一个内存块，迁移类型是不可迁移 （MIGRATE_UNMOVABLE），但是order大于或等于4的不可迁移类型的空闲链 表中没有空闲页块，那么页面分配器会怎么办？

会执行**回退分配（fallback allocation）**机制（`__rmqueue_fallback()`）：

1. 先按照预先定义好的**回退优先顺序表**（`fallbacks[MIGRATE_TYPES][...]`，大致顺序如 `UNMOVABLE → RECLAIMABLE → MOVABLE`，各类型都定义了各自愿意"借用"哪些其它类型），在 `order>=4` 的范围内，尝试从**允许被借用的其它迁移类型**（比如先尝试从 `MIGRATE_RECLAIMABLE`，找不到再尝试从 `MIGRATE_MOVABLE`）的空闲链表中找一个足够大的块；
2. 找到后，**优先选择尽可能大的 order**（从该其它类型里能找到的最大可用块开始），把它整体先按第1题的"分裂"流程切出所需的 `order=4` 大小返回给本次请求；剩余部分中，**通常会把这个借来的 pageblock 的迁移类型属性整体改标记为 `MIGRATE_UNMOVABLE`**（即"污染/转换"这个 `pageblock` 归属，除非该次借用被认为是"临时应急、不应永久改变归属"的特殊情况，视具体内核版本的启发式策略而定），这样做的目的是保持"同一个 `pageblock` 内的页尽量归属同一迁移类型"的整体不变量，减少未来该区域内碎片类型混杂；
3. 如果连所有允许借用的迁移类型也都找不到 `order>=4` 的空闲块，则说明当前该 zone 已经碎片化到没有满足条件的大块可用，会进一步触发**内存规整（第28、29题）**尝试腾出符合要求的大块，规整后再重试上述过程；
4. 若规整之后依然无法满足，则和其它分配失败情形一样，进入更广义的慢速路径处理（唤醒 kswapd/直接回收/重试/最终失败或 OOM，见第39~43题）。

### 47. 什么是内存外碎片化？Linux内核的页面分配器是如何发现外碎片的？

**外部碎片化（External Fragmentation）**：系统**总的空闲内存数量其实是足够的**，但这些空闲页分散在物理地址空间的各个角落、彼此不连续，导致**无法满足需要"一大块连续物理内存"的高阶（`order > 0`）分配请求**——区别于"内部碎片"（第1章第31题，指单次分配内部因对齐/取整浪费的空间），外碎片是"总量够、连续性不够"的问题。

Linux 通过 **`extfrag_index`（外碎片化指数，`mm/vmstat.c`/`mm/compaction.c` 中 `fragmentation_index()`）**这一指标来量化判断：其计算依据当前 zone 里各 order 的空闲块分布，估算"如果理想地把所有空闲页合并、这个 zone 本可以支持多大的最大连续分配"，与"实际观测到的、按当前碎片化程度能提供的最大连续分配能力"两者的差距——指数越接近 **1**，说明碎片化程度越严重（内存总量看似够，但几乎凑不出请求所需的连续大小，规整会很有价值）；越接近 **0**（甚至为负），则说明即使做规整也未必能显著改善（可能是总空闲量本身就不够，规整解决不了根本问题）。分配器/`kcompactd` 正是依据这一指数、结合高阶水位检查结果，判断"当前是否值得触发一次规整尝试"。

### 48. 当发现了内存外碎片，Linux 5.0内核是如何处理的？

发现外碎片化程度较高（`fragmentation_index` 接近 1、且高阶水位检查未通过）时，Linux（含本设备实测运行的 6.1 内核，机制与 5.0 一脉相承）主要通过**内存规整（compaction，第28~30题）**来处理：

1. 若是在页面分配的慢速路径中**同步**发现（`order>0` 请求失败，但总空闲量检查显示"理论上凑得出"），会**直接同步调用 `try_to_compact_pages()`**，现场进行一轮规整（迁移扫描指针+空闲扫描指针相向而行，把可迁移的占用页搬到一端、腾出连续空闲区），完成或达到预算上限后立即重试原本失败的分配；
2. 若是**后台 `kcompactd`** 定期/被水位检测唤醒发现的（不一定对应某个正在等待的具体分配请求），则异步地提前做规整，把碎片提前整理好，减少将来真正有高阶分配请求到来时需要同步等待规整的概率与延迟；
3. 规整过程本身会**优先处理 `MIGRATE_MOVABLE`/`MIGRATE_CMA` 这类可迁移页**（第30题），跳过 `MIGRATE_UNMOVABLE`（无法迁移，第25、44、46题）与被钉住/正在 I/O 的页面，因此**规整并不能保证 100% 消除外碎片**——如果碎片主要是由大量分散的 `MIGRATE_UNMOVABLE` 小块页造成（第45、46题所述"借用/污染"迁移类型的累积效应），规整的改善效果会打折扣，这也是为什么第45题所述的"尽量把不同迁移类型隔离存放、减少 UNMOVABLE 页扩散污染 MOVABLE 区域"在源头上如此重要——规整是**事后补救**手段，迁移类型隔离与合理的分配策略才是**从源头减少外碎片产生**的根本方法，两者需要配合，而非只依赖规整单独解决问题。
