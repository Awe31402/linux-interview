# 《奔跑吧 Linux內核》（第二版）卷1 第5章 內存管理之高級主題 高頻面試題

> **說明**：收錄自第 5 章開篇「本章的高頻面試題」，共計 48 題。
> 答案結合書本理論、Linux 內核原始碼（本專案樹，版本 6.1.115）、
> 以及 Radxa ROCK 5B（RK3588）實機（`radxa@192.168.68.57`）的實測資料。

---

### 1. page 資料結構中的 _refcount 和 _mapcount 有什麼區別？

- **`_refcount`（`atomic_t`）**：該 `struct page` 的**總引用計數**，代表"內核中有多少處代碼/資料結構正持有對這個物理頁的引用，認為它當前『正在被使用、不能被釋放/回收』"，涵蓋範圍很廣：被頁表映射、被 page cache 索引、被 `get_user_pages()` 釘住、被內核某處代碼臨時 `get_page()` 持有等都會增加它。**只要 `_refcount` 歸零，該頁才真正可以被釋放回夥伴系統**，因此它是"這個物理頁是否可以被回收/釋放"的最終判據。
- **`_mapcount`（`atomic_t`）**：專門統計**該頁當前被多少個用戶態頁表項（PTE）實際映射著**（即出現在多少個進程的地址空間中），不涉及"page cache 是否引用它""內核是否臨時借用它"這類非頁表映射的引用。`page_mapcount(page) = atomic_read(&page->_mapcount) + 1`（內部從 -1 起算，未被任何 PTE 映射時為 -1，讀出的 `mapcount` 是 0）。

關係：**`_mapcount` 是 `_refcount` 的一個子集來源**——每多一個 PTE 映射該頁，`_mapcount` 加一的同時，也會讓 `_refcount` 相應加一（因為"被頁表映射"本身也算一種引用）；但 `_refcount` 還包含很多 `_mapcount` 不統計的引用（如剛從夥伴系統分配出來、還未建立任何映射的頁 `_refcount=1` 但 `_mapcount=-1`(0)；被 page cache 索引但尚無進程 `mmap` 它）。因此 `_refcount >= _mapcount 相關的映射引用之和`，兩者服務於不同層面的判斷：`_mapcount` 回答"有多少地址空間在用它（決定是否需要 COW，見第4章第42題）"，`_refcount` 回答"這個物理頁整體是否還有任何存在的理由（決定能否釋放）"。

### 2. 匿名頁面和高速緩存頁面有什麼區別？

- **匿名頁面（Anonymous Page）**：**不對應任何檔案**，其內容"憑空產生"（如進程堆/棧、`malloc`/`mmap(MAP_ANONYMOUS)` 分配的內存），生命週期完全由進程地址空間決定。`page->mapping` 指向一個 `anon_vma`（並在低位打上 `PAGE_MAPPING_ANON` 標記以區分，見第6題），沒有底層檔案/存儲對應；一旦被換出，其資料只能存到 **swap 分區/檔案**才能保留，進程退出（且沒有共享者）後資料即徹底丟棄。
- **頁面高速快取（Page Cache）**：**對應某個檔案（或塊設備）的某段內容**，作為磁碟資料在內存中的快取副本，`page->mapping` 指向該檔案的 `address_space`，`page->index` 表示是檔案內第幾頁。回收該頁時，若內容乾淨（未被修改）可以直接丟棄（下次存取重新從檔案讀取即可，無需 swap）；若是髒頁（被寫過、`MAP_SHARED` 檔案映射寫入或普通 `write()` 系統調用寫入）則需要先**回寫（writeback）**到原檔案所在的存儲設備，而不是寫入 swap。

兩者共用同一套 `struct page`/LRU 框架管理（見第3~5、38~48題的頁面分配器與回收討論），但"資料的最終歸宿"不同——匿名頁靠 swap，檔案頁靠原檔案——這個區別直接決定了 `kswapd`/直接回收路徑處理它們時的不同策略（詳見第16~19題）。

### 3. page 資料結構中有一個鎖，我們稱為頁鎖，請問 trylock_page() 和 lock_page() 有什麼區別？

`PG_locked` 是 `page->flags` 中的一個標誌位，作為該頁的一把**互斥鎖（page lock）**，用於串行化對該頁內容/狀態的並發操作（如同時避免多個路徑同時對同一頁做 I/O、遷移、回收等）。

- **`lock_page()`**：**阻塞式**獲取頁鎖——若該頁當前已被其它路徑鎖住，調用者會**睡眠等待**（放入等待隊列，被喚醒後重試），直到成功拿到鎖才返回。適用於允許睡眠的上下文，且調用者確實需要獨佔存取該頁才能繼續（如必須要等這一頁的 I/O 完成）。
- **`trylock_page()`**：**非阻塞式**嘗試獲取頁鎖——若鎖已被占用，**立即返回失敗**（不睡眠、不等待），由調用者自行決定後續處理（如跳過這一頁、稍後重試、或退回到其它策略）。適用於不允許睡眠的上下文（如某些內存回收的快速路徑、中斷相關代碼），或者調用者只是"能拿到就處理，拿不到就算了"的場景（如 `kswapd`/直接回收掃描 LRU 時，若某頁正被別的路徑鎖住，通常選擇跳過它而非死等，以避免大範圍阻塞）。

### 4. 請畫出 page 資料結構中 flags 成員的佈局示意圖。

`page->flags` 是水一個 `unsigned long`，按位劃分為**狀態標誌位區**與**元資料編碼區**（section/node/zone/kasan-tag 等，具體位寬因 `CONFIG_SPARSEMEM`/`CONFIG_NUMA`/位寬而異），從最高位到最低位大致為：

```
高位                                                                        低位
+-----------+-----------+-----------+---------------------------------------+
| SECTION   |   NODE    |   ZONE    |         狀態旗標位 (PG_xxx)             |
| (可能與LAST_CPUPID共用) | (若NUMA)  | (區分該page屬於哪個zone)  | PG_locked, PG_error,     |
|           |           |           | PG_referenced, PG_uptodate,           |
|           |           |           | PG_dirty, PG_lru, PG_active,          |
|           |           |           | PG_slab, PG_writeback,                |
|           |           |           | PG_reserved, PG_swapcache, ...        |
+-----------+-----------+-----------+---------------------------------------+
```

具體每個欄位佔多少位由編譯期常量決定（`SECTIONS_WIDTH`、`NODES_WIDTH`、`ZONES_WIDTH` 等，取決於開啟了 `SPARSEMEM`/`SPARSEMEM_EXTREME`/`NUMA` 與否，以及物理地址位寬），最低若干位固定留給一組 `enum pageflags`（`include/linux/page-flags.h`）中定義的位標誌，通過 `PageLocked(page)`、`SetPageDirty(page)`、`ClearPageActive(page)` 等巨集存取；本設備因 `# CONFIG_NUMA is not set`（單節點 UMA），`NODE` 欄位實際寬度為 0（無需編碼 node id），`SECTION`（`CONFIG_SPARSEMEM`）欄位用於配合第3章第6題所述的、DTB 給出的 3 段不連續物理內存做 `pfn_to_page()`/`page_to_pfn()` 的換算。

### 5. 請列舉 page 資料結構中 _refcount 和 _mapcount 計數的使用案例。

**`_refcount` 使用案例**：
- 從夥伴系統 `alloc_pages()` 分配出一個新頁時初始化為 1；
- `get_page()`/`put_page()`：內核任意代碼路徑臨時需要"保證這個頁在我用完之前不會被釋放"時的標準做法（如 `get_user_pages()` 釘住用戶頁供 DMA 使用）；
- page cache 索引該頁時持有一份引用（被加入 `address_space` 的 XArray 時 `_refcount` +1）；
- `_refcount` 歸零觸發 `free_pages()` 真正釋放回夥伴系統。

**`_mapcount` 使用案例**：
- 每次 `page_add_anon_rmap()`/`page_add_file_rmap()`（在缺頁處理、`mmap`、`fork` 建立子進程頁表映射時調用）讓某個 PTE 指向該頁時 `_mapcount` +1；對應的 `page_remove_rmap()`（`munmap`、進程退出回收頁表、頁被換出前拆除映射時）則 -1；
- 第4章第42題的寫時複製判斷：`page_mapcount(page) == 1` 才允許"複用"而非複製；
- 第35題所述 KSM 的 `write_protect_page()` 中 `page_mapcount(page) + 1 + swapped != page_count(page)` 這類一致性檢查；
- 內存回收掃描 LRU 時，`page_mapcount(page) > 0` 用於判斷該頁當前是否仍被某進程實際映射（是則需要先做反向映射 unmap，才能真正回收，見第5章 rmap 相關題）。

### 6. 請簡述 page 資料結構中 mapping 成員的作用。

`page->mapping` 是水一個被"打了標記複用"的指標，依據其**最低兩位**決定含義：

- 最低位為 **0** 且指標非空：`page->mapping` 指向一個 **`struct address_space *`**，表示這是一個**檔案映射頁/page cache 頁**，該 `address_space` 通常掛在某個 `inode` 下，代表該頁屬於哪個檔案、結合 `page->index` 確定是檔案內哪一頁；
- 最低位為 **1**（即 `PAGE_MAPPING_ANON` 標記）：實際存的是一個 **`struct anon_vma *`** 指標（需要先清除最低位才能得到真實地址），表示這是一個**匿名頁面**，通過它可以找到反向映射所需的 `anon_vma`（進而找到所有映射該頁的 VMA，見第7、8題）；
- `page->mapping == NULL`：表示該頁當前未被任何檔案/匿名地址空間"擁有"（如剛從夥伴系統分配、尚未建立任何映射的頁，或是某些內核內部專用頁）。

這種"用指標最低若干位（因為指標本身按字對齊，低位天然是 0）編碼額外類型資訊"的技巧在內核中很常見，`page_mapping()`/`PageAnon()` 等輔助函數負責正確解析這個欄位，使內存管理的通用代碼路徑（如缺頁、回收、遷移）可以用統一的 `page->mapping` 欄位、通過類型判斷分別導向"匿名頁反向映射路徑"或"檔案頁地址空間路徑"。

### 7. 在 Linux 2.4.x 內核中，如何從一個頁面中找到所有映射該頁面的 VMA？RMAP 可以帶來哪些便利？

**Linux 2.4.x（舊版本，無 RMAP）**：內核**沒有為匿名頁維護任何"反向"索引**——要找出"哪些 VMA/進程映射了這個物理頁"，唯一的辦法是**從進程側正向遍歷**：拿到系統中所有進程的 `mm_struct`，逐個遍歷其所有 VMA，再逐個遍歷 VMA 覆蓋的每一個虛擬地址、查頁表，檢查該 PTE 是否恰好指向目標物理頁——即**O(所有進程 × 所有 VMA × 所有頁表項)** 的全局暴力掃描，在需要"回收/換出某個具體物理頁之前，必須先拆除所有映射它的 PTE"這類場景（`try_to_unmap`）下開銷極其高昂，是當時內存回收性能的一大瓶頸（尤其系統中進程數、內存量增大後幾乎不可行）。

**RMAP（Reverse Mapping，反向映射）帶來的便利**：為每個匿名頁/檔案頁額外維護一份**從"頁"直接指向"哪些 VMA 映射了它"的反向索引**（匿名頁通過 `anon_vma`/`anon_vma_chain`，檔案頁通過 `address_space` 的區間樹/`i_mmap`），使得"給定一個物理頁，找出所有映射它的 VMA"從 **O(全局進程/頁表規模)** 降為 **O(實際映射該頁的 VMA 數量)**，與系統總體規模無關，只與"這個頁實際被共享的程度"成正比。這讓內存回收（尤其是要精確回收特定幾個頁而非整體掃盡）、頁面遷移、`munmap` 局部清理等操作的性能變得可控，是現代 Linux 內存管理能夠在大內存、大量進程場景下依然高效工作的關鍵基礎設施之一。

### 8. 閱讀 Linux 5.0 內核 RMAP 機制的代碼，畫出父子進程之間 VMA、AVC、AV 以及 page 等資料結構之間的關係圖。

```
                    ┌─────────────┐                  ┌─────────────┐
                    │ 父進程 mm_struct │              │ 子進程 mm_struct │
                    └──────┬──────┘                  └──────┬──────┘
                           │ vma                             │ vma
                           v                                 v
                    ┌─────────────┐                  ┌─────────────┐
                    │  VMA (parent) │                │  VMA (child)  │  (fork 後各自獨立的 vm_area_struct，
                    └──────┬──────┘                  └──────┬──────┘   但邏輯上映射同一組匿名頁)
                           │ anon_vma_chain (AVC)            │ anon_vma_chain (AVC)
                           v                                 v
                    ┌─────────────┐                  ┌─────────────┐
                    │  AVC (parent) │ ──same_vma──── │  AVC (child)  │
                    └──────┬──────┘   鏈到各自vma的avc_lock    └──────┬──────┘
                           │ anon_vma                        │ anon_vma
                           v                                 v
                    ┌─────────────────────── AV (anon_vma) 樹 ───────────────────────┐
                    │  root AV (最初 fork 之前，父進程自己的 anon_vma)                    │
                    │     ├── child AV (fork 時為子進程新建，parent 指向 root)            │
                    │  透過 AV.rb_root（紅黑樹）串連所有子孫 AV，方便統一遍歷              │
                    └─────────────────────────────┬───────────────────────────────────┘
                                                    │ (AV->rb_root 中每個 avc 又反過來
                                                    │  掛在對應 page 的 anon_vma 之下)
                                                    v
                                            ┌───────────────┐
                                            │  page (匿名頁)  │  page->mapping 指向其中一個 AV(root)
                                            └───────────────┘
```

要點：`fork()` 時，子進程的每個匿名 VMA 會創建一個**新的 `anon_vma`（AV）**，但通過 `AV->parent`/`AV->root` 指標鏈接回父進程原來的 AV（形成一棵 AV 樹，`root` 始終指向最初那個"祖先" AV）；父、子各自的 VMA 都通過 `anon_vma_chain`（AVC，一個"多對多"關係的中間節點，既鏈在其所屬 VMA 的 `vma->anon_vma_chain` 鏈表上、也鏈在其所屬 AV 的 `avc->rb`/`same_anon_vma` 紅黑樹上）與自己的 AV 關聯；頁面的 `page->mapping` 只指向**根 AV**，但要找"映射該頁的所有 VMA"時，通過根 AV 的 `rb_root` 遍歷到所有子孫 AV，再經每個 AV 下掛的 AVC 找到各自的 VMA——這樣即使父子進程各有獨立的 AV，也能統一、完整地找到所有共享該匿名頁的 VMA。

### 9. 在 Linux 2.6.34 內核中，RMAP 機制採用了新的實現，Linux 2.6.33 內核和之前的版本中的 RMAP 機制稱為舊版本 RMAP 機制。那麼在舊版本 RMAP 機制中，如果父進程有 1000 個子進程，每個子進程都有一個 VMA，每個 VMA 裡面有 1000 個匿名頁面，當所有的子進程的 VMA 同時發生寫時複製時會是什麼情況呢？

**舊版本 RMAP（2.6.33 及之前）**：每個匿名頁的反向映射是通過 `page->mapping` 直接指向**唯一一個共享的 `anon_vma`**、並把所有映射該 `anon_vma` 的 VMA 直接掛在這**同一個 `anon_vma` 的鏈表**上實現的——即無論有多少個子進程 `fork()` 出來，只要它們的匿名 VMA 是從同一個父 VMA 複製而來，就都共享**同一把 `anon_vma`**（及其保護鎖），VMA 直接以鏈表形式全部掛在其下，沒有第8題所述"每個進程各自獨立 AV、再用 AVC/AV 樹間接關聯"的分層結構。

在題述場景（1000 個子進程，每個各有一個 VMA，且發生 COW）下：
- 所有 1000 個子進程的 VMA 全部掛在**同一個 `anon_vma`** 之下，任何一次需要遍歷"哪些 VMA 映射了某個匿名頁"的操作（如內存回收掃描、頁面遷移，或本題的 COW 處理路徑中檢查共享狀態），都必須**線性遍歷這條長達 1000 項的鏈表**，即使實際只關心其中一兩個 VMA；
- 更嚴重的是：這個共享的 `anon_vma` 只有**一把鎖**（`anon_vma->lock`），1000 個子進程各自的缺頁/COW 處理若都需要存取、修改與這個 `anon_vma` 相關的反向映射資訊，會在這一把全局鎖上產生**激烈的鎖競爭（lock contention）**——多核同時對不同子進程的不同頁面做 COW，本來邏輯上互不相干，卻會因為共享同一把 `anon_vma` 鎖而互相阻塞、串行化，導致本應可以並行的操作性能急劇下降（這就是官方 changelog 中提到的、促使 2.6.34 重新設計 RMAP 加入 `anon_vma_chain` 分層結構的直接動機——新方案讓每個子進程擁有自己獨立的 `anon_vma`，只在真正需要"遍歷所有共享者"這種低頻操作時才通過 AV 樹把它們關聯起來，日常的缺頁/COW 處理各自只需碰自己的 AV，互不阻塞）。

### 10. kswapd 內核執行緒何時會被喚醒？

`kswapd`（每個 NUMA 節點一個，本設備單節點即一個 `kswapd0`）在**頁面分配路徑檢測到某個 zone 的空閒頁低於該 zone 的 `low` 水位線**時被喚醒（`wakeup_kswapd()`，通常在 `__alloc_pages_slowpath()` 分配請求發現快速路徑不滿足水位而進入慢速路徑的早期階段調用）。被喚醒後 `kswapd` 轉入**異步的後台回收（background reclaim）**：從當前分配請求的 zone 開始，嘗試把該 zone 的空閒頁量從 `low`（或更低）回收提升到 `high` 水位線，回收足夠後重新進入睡眠；如果回收速度跟不上分配速度（`low`/`min` 水位持續被擊穿），發起分配的進程自身則會轉入**同步的直接回收（direct reclaim）**路徑，親自參與掃描回收（見第39題快速/慢速路徑的討論）。

實測本設備 `/proc/vmstat` 中 `kswapd_low_wmark_hit_quickly = 76`、`kswapd_high_wmark_hit_quickly = 6` 說明系統運行以來 `kswapd` 已被喚醒並成功／快速地把 zone 從低水位拉回高水位的次數；同時 `pgsteal_kswapd = 335187` 遠大於 `pgsteal_direct = 23254`，說明絕大多數內存回收工作是由 `kswapd` 在後台異步完成的，只有少部分（約 6.5%）落到了更影響應用延遲的直接回收路徑，反映系統整體內存壓力可控。

### 11. LRU 鏈表如何知道頁面的活動頻繁程度？

Linux 用**雙 LRU 鏈表（Active/Inactive）**近似實現"存取頻繁程度"的排序，而非精確統計每個頁面的存取次數（那樣開銷太大），核心訊號是頁面的 **`PG_referenced` 標誌位**與**頁表項的 Accessed 位（硬體維護，ARM64 對應 `PTE_AF`）**：

- 每當一個頁被存取（讀/寫命中，硬體自動置位對應 PTE 的 Access Flag），或某些路徑顯式調用 `mark_page_accessed()`，就會設置該頁的 `PG_referenced` 位（如果已經設置過，再次被存取會被視為"較活躍"的訊號，推動它從 Inactive 鏈表提升到 Active 鏈表）；
- 當 `kswapd`/直接回收掃描到某頁時，會檢查它的 `PG_referenced` 以及對應 PTE 的 Accessed 位（並順帶清除，為下一輪統計重新計時），據此判斷：**最近被存取過 → 判定為活躍，移到/留在 Active 鏈表（優先保留）；長期未被存取 → 留在/移入 Inactive 鏈表，成為優先被回收的候選**；
- 頁從 Active 移到 Inactive 需要"經過一次掃描發現未被再次存取"，是一種**近似 LRU（Least Recently Used）而非嚴格計數**的機制——本質上是"最近是否被用過一次"的粗粒度冷熱判斷，而非記錄精確的存取時間戳/頻次，兼顧了實現開銷與回收效果。

### 12. kswapd 按照什麼原則來換出頁面？

總體原則是"**優先換出最不可能很快被再次用到的頁面**"，具體體現為：

1. **優先從 Inactive 鏈表（而非 Active）挑選**：Inactive 鏈表本身就代表"較冷、近期未被存取"的候選集合；
2. **按 `swappiness` 權衡匿名頁 LRU 與檔案頁 LRU 各自貢獻多少掃描比例**（見第16題）——決定這一輪回收在匿名頁和檔案頁兩條 LRU 上各掃描多大比重；
3. 同一條 LRU 鏈表內部，從**鏈表尾部（最久未被移動到頭部/最久未被標記 referenced 的一端）**開始掃描，越靠尾部代表越久沒有活動跡象；
4. 對遇到的每一頁做進一步檢查（是否 `mlock` 不可回收、是否正被釘住 `_refcount`/`_mapcount` 異常、是否是髒頁需要先回寫等，見第18、19題），符合條件才真正回收，否則跳過或移至鏈表另一端。

本質上是"先分類（匿名/檔案、Active/Inactive），再按 LRU 順序、結合 swappiness 權重、結合頁面自身狀態（髒/乾淨、鎖定與否）"這一套組合策略，而非單一維度的簡單排序。

### 13. kswapd 按照什麼方向來掃描 zone？

與第1章第3題頁面分配器的掃描方向相反：`kswapd` 是**從"最低（最需要保護/最珍貴，如 DMA）"的 zone 開始，還是從與之前分配請求相關的目標 zone 開始向下**？準確地說，`kswapd` 會為其管轄的 **node 中所有 zone**，按照與分配請求相同的 `zonelist` 順序（即從"更高級/更常用"的 zone，如 `ZONE_MOVABLE`/`ZONE_NORMAL`，到"更基礎/更受限"的 zone，如 `ZONE_DMA`）**逐一檢查每個 zone 是否達到 `high` 水位**，對未達標的 zone 依次進行回收，目標是讓"當前分配請求錨定的那個最高 zone 及其下所有更低 zone"整體都恢復到健康水位——這與頁面分配器"從高 zone 向低 zone 找空閒頁"的方向是一致的（因為 `kswapd` 存在的目的就是提前替分配請求把水位準備好），並非獨立的另一套掃描順序。

### 14. kswapd 以什麼標準來退出掃描 LRU 鏈表？

`kswapd`（`balance_pgdat()`/`kswapd_shrink_node()`）持續掃描回收，直到滿足以下**任一**退出條件：

1. **該 node 下所有 zone 的空閒頁都已達到（或超過）各自的 `high` 水位線**——回收目標已達成，恢復到充裕狀態，可以安心睡眠；
2. **達到本輪設定的最大掃描/回收頁數上限（`nr_to_reclaim`/`SWAP_CLUSTER_MAX` 等相關限制）仍未達標，但已經掃描了足夠多輪（`priority` 從高優先級數值降到 0，即已經嘗試了最"用力"的掃描強度）**——說明當前內存壓力下已經盡力而為，避免無限掃描占用 CPU；
3. 掃描發現**基本沒有可回收的頁面了**（如可回收 LRU 長度過短、或者 `sc.nr_scanned` 相對 `sc.nr_to_reclaim` 的比例顯示繼續掃描性價比很低），提前放棄本輪；
4. 收到**更高優先級的中斷/需要讓路**（如 `kthread_should_stop()` 表示該內核執行緒要被停止）。

一句話：**"達到目標水位（成功）" 或 "已盡最大努力仍無法達到（放棄本輪，等待下次被喚醒再試）"** 兩種情況都會導致退出當前掃描，回到睡眠等待下一次被喚醒。

### 15. 移動設備作業系統（如 Android 作業系統），沒有交換分區或者交換檔案，kswapd 會掃描匿名頁面 LRU 嗎？

一般**不會**（或即使掃描也基本不會真正回收）。因為**匿名頁要被回收，必須先能夠換出到 swap**（把資料保存下來，否則直接丟棄即意味著資料永久丟失），如果系統**根本沒有配置任何 swap（分區/檔案/zram）**，內核的回收邏輯（`get_scan_count()`）在計算匿名頁與檔案頁各自應該分配多少掃描比重時，會檢測到 `total_swap_pages == 0`（無可用 swap 空間），從而**把匿名頁 LRU 的掃描比重直接設為 0**，本輪回收全部壓力都放在檔案頁 LRU 上（檔案頁無論如何都可以直接丟棄、下次存取重新從檔案讀回，不依賴 swap）。

不過要注意：現代 Android 實際上通常會配置 **zRAM（壓縮內存作為"swap"設備）** 或近年的 **內存壓縮 + 匿名頁回寫（如 `ZSWAP`）**等機制間接提供"匿名頁可以被換出"的能力，此時 `total_swap_pages > 0`，`kswapd` 才會正常按 `swappiness` 權重去掃描匿名頁 LRU；如果連 zRAM 都沒有配置（較老的、極簡的嵌入式系統），匿名頁確實完全不會被 LRU 掃描/換出，只能通過 `oom_killer` 直接殺進程來釋放內存。

實測本設備 `free -h` 顯示 `Swap: 3.9Gi`（`/proc/meminfo`：`SwapTotal: 4064756 kB`），確實配置了較大的 swap 空間（很可能是 zRAM 或普通 swapfile），且 `vm.swappiness = 100`（見第16題），說明本設備的回收策略會積極地對匿名頁 LRU 進行掃描與換出。

### 16. swappiness 的含義是什麼？kswapd 如何計算匿名頁面和頁面高速快取之間的掃描比重？

`vm.swappiness`（`0~200`，`/proc/sys/vm/swappiness`）是一個**傾向性調節旋鈕**，表示"內核在選擇回收匿名頁 vs 回收檔案頁（page cache）之間的相對偏好程度"：數值越**高**，越傾向於**更早、更多地換出匿名頁**（把寶貴的物理內存優先留給 page cache，認為磁碟 I/O 快取收益更大）；數值越**低**，越傾向於**優先回收檔案頁 cache、盡量少動匿名頁**（認為匿名頁/進程內存更重要，寧可犧牲檔案快取命中率）；`0` 表示盡量不回收匿名頁（除非檔案頁已經無法滿足回收目標），`100` 是傳統意義上的"兩者同等對待"基準點，`>100`（較新內核支援到 200）則可以進一步表達"比檔案頁更願意回收匿名頁"的傾向。

`get_scan_count()` 的大致計算邏輯：以 `swappiness` 與 `(200 - swappiness)` 分別作為匿名頁、檔案頁兩條 LRU 的**基礎權重比例**，再結合兩者各自當前的 Active/Inactive 比例、最近的 `refault`（換出後又馬上被換入，說明回收"回收錯了"，用作動態修正訊號，抑制過度回收某一類）等因素做調整，最終得到"本輪回收總共要掃描 N 個頁面時，其中大約分配給匿名頁 LRU 多少、檔案頁 LRU 多少"的具體掃描數量。

實測本設備 `vm.swappiness = 100`（並非常見桌面/伺服器預設的 60），說明該嵌入式系統的配置策略是"匿名頁和檔案頁同等對待、都願意積極換出"，結合前述 `SwapTotal` 較大（約 4GB），符合內存相對有限（8GB）、需要更積極利用 swap（很可能是低開銷的 zRAM）來換取更大可用內存的嵌入式設備典型調優思路。

### 17. 當系統中充斥著大量只存取一次的檔案存取時，kswapd 如何來規避這種存取？

這類"掃一遍就不再存取"的檔案存取模式（如大檔案順序讀取、`cp`/備份等一次性 I/O）如果按普通 LRU 邏輯處理，會把這些只用一次的頁面推到 Active 鏈表頭部，**擠佔（thrash）掉真正的熱點資料**，造成"有意義的快取被無意義的一次性資料衝刷掉"的問題。內核用以下機制規避：

- **`workingset` 檢測（refault distance）機制**：頁面被回收前會在對應的基數樹/XArray 位置留下一個"影子（shadow）"記錄，包含"當時它在 Inactive 鏈表中的大致位置（能推算出經過了多久/多少次回收才輪到它）"；當（若）該頁後來又被重新存取（refault）時，內核比較"這次存取距離上次被回收經歷了多長的 LRU 距離"，據此判斷：如果這個距離遠遠超過當前 Inactive 鏈表容量，說明它屬於典型的"掃一遍就不會再碰、純粹一次性"的存取模式，**不應該被提升進 Active 鏈表**（避免污染熱點資料集）；只有 refault 距離較短（說明確實是真正的工作集、只是暫時被擠出去了）時，才認為是"值得提升為活躍頁"的訊號；
- 該機制的統計即體現在 `/proc/vmstat` 的 `workingset_refault_*`/`workingset_activate_*`/`workingset_restore_*` 等計數上——實測本設備 `workingset_refault_file = 79979` 而 `workingset_activate_file` 同為 `79979`（幾乎相等），說明目前發生的檔案頁 refault 大多數確實被判定為"真正的工作集、應當重新激活"，尚未觀測到大規模"一次性掃描污染"的情況。

### 18. 在回收頁面高速快取時，對於髒的頁面高速快取，kswapd 會馬上回寫嗎？

**通常不會立即同步回寫**。原因是磁碟/存儲 I/O 相對內存操作極其緩慢，如果 `kswapd` 掃描到一個髒頁就同步等待其寫回完成，會嚴重拖慢整個回收掃描的推進速度（阻塞在單個慢速 I/O 上）。因此常見做法是：

- 掃描到髒頁時，**優先跳過**（留在 LRU 中，或移動位置），轉而繼續掃描尋找**乾淨、可以直接回收**的頁面，優先完成本輪回收目標；
- 真正需要處理髒頁回寫的，通常交由**獨立的回寫機制（如 `writeback` 相關的 `wb_workqueue`、定期的 `pdflush`/`flusher` 執行緒，或檔案系統日誌相關機制）異步進行**，與 LRU 回收掃描解耦；
- 只有在**回收壓力很大、乾淨頁已經不夠用、且髒頁比例過高**的情況下，`kswapd`/直接回收路徑才會退而求其次，**主動發起（或等待）少量髒頁的同步/異步回寫**，但會盡量克制（內核有 `nr_immediate`/`vmscan_immediate_reclaim` 等統計和限流邏輯，避免大量髒頁同時觸發 I/O 風暴）。

實測本設備 `/proc/vmstat` 中 `nr_vmscan_immediate_reclaim = 330`（相對 `pgscan_kswapd` 逾百萬次掃描而言佔比很小），說明絕大多數回收掃描過程中遇到髒頁時確實是"繞開、不硬等"，只有少數情況才觸發這種"立即回收（含等待回寫）"的較重路徑。

### 19. 內核中有哪些頁面會被 kswapd 回寫到交換分區？

只有**匿名頁（Anonymous Page）**及其等價物（如 `tmpfs`/`shmem` 頁——這類頁雖然掛在 `address_space` 下、行為上更接近檔案頁，但本質沒有真實磁碟檔案，回收時同樣需要寫入 swap 才能保留內容）會被寫入**交換分區/交換檔案（swap）**；普通的**檔案映射頁面高速快取（page cache）**即使是髒頁，也是回寫到它們**各自對應的原始檔案**（通過檔案系統的 writeback 機制），而**不會**被寫到 swap——因為檔案頁天生就有一個"真實的家"（磁碟上的原檔案），沒有必要（也不應該）額外佔用 swap 空間。

因此，"回寫到交換分區"這個動作專屬於**匿名頁 LRU** 一側的回收路徑（`shrink_page_list()` 處理匿名頁分支時，若該頁是髒的/需要換出，調用 `pageout()` → 交換子系統的 `swap_writepage()`），檔案頁分支走的是完全不同的 `->writepage`（檔案系統提供的回寫回調）路徑。

### 20. 請簡述匿名頁面的生命週期。

1. **產生**：進程通過 `malloc`/堆增長（`brk`）/`mmap(MAP_ANONYMOUS)`/棧增長等方式建立匿名 VMA；首次存取觸發缺頁，分配（清零的）物理頁並建立 PTE 映射（見第21題）；
2. **活躍使用**：被進程正常讀寫存取，`PG_referenced`/PTE Access Flag 被置位，傾向留在/晉升到 **Active Anon LRU**；
3. **變冷 / 被回收候選化**：一段時間未被存取，隨掃描逐步從 Active 降到 **Inactive Anon LRU**；
4. **（選填）加入 swap cache 並換出**：當發生內存回收且被選中，先分配 swap slot、把內容寫入 swap 設備，同時該頁短暫存在於 **swap cache** 中（作為"內存與 swap 設備之間的中渡態"）；寫回完成后，拆除頁表映射（PTE 被替換為記錄 swap 位置的 swap entry），最終真正釋放該 `struct page`（`_refcount` 歸零）回夥伴系統；
5. **（選填）換入**：進程之後又存取了該虛擬地址，觸發 `do_swap_page()`（主缺頁，見第4章第38、39題），從 swap 設備讀回資料，重新分配物理頁、加入 swap cache、建立新 PTE 映射，恢復到"活躍"狀態（此時該 swap slot 若仍被引用可能暫不釋放，直到確認沒有其它路徑還需要它）；
6. **最終回收/釋放**：進程退出、`munmap`、或該 VMA 段被 `madvise(MADV_DONTNEED)` 等主動丟棄時，拆除映射、若在 swap 中也一併釋放對應 swap slot，`_refcount` 歸零，物理頁歸還夥伴系統。

整個生命週期是"分配 → 活躍 ⇄ 不活躍（LRU 間往返，取決於是否被存取）→（可能反覆經歷換出/換入）→ 最終釋放"的循環/往返過程，不是一條單向直線。

### 21. 在什麼情況下會產生匿名頁面？

- 進程堆區增長（`brk()`/`sbrk()`）；
- `mmap()` 使用 `MAP_ANONYMOUS`（不關聯檔案的匿名映射，常用於執行緒棧、大塊動態內存如 glibc `malloc` 的大對象走 `mmap`）；
- 進程（用戶態）棧自動向下增長（內核檢測到存取地址落在棧 VMA 之下但在允許自動擴展的範圍內）；
- **寫時複製（COW）**產生的新頁：`fork()` 後子進程對共享的唯讀頁發生寫存取、或私有檔案映射（`MAP_PRIVATE`）發生寫存取時複製出的私有副本，即使原頁是檔案頁，COW 複製出的這份私有副本此後也按**匿名頁**管理（內容已與原檔案脫鉤）；
- `shmem`/`tmpfs`（內存檔案系統）本質上也是一種特殊管理的匿名頁（雖然掛靠在 `address_space` 下，但沒有真實磁碟檔案，回收時同樣走 swap 路徑，行為上更接近匿名頁，見第19題）。

### 22. 在什麼條件下會釋放匿名頁面？

- 該頁的 `_refcount` 歸零，即**再沒有任何頁表映射、也沒有 swap cache/其它內核路徑持有引用**——通常由以下操作觸發這一系列引用遞減：
  - 進程 `munmap()` 顯式取消映射對應區域；
  - 進程整體退出（`exit_mmap()` 拆除所有 VMA 及其頁表映射）；
  - `madvise(MADV_DONTNEED)`/`MADV_FREE` 等主動告知內核"這段內存不再需要"；
  - 內存回收（`kswapd`/直接回收）掃描到該頁且判定應當回收：先換出到 swap（如果尚未在 swap 中）、拆除所有映射它的 PTE（通過 rmap 找到所有共享者，逐一 `unmap`），確認沒有其它引用後釋放；
- 若是被多個進程共享的匿名頁（`fork()` 後父子共享、尚未 COW），需要**所有共享者**都不再映射它（`_mapcount` 歸零，且沒有其它 `_refcount` 來源如 swap cache 臨時引用）才會真正釋放物理頁——單個進程 `munmap`/退出只是讓自己這一份映射消失，`_mapcount`/`_refcount` 相應遞減，但只要還有其它進程仍映射著它，物理頁依然存活。

### 23. 頁面遷移是基於什麼原理來實現的？

頁面遷移（`migrate_pages()`，`mm/migrate.c`）的基本原理是：**在不改變"邏輯上這段虛擬地址指向的資料內容"的前提下，把資料從一個物理頁搬到另一個物理頁，並同步更新所有指向它的映射關係**，對上層（進程）完全透明。實現步驟大致為：

1. **隔離（isolate）**：先把目標頁從其所在的 LRU 鏈表（或其它內核資料結構如 page cache）中摘除，防止遷移過程中被並發的分配器/回收器/其它路徑干擾；
2. **在目標位置分配水一個新的空閒頁**（大小、屬性與源頁一致）；
3. **拆除映射並搬運內容**：通過反向映射（rmap，見第7~9題）找到所有映射源頁的 PTE，先把它們統一替換成一種特殊的**遷移中間態（migration entry，一種特殊編碼的"臨時失效"PTE）**，暫時阻止新的存取（存取會觸發缺頁、被阻塞等待遷移完成）；然後把源頁的實際資料內容**拷貝（`copy_page`/`copy_highpage`）**到新分配的目標頁；
4. **重新建立映射**：把之前替換成 migration entry 的所有 PTE，改指向新頁（同時恢復正常的存取權限位），並更新該頁在 page cache（若是檔案頁）等結構中的索引指向新頁；
5. **釋放源頁**：確認所有映射與索引都已切換完畢後，源頁的引用計數歸零，釋放回夥伴系統；
6. 重新把目標頁加入 LRU 等常規內核結構，遷移完成、恢復正常可存取狀態。

整個過程通過"先暫時凍結存取（migration entry）→ 拷貝資料 → 切換所有引用指向新頁 → 解除凍結"來保證遷移過程中不會有任何路徑讀到"一半舊一半新"的不一致資料，也不會發生資料丟失。

### 24. 內核中有哪些頁面可以遷移？

- 標記為 **`__GFP_MOVABLE`** 分配出來的頁面，典型是用戶態匿名頁與（大部分）檔案頁面高速快取（屬於 `MIGRATE_MOVABLE` 遷移類型，可參與內存規整/遷移，見第4章第4題、本章第45題）；
- 部分**可遷移的內核對象**，如某些通過專門介面註冊了遷移回調（`address_space_operations->migrate_page` 或更新的 `migrate_folio`）的特殊頁面（如某些檔案系統的元資料頁、`zsmalloc`/`zswap` 使用的壓縮頁在支援的情況下）；
- **CMA（Contiguous Memory Allocator）區域內的頁面**：CMA 區平時可以被 `MIGRATE_MOVABLE` 類型的普通分配占用，但當真正需要該區域做大塊連續 DMA 分配時，會強制把當前占用它的可遷移頁遷移走，騰出連續空間（這正是 CMA 依賴頁面遷移機制才能工作的原因，實測本設備 `/proc/pagetypeinfo` 中 `DMA` zone 有 `128` 個 `CMA` 類型的 pageblock，對應 `/proc/meminfo` 的 `CmaTotal: 262144 kB`）；
- 用於 **NUMA 負載均衡（AutoNUMA）** 場景下把頁面遷移到存取它更頻繁的 CPU 所在節點更近的內存（本設備因 `CONFIG_NUMA` 未開啟，此用途不適用）。

### 25. 內核本身使用的頁面是否可以遷移？為什麼？

**通常不可以（預設屬於 `MIGRATE_UNMOVABLE` 類型）**。原因：內核內部大量資料結構（如頁表頁本身、`slab`/`slub` 分配出的各種內核對象、`kmalloc` 出來的緩衝區等）往往通過**裸的內核虛擬地址（線性映射區地址）、甚至直接的物理地址/PFN**被內核代碼中大量、分散、且未必都能被統一追蹤的位置直接引用（不像用戶態頁面那樣，所有引用都規範地通過 VMA/頁表這一套受控機制暴露、可以用統一的 rmap 機制找出所有引用者）——內核沒有一個類似"反向映射"的通用機制能保證枚舉出"當前有哪些指標正指向這個內核頁"，因此**無法安全地做『暫停存取 → 拷貝 → 切換所有引用 → 恢復』這一整套遷移流程**，貿然遷移會導致某些遺漏更新的指標仍指向舊（已被釋放/挪作他用）的物理地址，造成極其隱蔽且危險的內存錯誤。

因此像頁表本身佔用的頁、`slab` 對象所在的頁等，預設都分配自 **`MIGRATE_UNMOVABLE`** 遷移類型（見第45題），夥伴系統會盡量把這類"釘死在原地"的分配都集中放在專門的 `pageblock` 中，避免與可遷移頁混雜在一起、影響可遷移區域的連續性。少數**特別設計過、顯式實現了遷移回調**的內核對象（如部分檔案系統的元資料、`zsmalloc` 池中的頁）例外，可以支援受控的遷移。

### 26. 在頁面遷移的過程中需要注意些什麼？

- **一致性與原子性**：遷移過程中不能出現"部分調用者已經看到新頁、部分還停留在舊頁"的中間不一致狀態，需要通過 migration entry（第23題）等機制確保遷移期間的並發存取被正確阻塞/重定向；
- **引用計數處理**：遷移開始前要正確隔離（防止被 LRU 掃描/其它路徑並發操作），遷移過程中要小心處理源頁/目標頁的 `_refcount`/`_mapcount`，避免計數錯亂導致提前釋放或永久洩漏；
- **髒頁/回寫狀態**：若源頁是髒頁（尤其是檔案頁），需要保證內容被完整、正確地搬到新頁，且後續回寫機制（`writeback`）能正確地對新頁生效，不能丟失"待回寫"這一狀態；
- **鎖的正確獲取順序**：涉及頁鎖（`PG_locked`）、`i_pages`/`address_space` 的鎖、`anon_vma` 鎖、頁表鎖等多把鎖，必須按照內核規定的順序獲取，避免死鎖；
- **失敗回滾**：遷移過程中若目標頁分配失敗、或發現該頁正被其它不相容的操作（如正在做 direct I/O、被釘住 `_refcount` 異常升高）占用，需要能夠乾淨地**回滾**——恢復源頁的正常映射與狀態，不能讓頁面停留在"遷移到一半"的損壞狀態；
- **不可遷移的頁要提前排除**（第25題），遷移框架本身需要先判斷該頁是否屬於可遷移類型，避免對不支援遷移的頁盲目嘗試。

### 27. 什麼是傳統 LRU 頁面和非 LRU 頁面？

- **傳統 LRU 頁面（LRU page）**：會被正式掛入某個 zone/`memcg` 的 **Active/Inactive Anon/File LRU 鏈表**、參與標準的 `kswapd`/直接回收掃描邏輯的頁面，即前述第2、11~19題討論的匿名頁與檔案頁 cache 主體，佔內核管理的"可正常按 LRU 冷熱規律回收"的絕大多數頁面。
- **非 LRU 頁面（Non-LRU page）**：**不掛在標準 LRU 鏈表上**、有著自己獨立生命週期/回收規則管理的一類特殊頁面，典型如：`slab`/`slub` 分配出的內核對象所在頁（由 slab 分配器自己的機制管理增減，見第4章11題）、頁表本身佔用的頁、大部分 `MIGRATE_UNMOVABLE` 類型的內核專用頁、以及一些通過 `PG_slab` 等專用標誌位區分、明確"不參與常規頁面回收算法"的頁面。內核用頁標誌（如 `PageLRU()`）來判斷一個頁當前是否處於 LRU 管理體系內，只有 `PageLRU()` 為真的頁才會被 `shrink_page_list()` 這類標準回收路徑處理，非 LRU 頁面依賴各自子系統自身的回收/收縮鉤子（如 `shrinker` 介面，slab 收縮、`shrink_slab()`）參與整體內存壓力回應。

### 28. 內存規整是基於什麼原理來實現的？

**內存規整（compaction）**用於解決**外部碎片化（第47題）**——即總的空閒頁數量夠，但都是分散的小塊、缺乏足夠大的連續空閒塊可用。其原理與經典的"磁碟碎片整理"思路類似，但更巧妙地利用了頁面遷移機制（第23題）：在一個 zone 內同時維護**兩個掃描指標**：

- **遷移掃描指標（migrate scanner）**：從 zone 的**低地址端**向高地址掃描，尋找**已被占用、且屬於可遷移類型（`MIGRATE_MOVABLE`）**的頁面（這些頁可以被安全地挪走）；
- **空閒掃描指標（free scanner）**：從 zone 的**高地址端**向低地址掃描，尋找**當前空閒**的頁面，作為遷移的目標位置；

兩個指標相向而行：每找到一批"可遷移的占用頁"與相應數量的"空閒目標頁"，就調用頁面遷移機制，把低地址端的占用頁**遷移搬運到高地址端的空閒頁**中去，這樣一來，低地址端原本"占用頁與空閒頁犬牙交錯"的區域會被逐漸清空、騰出連續的大塊空閒空間，而高地址端則相應地被占用頁填滿——整體效果是把空閒空間從"分散在各處的小碎片"**規整聚攏**為"集中在低地址端的大塊連續空閒區間"，從而重新有能力滿足高階（大塊連續物理內存）分配請求。兩指標相遇（或提前達到預設的規整目標/時間預算）時本輪 compaction 結束。

### 29. 如何觸發內存規整？

- **直接（同步）觸發**：在頁面分配的慢速路徑中，當發現請求的高階（`order > 0`）連續內存無法滿足、而水位檢查顯示總空閒頁數其實是夠的（說明問題出在碎片化而非真正缺內存）時，`__alloc_pages_slowpath()` 會調用 `try_to_compact_pages()` 現場發起一次同步的內存規整，規整完成后立即重試分配；
- **後台異步觸發（`kcompactd`）**：類似 `kswapd`，系統為每個 node 啟動一個 **`kcompactd`** 內核執行緒，當檢測到某 zone 的**碎片化指數（fragmentation index）**偏高、或高階水位（`min_free_kbytes`/`watermark_boost` 結合的高階專屬檢查）不滿足時被喚醒，在後台異步做規整，盡量趕在真正的分配請求到來之前把碎片提前整理好，減少分配路徑同步等待規整的延遲；實測本設備 `/proc/vmstat` 中 `compact_daemon_wake = 100`，說明 `kcompactd` 確實在系統運行過程中被喚醒過多次；
- **用戶主動觸發**：寫 `/proc/sys/vm/compact_memory`（觸發全系統所有 zone 的一次同步規整）、或針對具體 node 的 `/sys/devices/system/node/nodeN/compact`，常用於運維/除錯場景手動整理內存碎片。

### 30. 哪些頁面適合做內存規整？哪些頁面不適合做內存規整？

- **適合**：屬於 **`MIGRATE_MOVABLE`** 遷移類型的頁面——典型是用戶態匿名頁、大部分檔案頁面高速快取，這些頁面的所有引用者都規範地通過頁表/`address_space` 暴露、有完整的反向映射資訊，可以安全地執行"遷移掃描指標"所做的搬運操作（本質就是調用第23題所述的頁面遷移）；
- **不適合**：
  - **`MIGRATE_UNMOVABLE`** 類型的頁面（第25題所述的內核內部頁表頁、`slab` 對象頁等）——沒有通用的反向映射機制，無法安全遷移；
  - 被 **`mlock()`** 鎖定、標記為 `PG_mlocked`/`VM_LOCKED` 的頁面——用戶顯式要求"不允許被換出/挪動"，規整也應尊重這一約束（除非配置了 `vm.compact_unevictable_allowed`，允許規整觸及不可回收但可遷移的頁，本設備實測 `vm.compact_unevictable_allowed = 1` 即開啟了這一放寬）；
  - 正在進行 **direct I/O**（如某驅動正對該物理頁做 DMA 傳輸）等場景下被臨時釘住（`_refcount` 異常升高、`PageWriteback()`/相關標誌位表明正處於關鍵 I/O 狀態）的頁面——此時遷移它風險很高（DMA 硬體並不知道頁面被挪動了，硬體仍會往舊的物理地址讀寫），規整邏輯會檢測並跳過這類頁面；
  - `MIGRATE_CMA` 類型的頁雖然本身設計為"總是可遷移"（第24題），但要額外遵守 CMA 區域自身的分配約束。

### 31. KSM 是基於什麼原理來合併頁面的？

KSM（Kernel Samepage Merging）的原理：**掃描被應用主動標記為"可合併"（通常通過 `madvise(MADV_MERGEABLE)`）的匿名頁，找出內容完全相同的多個物理頁，將它們合併為一份唯讀共享的物理頁，原來分別指向各自獨立副本的多個 PTE 全部改為指向這唯一一份共享頁（並置為唯讀），從而節省物理內存**（典型受益場景：虛擬機場景下多個客戶機作業系統內存中大量相同的內核代碼/常見庫/全零頁；或桌面瀏覽器多個標籤頁/多進程共享大量相同的渲染快取內容）。當其中任何一個引用者之後嘗試寫入這份共享頁時，走標準的**寫時複製（COW，第4章41、42題）**路徑，複製出一份私有副本再寫入——因此 KSM 合併頁在使用體驗上與普通"多個進程通過 COW 共享同一份唯讀資料"完全一致，唯一區別是這份共享關係是內核**主動掃描發現相同內容後建立**的，而非天然來自 `fork()`/共享映射。

### 32. 內容相同的頁面在 KSM 裡是如何被掃描和合併的？其工作流程是什麼樣的？

`ksmd` 內核執行緒週期性（由 `sleep_millisecs`、`pages_to_scan` 兩個 sysfs 參數控制掃描節奏與每輪掃描量，實測本設備 `sleep_millisecs=20`、`pages_to_scan=100`）遍歷所有註冊為 `MADV_MERGEABLE` 的匿名頁，工作流程大致：

1. 對候選頁計算一個**校驗和（checksum）**，先做低成本的初步比較；
2. 到 **不穩定樹（unstable tree，一棵按內容/校驗和排序的紅黑樹，`ksm_rmap_item->node`）**中查找是否已有內容"疑似相同"的頁——不穩定樹中的頁可能後續還會被修改（"不穩定"因此得名），比較時需要逐位元組核實內容確實完全一致；
3. 若在不穩定樹中找到內容完全相同的頁，則觸發合併：把兩者都變為唯讀，將其中一個提升為 **穩定樹（stable tree，`ksm_stable_node`，內容一經進入即視為"已確認可長期共享、不會再變"）**中的一個節點，兩個原頁面的映射關係（`ksm_rmap_item`）都掛到這一個 `stable_node` 的 `hlist` 下，其中一份物理頁被真正釋放，所有相關 PTE 改為指向另一份；
4. 若穩定樹中本來就已存在內容相同的頁（更常見的情況——只要系統運行一段時間，穩定樹裡已經積累了不少"熱門重複內容"），新掃到的候選頁直接與穩定樹中的對應節點比較，相同則直接合併進去（追加到該 `stable_node` 的 `hlist`），不必再經過不穩定樹；
5. 後續若某個共享頁被寫入（COW），對應的 `ksm_rmap_item` 從該 `stable_node` 的 `hlist` 摘除，恢復為普通獨立的匿名頁，重新參與後續的掃描（可能之後又與其它內容相同的頁再次被合併）。

### 33. 若穩定的節點的 hlist 中堆積了幾百萬個 rmap_item，那麼對系統會產生什麼影響？

- **穩定樹本身的比較開銷增大**：`stable_node` 掛載在穩定樹（紅黑樹）中，若某幾個"超級熱門"內容對應的 `hlist` 極其龐大，雖然不直接影響紅黑樹本身按內容比較/查找單個節點的複雜度（那仍是 O(log N)，N 是不同"內容"的種類數，與區域內某節點下掛了多少 `rmap_item` 無關），但**任何需要遍歷該 `stable_node->hlist`（如該共享頁需要被回收/遷移/規整時，必須通過 rmap 機制找出所有引用它的 VMA/`rmap_item` 逐一處理，見第7~9題反向映射的開銷討論）的操作，其時間複雜度會隨 `hlist` 長度線性增長**——如果 `hlist` 長達數百萬項，任何這類操作都會變得極其緩慢，長時間占用相關的鎖（如 `stable_node` 或對應頁鎖），阻塞其它並發路徑；
- **內存遷移/規整（第26、28~30題）的代價急劇上升**：若這個被大量共享的物理頁恰好需要參與頁面遷移或內存規整，需要逐一更新數百萬個 PTE 映射，可能導致長時間的鎖持有與延遲尖峰；
- **正是這一問題**促使後續內核版本引入了 **`stable_node_chain`/`stable_node_dup`**（"節點鏈"機制，見第34題）：把原本"一個 `stable_node` 掛一條超長 `hlist`"的結構，拆分/限制為多個"重複節點（dup）"、每個 dup 下的 `hlist` 長度被 `max_page_sharing`（實測本設備該 sysfs 值為 `256`）上限約束，超出上限後新的相同內容頁不再無限制地往同一個物理頁合併，而是允許保留少量"冗餘"的獨立副本，用可控的內存節省幅度換取避免上述性能災難。

### 34. 新版本的 KSM 對穩定的節點做了哪些優化？

針對第33題所述問題，新版本 KSM 引入了：

1. **`max_page_sharing` 上限**（`/sys/kernel/mm/ksm/max_page_sharing`，本設備實測值 `256`）：限制單個共享物理頁最多能被多少個 `rmap_item`/映射關係共享，一旦某個 `stable_node` 下的 `hlist` 長度達到這個上限，後續再發現內容相同的新頁時，**不再繼續往這唯一一個物理頁合併**，而是分裂/建立新的"重複節點（dup）"；
2. **`stable_node_chain`/`stable_node_dup` 結構**（第32題結構體定義中可見 `STABLE_NODE_CHAIN` 特殊標記、`hlist_dup`/`list` 欄位）：把"內容相同"這一組關係組織成一條**節點鏈（chain）**，鏈上掛著多個**重複節點（dup）**，每個 dup 各自獨立維護一份不超過 `max_page_sharing` 長度的 `hlist`——原來"一個內容對應恰好一個物理頁、一條可能無限長的 hlist"，變成"一個內容可以對應多個（數量隨需要動態增長的）物理頁副本，分攤在多個 dup 上，每個 dup 的 hlist 長度都被上限約束"，把原本 O(N) 的最壞情況操作拆分成多個規模可控的小塊；
3. **`stable_node_chains_prune_millisecs` 週期性垃圾回收**（本設備實測 `2000` 毫秒）：定期檢查/合併這些 dup 鏈，清理已經不再需要的中間結構，避免鏈本身隨時間無限膨脹；
4. 代價：這一優化本質是"用適度增加的物理內存占用（允許同一內容存在少量副本，而非強制唯一）"來換取"避免單一共享頁 hlist 無限增長導致的嚴重性能劣化"，體現了 KSM 在"內存節省"與"系統回應性/可控性"之間做出的權衡取捨。

### 35. 在 KSM 機制裡，合併過程中把頁面設置成寫保護的函數 write_protect_page() 中有如下判斷。if (page_mapcount(page) + 1 + swapped != page_count(page)) { goto out_unlock; } 這個判斷的依據是什麼？

這是一個**一致性核實（sanity check）**：確保"當前這個頁面除了我們已知的、正準備參與合併的這些映射/引用之外，沒有任何其它意料之外的引用者"，否則貿然把它設為唯讀並繼續合併流程可能會破壞那些"未知引用者"原本的語義（如它們本以為自己獨佔該頁、可以隨時寫入）。

- `page_mapcount(page)`：當前有多少個頁表項（PTE）映射著這個頁；
- `+ 1`：`write_protect_page()` 自己在核實這一刻，通常也臨時持有/即將持有一份對該頁的引用（如通過 `page_lock`/`get_page` 或調用路徑本身），這個 `+1` 是把"當前這次檢查操作自身占用的那一份引用"也算進預期值裡；
- `swapped`：如果該頁當前同時存在於 **swap cache** 中（尚未被完全從換出狀態清理乾淨），swap cache 本身也會持有一份額外的 `_refcount` 引用，這裡作為"已知、可以解釋"的引用一併加進預期總數；
- `page_count(page)`（即 `_refcount`）：該頁**實際的總引用計數**。

如果 `page_mapcount + 1 + swapped == page_count`，說明"能想到的所有引用來源（頁表映射 + 本次操作自身 + swap cache）加起來，正好等於實際總引用數"，**沒有遺漏、沒有多出來的神秘引用者**，可以放心繼續把該頁設為寫保護、納入合併流程；如果**不相等**（通常是 `page_count` 更大），說明還有別的代碼路徑也正持有對這個頁的引用（比如正在被 `get_user_pages()` 釘住做 DMA、或被其它子系統臨時引用），此時貿然合併/寫保護可能與那個"不知情"的引用者產生衝突或資料不一致，因此保守地 `goto out_unlock` 放棄本次合併，等下次掃描再重新嘗試。

### 36. 如果多個 VMA 的虛擬頁面同時映射了同一個匿名頁面，那麼此時 page->index 應該等於多少？

對於**匿名頁**，`page->index` 記錄的是：**該頁在它所屬的那個 `anon_vma` 對應"虛擬地址區間"裡的（以頁為單位）的偏移量**，具體計算為該頁第一次建立映射時所在 VMA 內的相對頁偏移（`(address - vma->vm_start) >> PAGE_SHIFT`，再結合 VMA 自身的 `vm_pgoff` 基準做統一換算），**它是以最初建立該匿名頁映射的那個 VMA/地址關係為準記錄的一個固定值，即使後來這個頁又被其它 VMA（比如 `fork()` 產生的子進程 VMA，各自 `vm_start` 可能不同但相對偏移一致，因為是同一段地址區間在父子間的複製）共享映射，`page->index` 依然沿用最初確定的這個值不變**——因為 `fork()` 後子進程的匿名 VMA 雖然是獨立的 `vm_area_struct`，但通常與父 VMA 描述的是"同一段相對地址關係"（子進程的地址空間佈局是從父進程複製而來，只是 `mm_struct`/頁表不同，虛擬地址數值本身通常保持一致，或至少相對偏移一致），所以 `page->index` 這個"頁在其虛擬地址區間內的相對位置"標識，對於所有共享該匿名頁的 VMA 而言依然是一致、有意義的，可以配合 `anon_vma`/rmap 機制推算出該頁在任意一個共享者 VMA 中對應的具體虛擬地址（第3章第3題所述"由 page 和 VMA 找到 vaddr"的轉換關係，正是依賴 `page->index` 這個記錄）。

### 37. 頁面和 KSM 頁面的區別是什麼？

- **普通（未合併）匿名/檔案頁面**：`page->mapping` 指向 `anon_vma`（匿名）或 `address_space`（檔案），`_mapcount` 反映的是"實際有多少 PTE 直接映射它"，沒有經過 KSM 的內容比較/合併流程，是"天然"產生（分配/COW/檔案讀入）的頁面。
- **KSM 頁面**：是被 KSM 掃描發現"內容與另一個/另一批頁完全相同"後，經過合併流程產生的**特殊唯讀共享匿名頁**——`PageKsm(page)` 為真，其反向映射不再是簡單的"一個 `anon_vma`"，而是通過 `stable_node`（第32~34題）關聯著**一組內容相同、原本分別屬於不同虛擬地址/不同 `anon_vma` 的 `rmap_item`**；該頁始終保持**唯讀**，任何寫入企圖都會觸發 COW（第31題）先分裂出一份獨立副本，寫入後該副本便"退出" KSM 管理、變回普通匿名頁。

一句話：**KSM 頁面是"內容驅動、事後被內核主動發現並人為建立共享關係"的一類特殊匿名頁，普通頁面的共享關係（若有）則是"天然"由 `fork()`/`mmap(MAP_SHARED)` 等操作預先決定的**，兩者在使用層面（讀共享、寫 COW）表現一致，區別主要體現在反向映射資料結構與產生原因上。

### 38. 頁面分配器如何管理空閒頁面和分配請求之間的關係？

頁面分配器（`mm/page_alloc.c`）本質上是在**"當前各 zone 各 order 的空閒頁塊存量（夥伴系統空閒鏈表 + per-CPU 快取 pcplist）"**與**"源源不斷到來的、大小（order）、屬性（GFP 掩碼/遷移類型/zone 上限）各異的分配請求"**之間做撮合與調度：

- **空閒側**：夥伴系統按 zone、按 order、按遷移類型（第45題）分門別類地組織空閒頁塊鏈表，另外每個 CPU 還維護一份 order-0（單頁）的**per-CPU 快取（pcplist）**，把最常見的小額高頻分配/釋放請求攔截在 per-CPU 層面處理，減少直接碰全局夥伴系統鏈表（及其鎖）的頻率；
- **請求側**：每個分配請求攜帶 `order`（要幾個連續頁）、`gfp_mask`（決定最高允許到哪個 zone、是否允許睡眠回收、是否走緊急預留等）、隱含的遷移類型（決定優先從哪個遷移類型的空閒鏈表找）；
- **撮合邏輯**：優先走**快速路徑**（第39題）直接在合適的 zone/pcplist 找現成空閒塊；找不到則觸發**喚醒 kswapd（第10題）**、必要時**內存規整（第28、29題）**、**直接回收**、甚至**犧牲預留內存（第43題）/OOM**等一系列"創造更多可用空閒頁/塊"的手段，反覆重試，直到滿足請求或最終失敗（返回 `NULL`/觸發 OOM）。

它可以把它類比為一個"資源池管理器"：空閒頁是資源池存量，分配請求是取用需求，分配器通過分層快取（pcplist）、分類組織（zone/order/migratetype）、以及在存量不足時主動觸發多種"補貨"機制（回收、規整、喚醒後台執行緒），盡量在**性能（大多數請求走最快路徑）**與**成功率（存量緊張時仍有辦法滿足關鍵請求）**之間取得平衡。

### 39. 什麼是頁面分配器的快速路徑？什麼是慢速路徑？

- **快速路徑（fast path，`get_page_from_freelist()`）**：假設當前內存較為充裕的"樂觀路徑"——直接按 `zonelist` 順序遍歷候選 zone，對每個 zone 做水位檢查（`zone_watermark_fast()`，一般以 `high` 或 `low` 水位為準，取決於是否已經喚醒過 `kswapd`），一旦找到某個 zone 滿足水位且有所需 order/遷移類型的空閒塊（優先查 per-CPU pcplist，其次夥伴系統鏈表），立即分配成功返回——全程**不涉及內存回收、不觸發睡眠等待**，開銷小、延遲低，是絕大多數（內存充裕時）分配請求的實際路徑；
- **慢速路徑（slow path，`__alloc_pages_slowpath()`）**：當快速路徑未能找到滿足條件的 zone/空閒塊時才會進入，會依次嘗試一系列"更費力"的補救手段——**喚醒 `kswapd`**、放寬/調低水位檢查標準（`ALLOC_HARDER` 等，若調用者身份允許）、**內存規整**、**直接回收**（自己參與掃描 LRU 釋放內存）、必要時**多次重試**上述過程（配合優先級/水位逐步放寬），最終仍不行則依據 `gfp_mask` 決定是**返回失敗（`NULL`）**還是觸發 **OOM Killer**。慢速路徑涉及可能的睡眠等待（回收、規整期間），開銷和延遲遠高於快速路徑，只在內存緊張時才會被觸發。

### 40. 當一個普通的進程惡意占用內存時，頁面分配器是如何處理的？

頁面分配器本身**不直接"懲罰"某個進程**，它的職責只是按照水位、回收、規整等標準機制盡力滿足或拒絕分配請求；真正針對"內存被過度占用、系統整體承壓"的應對，主要體現在慢速路徑觸發的一系列連鎖反應：

1. **持續喚醒 `kswapd` 後台回收**（試圖騰出內存供其它進程使用）；
2. 該惡意進程自身繼續申請內存時，也會被迫走**直接回收（direct reclaim）**——即它自己發起的分配請求會因為內存緊張而被拖慢（在自己的系統調用路徑中被迫參與掃描回收，相當於"誰申請內存申請得兇，誰自己就要承擔更多用於回收的 CPU 開銷"，一定程度上自我抑制）；
3. 若持續到水位即使經過回收、規整也無法恢復、系統整體陷入內存耗盡的僵局，最終觸發 **OOM Killer**：內核根據每個進程的 `oom_score`（主要由其占用的內存量、`oom_score_adj` 等因素決定，占用內存越多通常 `oom_score` 越高）選擇"最應該被犧牲"的進程直接 `SIGKILL` 掉，強制釋放其占用的全部內存——這正是應對"某進程惡意/異常地持續占用大量內存導致系統整體不可用"這一場景的最終手段。

也就是說，頁面分配器體系提供的是"回收（迫使占用者自身分擔開銷）→ 規整 → 拒絕新請求（超過 `overcommit` 限制則 `mmap`/`malloc` 直接失敗）→ OOM 殺死重罪進程"這樣一套層層遞進、最終有底線的應對機制。

### 41. 若一個普通進程處於內存承壓的情況下，頁面分配器嘗試哪些努力來保證分配成功？

（即慢速路徑 `__alloc_pages_slowpath()` 依次嘗試的手段，按大致順序）：

1. **喚醒 `kswapd`**，寄望其後台回收能在本次分配重試時已經騰出足夠空間；
2. **放寬本次檢查用的水位/分配標誌**（如某些場景允許降級使用 `ALLOC_HARDER`），讓原本卡在 `low`/`high` 水位線的請求有機會以更低的門檻（如降到接近 `min`）通過；
3. **嘗試內存規整（compaction）**：若判斷問題主要是外碎片化（總空閒夠、缺大塊連續），先規整騰出連續大塊再重試分配；
4. **發起直接回收（direct reclaim）**：調用者自己參與掃描、回收 LRU 頁面，回收出一定數量的空閒頁後重新嘗試分配；
5. **反覆重試上述 2~4 步驟若干輪**（每輪可能進一步調整策略/放寬條件），期間也會檢查是否有訊號/致命條件需要提前退出；
6. 若調用者具備特殊身份（如 `PF_MEMALLOC`、`TIF_MEMDIE` 等標誌，常見於正在回應內存壓力本身的路徑、或已經被 OOM 選中即將退出的進程），可能被允許使用**緊急預留內存**（`ALLOC_HARDER`/`ALLOC_HIGH`/`ALLOC_OOM`，第43題）；
7. 若以上均告失敗，根據 `gfp_mask` 是否允許（如是否 `__GFP_NOFAIL`/是否可以直接返回失敗）決定**放棄並返回 `NULL`**，還是觸發 **OOM Killer** 後再重試一次。

對於"普通進程"（不具備上述特殊豁免身份）而言，主要能依靠的就是 1~5 這幾步"公平參與回收/規整"的常規努力，通常不會被給予存取緊急預留內存的特權。

### 42. 在什麼情況下頁面分配器可以訪問系統預留內存？

系統會為每個 zone 預留一部分"平時不動用、專供極端緊急場景使用"的內存（體現為水位線 `min` 以下、以及第43題所述通過降低有效水位間接"借用"到的部分），只有滿足以下條件之一時，分配請求才被允許突破正常水位限制去動用這部分預留：

- **`__GFP_ATOMIC`/`__GFP_HIGH`（對應 `ALLOC_HIGH`）**：調用者明確聲明"這是原子/高優先級分配，不能失敗也不能等待回收"（如網路協定棧在中斷/軟中斷上下文中的關鍵分配路徑），允許適度突破水位（`min -= min/2`）；
- **`ALLOC_HARDER`**（常與 `__GFP_ATOMIC` 一起、或某些實時性要求高的場景）：進一步放寬水位要求（`min -= min/4`）；
- **進程正處於 OOM 流程中、即將退出（`ALLOC_OOM`/被標記為 OOM 受害者）**：允許它自己在退出前所需的少量內存分配更容易成功（更快地釋放它占用的其它內存，儘快讓系統脫離危機），此時 `min -= min/2`（與 `ALLOC_HIGH` 同等幅度，但語義不同、且常與 `ALLOC_HARDER` 一起生效）；
- **`ALLOC_NO_WATERMARKS`**：完全不檢查水位（用於極特殊的、必須成功的核心路徑，如某些內存回收自身運作所需的關鍵分配、`PF_MEMALLOC` 標記的進程），本質上就是被允許"想用多少預留就用多少"；
- 具備 **`CAP_SYS_ADMIN`** 等特權、或被 cpuset/memcg 配置顯式豁免的場景，也可能被授予接近 `ALLOC_NO_WATERMARKS` 的待遇。

普通、不帶任何特殊標誌的一般分配請求（如典型的 `GFP_KERNEL`/`GFP_HIGHUSER_MOVABLE`）**始終不能**觸碰這部分預留，必須老老實實排隊等待回收/規整騰出正常空閒內存。

### 43. 在頁面分配器中，分配掩碼 ALLOC_HIGH、ALLOC_HARDER、ALLOC_OOM 以及 ALLOC_NO_WATERMARKS 之間有什麼區別？它們各自能訪問系統預留內存的百分比是多少？思考為什麼頁面分配器需要做這樣的安排。

實測本設備內核原始碼 `mm/page_alloc.c` 的 `__zone_watermark_ok()` 給出精確的判定邏輯（`min` 是本來應滿足的水位線數值）：

```c
if (alloc_flags & ALLOC_HIGH)
    min -= min / 2;                 // ALLOC_HIGH：水位門檻降為原本的 50%

if (unlikely(alloc_harder)) {       // alloc_harder = ALLOC_HARDER | ALLOC_OOM
    if (alloc_flags & ALLOC_OOM)
        min -= min / 2;             // ALLOC_OOM：在 alloc_harder 分支內再打對折
    else
        min -= min / 4;             // ALLOC_HARDER（非OOM）：只降低 25%
}
```

- **`ALLOC_HIGH`**（對應 `__GFP_HIGH`）：門檻降為原來的 **50%**（`min -= min/2`），即最多可以額外動用相當於"正常門檻一半"的預留部分；
- **`ALLOC_HARDER`**（不含 OOM，如部分 `__GFP_ATOMIC` 場景）：在（可能已被 `ALLOC_HIGH` 降過一次的）門檻基礎上再降低 **25%**（`min -= min/4`）；由於 `__GFP_ATOMIC` 實際上會**同時**設置 `ALLOC_HARDER` 與 `ALLOC_HIGH` 兩個標誌（原始碼註釋明確說明），兩次削減是**複合疊加**的：先變為 `min/2`，再在此基礎上減去 `(min/2)/4`，最終門檻降為原始 `min` 的 **3/8（37.5%）**，即可以動用高達 **62.5%** 的原本"預留"部分；
- **`ALLOC_OOM`**：屬於 `alloc_harder` 判斷範圍內的更激進分支，直接再打對折（`min -= min/2`），若與 `ALLOC_HIGH` 疊加（`min/2` 基礎上再 `-min/2`）門檻可降至原始的 **1/4（25%）**，即可動用高達 **75%** 的預留——比普通 `ALLOC_HARDER` 更激進，因為 OOM 受害進程"即將退出、這次分配是為了讓它能儘快釋放更多內存、緩解危機"，值得給予最大讓步；
- **`ALLOC_NO_WATERMARKS`**：**完全不檢查水位**（直接跳過整個 `__zone_watermark_ok` 判斷，見 `mm/page_alloc.c` 中 `if (alloc_flags & ALLOC_NO_WATERMARKS) return true;` 一類的短路邏輯），相當於可以動用 **100%** 的預留（乃至理論上榨乾 zone 裡最後一頁），是最高等級的豁免。

**為什麼要這樣分級安排**：內存耗盡是一個"越接近臨界點、越需要區別對待不同緊迫程度的請求"的場景——完全不加區分地對所有請求一視同仁（要麼都嚴格守住水位、要麼都放開），要麼會讓本可以通過犧牲少量預留就能解決的緊急/關鍵分配（如網路中斷處理、內存回收自身運作所需的分配）在最需要成功的時候反而失敗、引發連鎖故障；要麼會讓預留內存被普通請求輕易耗盡，起不到"留給關鍵場景兜底"的作用。分級、按比例遞進地放寬水位，本質上是一種**優先級調度**：讓"越關鍵、越不能失敗、越有助於讓系統儘快脫離危機（如 OOM 受害者退出釋放內存）"的請求，被允許拿到更大比例的"最後儲備"，而普通請求則被要求老實排隊、通過回收/規整等正常手段自行爭取內存，從而在系統瀕臨崩潰的邊緣盡量保證最重要的操作還能繼續運作，避免"雪崩式"的全面失敗。

### 44. 夥伴系統算法是如何減少內存碎片的？

- **合併（coalescing）機制**（第7題）：釋放頁面時主動檢測並合併相鄰夥伴塊，持續把小的空閒碎片聚合回大塊，是對抗碎片化最基本的"自癒"能力；
- **按 order 分級管理**：不同大小的空閒請求各自在對應 order 的鏈表中查找，避免了"不按大小分類、隨意查找"可能導致的大塊被不斷切碎、小塊卻難以拼湊回大塊的亂象；
- **分裂時優先切割大塊的高位部分**：保證每次分裂產生的夥伴仍然是"規整對齊"的地址（`buddy_pfn = pfn XOR (1<<order)`），使得日後釋放時依然能高效地按位運算找回夥伴、大概率能重新合併回去，而不是產生難以再拼接利用的"歪斜"地址範圍；
- **遷移類型分區**（第45、46題）：把可遷移、可回收、不可遷移的頁分開存放在不同的 `pageblock` 中，避免"生命週期/可挪動性差異很大"的頁面混雜在一起，防止長期存在的不可遷移小對象"釘"在一大片區域中間，阻礙相鄰大量可遷移頁合併成大塊；
- 結合**內存規整（compaction，第28~30題）**：當純粹依賴"合併"仍然不足以應對外碎片化時（存在無法直接合併、但可以遷移挪動的占用頁），主動遷移騰挪，進一步提升大塊連續物理內存的可獲得性，是對夥伴系統"被動合併"能力的主動補強。

### 45. 為什麼要把內存分成不同的遷移類型？這些遷移類型有什麼區別？

**原因**：不同用途的內存分配，其"生命週期長短"與"是否可以被安全挪動（遷移/回收）"差異巨大——如果把"可能長期占用、無法遷移"的內核對象（如頁表頁、slab 對象）與"隨時可能被回收/遷移走"的用戶態頁面混雜分配在同一片物理區域中，當需要通過合併或規整騰出大塊連續內存時，會因為這片區域中夾雜著幾個"釘死不能動"的頁而始終無法把周圍一大片本可遷移的空閒/占用頁湊成連續大塊——即所謂"一顆老鼠屎壞了一鍋粥"。為了避免這種情況，Linux 引入**遷移類型（migratetype）**，盡量讓"同類生命週期/遷移特性"的頁面聚集分配在同一批 `pageblock`（本設備實測 `Page block order: 9`，即每 512 個連續頁為一個 `pageblock` 單位）中，隔離不同類型，最大化規整/合併的效果。

各遷移類型區別（對應 `enum migratetype`，`include/linux/mmzone.h`）：

- **`MIGRATE_UNMOVABLE`（不可移動）**：如頁表頁、大部分內核 `slab` 對象、`kmalloc` 大塊內存——一旦分配位置固定，就不能被遷移/挪動，是內存規整時唯一真正"繞開、無法處理"的類型；
- **`MIGRATE_MOVABLE`（可移動）**：用戶態匿名頁、大部分檔案頁面高速快取——可以被頁面遷移機制自由搬動，是內存規整的主要操作對象，佔實測本設備 `pagetypeinfo` 中數量最多的 pageblock 類型（`DMA: 1589`、`Normal: 1933`）；
- **`MIGRATE_RECLAIMABLE`（可回收）**：內容可以直接被丟棄、之後按需重新生成/重新讀取的內核對象（如部分可回收的 `slab` cache、某些檔案系統元資料快取），不需要真正"遷移"，回收壓力大時可以直接釋放騰出空間；
- **`MIGRATE_HIGHATOMIC`**：為滿足**原子（不可睡眠）、高優先級但又不適合走緊急預留水位機制**的高階分配請求而預留的一小部分頁塊，避免這類請求因為找不到合適大小的連續塊而失敗；
- **`MIGRATE_CMA`**：CMA（Contiguous Memory Allocator）專屬類型（第24題），平時可被普通可遷移分配借用，但需要時會強制騰空、專供大塊連續 DMA 使用，本設備實測 `DMA` zone 有 128 個此類型 pageblock（對應 256MB CMA 保留）；
- **`MIGRATE_ISOLATE`**：臨時"隔離"狀態，用於內存熱插拔（下線某段物理內存前）、`CMA`/規整過程中臨時標記"這段區域暫時禁止被任何新分配觸及，正在被特殊處理"的中渡態，不是一種"正常使用"的分類，而是一種操作過程中的臨時標記。

### 46. 假設請求分配 order 為 4 的一個內存塊，遷移類型是不可遷移（MIGRATE_UNMOVABLE），但是 order 大於或等於 4 的不可遷移類型的空閒鏈表中沒有空閒頁塊，那麼頁面分配器會怎麼辦？

會執行**回退分配（fallback allocation）**機制（`__rmqueue_fallback()`）：

1. 先按照預先定義好的**回退優先順序表**（`fallbacks[MIGRATE_TYPES][...]`，大致順序如 `UNMOVABLE → RECLAIMABLE → MOVABLE`，各類型都定義了各自願意"借用"哪些其它類型），在 `order>=4` 的範圍內，嘗試從**允許被借用的其它遷移類型**（比如先嘗試從 `MIGRATE_RECLAIMABLE`，找不到再嘗試從 `MIGRATE_MOVABLE`）的空閒鏈表中找一個足夠大的塊；
2. 找到後，**優先選擇盡可能大的 order**（從該其它類型裡能找到的最大可用塊開始），把它整體先按第1題的"分裂"流程切出所需的 `order=4` 大小返回給本次請求；剩餘部分中，**通常會把這個借來的 pageblock 的遷移類型屬性整體改標記為 `MIGRATE_UNMOVABLE`**（即"污染/轉換"這個 `pageblock` 歸屬，除非該次借用被認為是"臨時應急、不應永久改變歸屬"的特殊情況，視具體內核版本的啟發式策略而定），這樣做的目的是保持"同一個 `pageblock` 內的頁盡量歸屬同一遷移類型"的整體不變量，減少未來該區域內碎片類型混雜；
3. 如果連所有允許借用的遷移類型也都合找不到 `order>=4` 的空閒塊，則說明當前該 zone 已經碎片化到沒有滿足條件的大塊可用，會進一步觸發**內存規整（第28、29題）**嘗試騰出符合要求的大塊，規整後再重試上述過程；
4. 若規整之後依然無法滿足，則和其它分配失敗情形一樣，進入更廣義的慢速路徑處理（喚醒 kswapd/直接回收/重試/最終失敗或 OOM，見第39~43題）。

### 47. 什麼是內存外碎片化？Linux 內核的頁面分配器是如何發現外碎片的？

**外部碎片化（External Fragmentation）**：系統**總的空閒內存數量其實是足夠的**，但這些空閒頁分散在物理地址空間的各個角落、彼此不連續，導致**無法滿足需要"一大塊連續物理內存"的高階（`order > 0`）分配請求**——區別於"內部碎片"（第1章第31題，指單次分配內部因對齊/取整浪費的空間），外碎片是"總量夠、連續性不夠"的問題。

Linux 通過 **`extfrag_index`（外碎片化指數，`mm/vmstat.c`/`mm/compaction.c` 中 `fragmentation_index()`）**這一指標來量化判斷：其計算依據當前 zone 裡各 order 的空閒塊分佈，估算"如果理想地把所有空閒頁合併、這個 zone 本可以支援多大的最大連續分配"，與"實際觀測到的、按當前碎片化程度能提供的最大連續分配能力"兩者的差距——指數越接近 **1**，說明碎片化程度越嚴重（內存總量看似夠，但幾乎湊不出請求所需的連續大小，規整會很有價值）；越接近 **0**（甚至為負），則說明即使做規整也未必能顯著改善（可能是總空閒量本身就不夠，規整解決不了根本問題）。分配器/`kcompactd` 正是依據這一指數、結合高階水位檢查結果，判斷"當前是否值得觸發一次規整嘗試"。

### 48. 當發現了內存外碎片，Linux 5.0 內核是如何處理的？

發現外碎片化程度較高（`fragmentation_index` 接近 1、且高階水位檢查未通過）時，Linux（含本設備實測運行的 6.1 內核，機制與 5.0 一脈相承）主要通過**內存規整（compaction，第28~30題）**來處理：

1. 若是在頁面分配的慢速路徑中**同步**發現（`order>0` 請求失敗，但總空閒量檢查顯示"理論上湊得格"），會**直接同步調用 `try_to_compact_pages()`**，現場進行一輪規整（遷移掃描指標+空閒掃描指標相向而行，把可遷移的占用頁搬到一端、騰出連續空閒區），完成或達到預算上限後立即重試原本失敗的分配；
2. 若是**後台 `kcompactd`** 定期/被水位檢測喚醒發現的（不一定對應某個正在等待的具體分配請求），則異步地提前做規整，把碎片提前整理好，減少將來真正有高階分配請求到來時需要同步等待規整的概率與延遲；
3. 規整過程本身會**優先處理 `MIGRATE_MOVABLE`/`MIGRATE_CMA` 這類可遷移頁**（第30題），跳過 `MIGRATE_UNMOVABLE`（無法遷移，第25、44、46題）與被釘住/正在 I/O 的頁面，因此**規整並不能保證 100% 消除外碎片**——如果碎片主要是由大量分散的 `MIGRATE_UNMOVABLE` 小塊頁造成（第45、46題所述"借用/污染"遷移類型的累積效應），規整的改善效果會打折扣，這也是為什麼第45題所述的"盡量把不同遷移類型隔離存放、減少 UNMOVABLE 頁擴散污染 MOVABLE 區域"在源頭上如此重要——規整是**事後補救**手段，遷移類型隔離與合理的分配策略才是**從源頭減少外碎片產生**的根本方法，兩者需要配合，而非只依賴規整單獨解決問題。
