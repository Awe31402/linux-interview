# 《奔跑吧 Linux內核》（第二版）卷1 第2章 ARM64 在 Linux 核心中的實現 高頻面試題

> **說明**：收錄自第 2 章開篇「本章的高頻面試題」，共計 23 題。
> 答案結合書本理論、Linux 核心原始碼（本專案樹 `arch/arm64/`，版本 6.1.115）、
> 以及 Radxa ROCK 5B（RK3588）實機（`radxa@192.168.68.57`）的實測資料。

---

### 1. ARM64 處理器中有兩個頁表基位址暫存器 TTBR0 和 TTBR1，處理器如何使用它們？

ARM64 把 64 位元虛擬位址空間一分為二，用位址的**第 63 位元（VA[63]）**決定選用哪一個頁表基址暫存器：

- **VA[63] = 0** → 使用 **TTBR0_ELx**，涵蓋使用者空間位址範圍（如 4 級頁表下的 `0x0000000000000000 ~ 0x0000ffffffffffff`）；
- **VA[63] = 1** → 使用 **TTBR1_ELx**，涵蓋核心空間位址範圍（如 `0xffff000000000000 ~ 0xffffffffffffffff`）。

對應地，`TCR_ELx`（Translation Control Register）裡有獨立的 `T0SZ`/`T1SZ` 欄位分別設定 TTBR0、TTBR1 各自管理的位址範圍大小（有效虛擬位址位元數），兩者可以設定為不同大小，也可各自獨立啟用/停用（`TCR.EPD0`/`EPD1`）。在 Linux 核心中：

- `swapper_pg_dir`（核心全域頁表）的實體位址寫入 **TTBR1**，且從不寫入 TTBR0；
- 每個行程的使用者態頁表（`mm->pgd`）寫入 **TTBR0**，行程切換（`switch_mm`）時只需要更換 TTBR0（連同 ASID），TTBR1 指向的核心頁表在所有行程間保持不變，因此核心位址在所有位址空間中都能直接存取，無需切換。

實測該 6.1 核心原始碼 `arch/arm64/mm/context.c`、`arch/arm64/kernel/head.S` 中對 TTBR0/TTBR1 的寫入正對應上述職責劃分；本裝置 `CONFIG_ARM64_VA_BITS=48`、`CONFIG_ARM64_PA_BITS=48`，使用者/核心各自 48 位元虛擬位址空間。

### 2. 請簡述 ARM64 處理器的 4 級頁表的映射過程，假設頁面粒度為 4KB，位址寬度為 48 位元。

4KB 頁、48 位元虛擬位址下，轉換表分為 **L0（PGD）→ L1（PUD）→ L2（PMD）→ L3（PTE）**共 4 級，每級用 9 位元位址位元索引（每張表 512 項，`2^9 = 512`，每項 8 位元組，正好一張表佔一個 4KB 頁），加上 12 位元頁內偏移：

```
|63    56|55    48|47    40|39    32|31    24|23    16|15     8|7      0|
 |                 |         |         |         |         v
 |                 |         |         |         |    [11:0]  頁內偏移
 |                 |         |         |         +--> [20:12] L3(PTE) 索引
 |                 |         |         +------------> [29:21] L2(PMD) 索引
 |                 |         +----------------------> [38:30] L1(PUD) 索引
 |                 +--------------------------------> [47:39] L0(PGD) 索引
 +----------------------------------------------------------> [63] 選擇 TTBR0/TTBR1
```

映射流程：
1. 處理器根據 VA[63] 選擇 TTBR0 或 TTBR1，得到 L0 表的實體基址；
2. 用 VA[47:39] 索引 L0 表，取得一個表項，指向 L1 表實體基址；
3. 用 VA[38:30] 索引 L1 表項；此級除了指向下一級表外，也可能是 1GB 的**區塊（block）描述符**直接映射到實體頁（跳過 L2/L3）；
4. 用 VA[29:21] 索引 L2 表項；同樣可能是 2MB 區塊描述符直接映射；
5. 若未在 L1/L2 提前作為區塊映射結束，則用 VA[20:12] 索引 L3（PTE）表項，得到最終的 4KB **頁描述符**（page descriptor），其中含實體頁頁幀號（PFN，即 VA[47:12] 轉換後對應的 PA 高位）及存取屬性（AP、AF、SH、AttrIndx 等）；
6. 最終實體位址 = 頁描述符中的 PFN（拼接 PA 高位）+ VA[11:0]（頁內偏移）。

本裝置 6.1 核心確認 `CONFIG_PGTABLE_LEVELS=4`、`CONFIG_ARM64_4K_PAGES=y`、`CONFIG_ARM64_VA_BITS_48=y`，與上述計算完全一致。

### 3. 在 L0～L2 頁表項描述符中，如何判斷一個頁表項是區塊類型還是頁表類型？

用頁表項（descriptor）的**最低 2 位元 `[1:0]`**來區分：

- `[1:0] = 0b11`（Valid=1, 表類型位元=1）→ **表描述符（Table descriptor）**，指向下一級頁表的實體基址，需要繼續往下查；
- `[1:0] = 0b01`（Valid=1, 表類型位元=0）→ **區塊描述符（Block descriptor）**，本級直接給出一段實體記憶體的映射，轉換到此結束（僅 L1/L2 有效——L1 區塊對應 1GB，L2 區塊對應 2MB；L0 級隻允許表描述符，不支援區塊映射）；
- `[1:0] = 0b00` 或 `0b10`（Valid=0）→ 該頁表項無效（Invalid），存取該位址觸發轉換錯誤（Translation Fault）。

而在最底層的 L3（PTE）表中，`[1:0] = 0b11` 表示有效的**頁描述符（Page descriptor）**（此時 bit1 複用為 "page" 標誌而非 "table" 標誌，語意上是葉子節點），是轉換的最終終點。

### 4. 在 ARM64 Linux 核心中，使用者空間和核心空間是如何劃分的？

依據核心官方文件 `Documentation/arm64/memory.rst`（本儲存庫繁中翻譯版 `Documentation/translations/zh_TW/arm64/memory.txt`）：用位址第 63 位元區分——**使用者位址空間第 63:48 位元為 0**，**核心位址空間對應位元為 1**。以 4KB 頁 + 4 級頁表（48 位元虛擬位址）為例：

```
起始位址              結束位址              大小     用途
0000000000000000  0000ffffffffffff  256TB    使用者空間（TTBR0）
ffff000000000000  ffffffffffffffff  256TB    核心空間（TTBR1）
```

使用者空間由每個行程各自的 `mm->pgd` 描述、載入 TTBR0；核心空間由全域唯一的 `swapper_pg_dir` 描述、載入 TTBR1，對所有行程共享。本裝置核心設定 `CONFIG_ARM64_VA_BITS=48`，與該文件給出的 256TB/256TB 劃分吻合；實測 `_stext` 位址為 `ffff800008010000`，落在核心空間範圍內（`0xffff8...` 字首正是 `PAGE_OFFSET` 之後的線性映射高位區）。

### 5. 在 ARM64 Linux 核心中，PAGE_OFFSET 表示什麼意思？

`PAGE_OFFSET` 是**實體記憶體在核心空間中做線性映射（linear/direct mapping）的起始虛擬位址**。核心會把（幾乎）全部實體記憶體，從 `PAGE_OFFSET` 開始按 `虛擬位址 = 實體位址 + (PAGE_OFFSET - PHYS_OFFSET)` 的固定偏移關係一一映射到核心空間的這段區域，使核心可以用簡單的加/減運算（而非查頁表）在實體位址與該段虛擬位址之間轉換（對應 `__va()`/`__pa()` 巨集），以加速核心直接存取實體記憶體。

原始碼定義（`arch/arm64/include/asm/memory.h`）：

```c
#define _PAGE_OFFSET(va)    (-(UL(1) << (va)))
#define PAGE_OFFSET         (_PAGE_OFFSET(VA_BITS))
```

即取虛擬位址空間「核心部分」的最低位址。本裝置 `VA_BITS=48` 時，`PAGE_OFFSET = 0xffff800000000000`，與書中 QEMU 範例（`VA_BITS=48` 時同為 `0xFFFF800000000000`）一致；實測 `_stext = ffff800008010000` 正好比 `PAGE_OFFSET` 大約 0x8010000，符合「核心映像位於線性映射區域附近偏移處」的佈局（詳見第 9 題）。

### 6. KIMAGE_VADDR 表示什麼意思？

`KIMAGE_VADDR` 表示**核心映像檔案（vmlinux）被映射到核心虛擬位址空間的起始虛擬位址**。書中所述的舊版本核心將其定義為一個獨立的固定常數（如 `0xFFFF000010000000`），但本裝置實際運行的 6.1 核心原始碼已經改為：

```c
#define KIMAGE_VADDR    (MODULES_END)
#define MODULES_END     (MODULES_VADDR + MODULES_VSIZE)
#define MODULES_VADDR   (_PAGE_END(VA_BITS_MIN))
```

即**核心映像映射區緊跟在 modules 區域（存放可載入核心模組程式碼的虛擬位址區間）之後**，兩者共同構成核心位址空間高位址端的一段 "非線性映射" 專用區域，與 `PAGE_OFFSET` 開始的實體記憶體線性映射區分開存放——這樣核心映像本身的載入位置與實體記憶體大小、線性映射區大小解耦，也便於支援 KASLR（核心位址空間佈局隨機化）獨立隨機化核心映像與線性映射的相對位置。這是新舊核心版本在同一概念上具體實現演進的一個典型例子。

### 7. TEXT_OFFSET 表示什麼意思？

在書中所述的早期核心版本裡，`TEXT_OFFSET` 表示**核心映像程式碼段（.text）相對於 `KIMAGE_VADDR`（及對應載入實體基址）的位元組偏移量**，連結腳本 `vmlinux.lds.S` 中 `. = KIMAGE_VADDR + TEXT_OFFSET;` 決定了 `.text` 段真正的起始虛擬位址；預設值為 `0x00080000`（512KB），也可透過 `CONFIG_ARM64_RANDOMIZE_TEXT_OFFSET` 讓 bootloader/核心在這個偏移上做隨機化，彌補當時核心本身還不支援隨機化基址（KASLR）的空缺，同時相容不同 bootloader 對映像頭部的預留空間要求。

實測本儲存庫（6.1 核心）中已經**不存在 `TEXT_OFFSET` 這個巨集/Makefile 變數**（`grep -rn "TEXT_OFFSET" arch/arm64/Makefile arch/arm64/kernel/vmlinux.lds.S` 無比對），這是因為自 Linux 4.6 起（commit "arm64: kernel: Update the kernel image..." 系列）核心映像本身已支援在任意 2MB 對齊位址載入並做重定位/KASLR，不再需要 bootloader 預留固定的 `TEXT_OFFSET` 頭部空間，`.text` 現在直接從映像的起始處（偏移 0）開始連結。這體現了 ARM64 Linux 啟動機制隨版本演進而簡化的過程——回答此題時應說明書本描述的是歷史機制，並指出目前核心已經移除該概念。

### 8. 核心映像檔案包含哪些段？這些段的作用是什麼？在 System.map 檔案中它們分別使用哪些符號來表示段的開始和結束？

主要段及作用：

| 段 | 作用 | System.map / kallsyms 起止符號 |
|---|---|---|
| `.text` | 核心程式碼段（可執行指令） | `_text` ~ `_etext` |
| `.rodata` | 唯讀資料（字串常數、`const` 資料、異常表等） | `__start_rodata` ~ `__end_rodata` |
| `.data` | 已初始化的可讀寫全域/靜態資料 | `_sdata` ~ `_edata` |
| `.bss` | 未初始化（預設歸零）的全域/靜態資料 | `__bss_start` ~ `__bss_stop` |
| `__init` | 僅初始化階段使用、啟動完成後即可釋放的程式碼/資料（如 `__init` 函數、`initcall` 表） | `__init_begin` ~ `__init_end` |
| `_end` | 整個核心映像的最終結束位址 | `_end` |

實測本裝置 `/proc/kallsyms`（root 權限讀取）：
```
ffff800008010000 T _stext
ffff800009070000 D _etext
ffff800009730000 T __init_begin
```
與 dmesg 啟動日誌中的段大小統計一致：
```
Memory: 7840408K/8386560K available (16768K kernel code, 3796K rwdata,
6856K rodata, 7296K init, 769K bss, 284008K reserved, 262144K cma-reserved)
```
即核心程式碼 16768K、可讀寫資料 3796K、唯讀資料 6856K、init 段 7296K、bss 769K，啟動流程 (`free_initmem`) 結束時會把 `__init_begin~__init_end` 區間整體釋放歸還給夥伴系統（dmesg 中 `Freeing unused kernel memory: 7296K` 正對應此操作）。

### 9. 請畫出 ARM64 Linux 核心的記憶體佈局。

以本裝置實際設定（4KB 頁、4 級頁表、`VA_BITS=48`）為例，結合核心文件 `Documentation/arm64/memory.rst`：

```
0x0000000000000000 +----------------------------+
                    |         使用者空間 (TTBR0)     |  256TB
                    |   0 ~ 0x0000ffffffffffff    |
0x0000ffffffffffff +----------------------------+
        ...                (未使用的位址空洞，VA[63:48] 需一致)                
0xffff000000000000 +----------------------------+ <- 核心空間起點
                    |  modules 區 (MODULES_VADDR) |
                    |----------------------------|
                    | 核心映像映射區 KIMAGE_VADDR   |  <-- KIMAGE_VADDR = MODULES_END
                    |   [_stext .. _end]           |
0xffff800000000000 +----------------------------+ <- PAGE_OFFSET
                    |   實體記憶體線性映射區           |
                    |   (__va(pa) = pa + 常數偏移)  |
                    |----------------------------|
                    |   vmalloc 區域                |
                    |----------------------------|
                    |   VMEMMAP（struct page 陣列） |
                    |----------------------------|
                    |   PCI I/O 空間 / fixmap       |
0xffffffffffffffff +----------------------------+
```

（各子區域的精確邊界由 `MODULES_VADDR/END`、`PAGE_OFFSET`、`VMEMMAP_START/END`、`PCI_IO_START/END`、`FIXADDR_TOP` 等巨集在 `arch/arm64/include/asm/memory.h` 中依據 `VA_BITS` 計算得出，隨核心版本可能有細節差異；本機啟動日誌因未開啟 `CONFIG_DEBUG_VM`/ptdump 而不再列印詳細分區表，可用 `zcat /proc/config.gz` 結合原始碼巨集計算精確邊界。）

### 10. __pa_symbol() 巨集和 __pa() 巨集有什麼區別？

兩者都用於把**核心虛擬位址轉換為實體位址**，但適用的虛擬位址來源不同：

- **`__pa(x)`**：假定 `x` 是**線性映射區（`PAGE_OFFSET` 之後）**內的虛擬位址，轉換公式基於 `PAGE_OFFSET`/`PHYS_OFFSET` 的固定偏移，即 `pa = (va & ~PAGE_OFFSET) + PHYS_OFFSET`，適用於透過 `__va()`/`page_address()` 等獲得的、指向 "實體記憶體直接映射區" 的位址；
- **`__pa_symbol(x)`**：假定 `x` 是**核心映像本身的符號位址**（即 `KIMAGE_VADDR + ...` 區域，如各種全域變數、函數指標取位址得到的 `&some_kernel_symbol`），這類位址位於核心映像映射區而非線性映射區，不能直接套用 `PAGE_OFFSET` 公式，而要透過 `kimage_voffset`（核心映像虛擬位址與實體位址之間的固定偏移，運行時計算得出）做減法：`pa = addr - kimage_voffset`。

原始碼（`arch/arm64/include/asm/memory.h`）：
```c
#define __pa_symbol(x)          __phys_addr_symbol(RELOC_HIDE((phys_addr_t)(x), 0))
#define __pa_symbol_nodebug(x)  __kimg_to_phys((phys_addr_t)(x))
#define __kimg_to_phys(addr)    ((addr) - kimage_voffset)
```
若把本該用 `__pa_symbol()` 的映像符號位址誤用 `__pa()` 計算（或反之），在核心映像與線性映射區偏移不同（這是常態，二者是兩個獨立映射）時會得到錯誤的實體位址，因此核心對這兩類位址來源做了嚴格區分。

### 11. 在實體記憶體還沒有線性映射到核心空間時，核心映像檔案映射到什麼地方？

在核心啟動的最早期（組合語言階段 `head.S` 執行、尚未建立面向 `PAGE_OFFSET` 的完整線性映射之前），核心會先建立一個**小範圍、專門針對核心映像自身的映射**：把核心映像透過**區塊映射（block mapping，用 L1/L2 區塊描述符，2MB 粒度）**方式映射到 `KIMAGE_VADDR + TEXT_OFFSET`（書中所述版本）起始的虛擬位址上，映射範圍通常隻涵蓋核心映像大小本身（幾 MB），並不涵蓋全部實體記憶體。此時該映射區的虛擬位址與實體位址之間的固定差值就被記錄到全域變數 **`kimage_voffset`** 中（`arch/arm64/kernel/head.S`：`str_l x4, kimage_voffset, x5`），供尚未建立完整線性映射前的階段使用 `__pa_symbol()` 轉換位址。直到後續 `paging_init()`/`map_mem()` 建立起從 `PAGE_OFFSET` 開始涵蓋全部實體記憶體的線性映射後，`__pa()`/`__va()` 才可用於線性映射區位址的轉換。

### 12. 在 ARM Linux 核心中，kimage_voffset 代表什麼意思呢？

`kimage_voffset` 是一個核心啟動早期就計算並儲存的**全域變數**，代表「核心映像的虛擬位址」與「核心映像的實體位址」之間的固定偏移量，即：

```
kimage_voffset = 核心映像的虛擬位址 - 核心映像的實體位址
```

它的作用類似於線性映射區的 `PAGE_OFFSET`——隻不過 `PAGE_OFFSET` 針對的是「全部實體記憶體的線性映射」，而 `kimage_voffset` 針對的是「核心映像自身這一小段特殊映射」（見第 11 題）。凡是要把**核心映像內部符號**的虛擬位址換算為實體位址（如 `__pa_symbol()`），都透過 `addr - kimage_voffset` 這個簡單減法完成，而不必查頁表，從而在核心啟動的很早階段（頁表還未完全建立、也沒有建立通用線性映射時）就能高效地做位址轉換。原始碼定義：`arch/arm64/mm/mmu.c: u64 kimage_voffset __ro_after_init;`，並在 `head.S` 中於建立恆等映射/核心映像映射之後立即計算寫入。

### 13. 在 ARMv8 架構中，快取管理的 PoC 和 PoU 有什麼區別？

- **PoC（Point of Coherency，一致性點）**：站在**整個系統**的角度看 Cache 一致性——系統中所有能夠存取記憶體的代理（不僅是 CPU 核心，還包括 GPU、DMA 控制器等）都能觀察到同一份最新資料的點。通常這個觀察點就是**主記憶體（DDR）本身**，因為像 GPU 這類不一定在 CPU 的 Cache 一致性域（Cache coherent interconnect）內的代理，唯一能確保雙方都看到同一份資料的地方就是主記憶體。
- **PoU（Point of Unification，統一點）**：站在**單個處理器核心**的角度看——該核心的指令快取（I-Cache）、資料快取（D-Cache）、TLB 與其「下一級共享的、能同時被 I 端和 D 端存取到」的儲存層級之間達成一致的觀察點，通常是該核心的 **L2 Cache**（如果 I-Cache/D-Cache 都能看到同一份 L2 資料即視為已統一）。

一句話區分：**PoC 是全系統範圍的一致性觀察點（通常落在主記憶體），PoU 是單核內部 I/D 路徑的一致性觀察點（通常落在該核的 L2）**。兩者決定了 Cache 維護指令（如 `DC CIVAC`、`IC IVAU`）需要清理/無效化到多深的層級：例如載入核心模組後 patch 程式碼需要 `flush_icache_range()`，其內部按 PoU 清理 D-Cache（讓即將被取指的新程式碼從 L2 可見）、按 PoU 使 I-Cache 無效化，而涉及跨 CPU 一致性域存取裝置記憶體（如 DMA 緩衝區）則要清理到 PoC。

### 14. 在 ARMv8 架構中，ASID 是什麼意思？有什麼作用？

**ASID（Address Space ID，位址空間識別碼）**是 ARMv8 為區分不同行程位址空間而設計的硬體標籤。TLB 項除快取「虛擬位址→實體位址」映射外，還攜帶產生該映射時所屬行程的 ASID（本裝置 TLB 項中 ASID 存放於 Bit[63:48]），查 TLB 命中的條件變成「虛擬位址比對 **且** ASID 比對」。

**作用**：解決第 1 章第 20 題所述的「行程頻繁切換導致 TLB 大量無效化」問題——不同行程的 TLB 項可以依靠不同的 ASID 共存於 TLB 中而不互相衝突/污染，行程切換（`switch_mm`）時只需要切換 TTBR0 與當前 ASID，無需 flush 整個 TLB，顯著減少切換後立即發生大量 TLB miss 的開銷。

Linux 在軟體層面為每個 `mm_struct` 維護一個軟體 ASID 計數（`mm->context.id` 的低若干位元），支援的硬體 ASID 位元寬度可為 **8 位元（256 個）或 16 位元（65536 個，需要 `ID_AA64MMFR0_EL1` 報告支援且軟體/`TCR_EL1.AS` 置位 `TCR_ASID16`）**；當 8/16 位元 ASID 空間用盡發生「迴轉（rollover）」時，核心會分配新一輪 ASID 版本號並對所有 CPU 做一次全域 TLB flush（`arch/arm64/mm/context.c` 中的 `asid_bits`、`ASID_FIRST_VERSION`、`NUM_USER_ASIDS` 等即實現此邏輯）。此外 ARMv8 虛擬化擴充中還有類似機制的 **VMID（Virtual Machine ID）**，用於區分不同虛擬機的 Stage-2 頁表 TLB 項。

### 15. 在 ARMv8 架構中支援哪幾種記憶體屬性？它們都有哪些特點？

ARMv8 透過 `MAIR_ELx`（Memory Attribute Indirection Register）配合頁表項中的 `AttrIndx` 欄位，將記憶體劃分為兩大類：

- **Normal Memory（一般記憶體）**：用於常規 RAM，特點是：
  - 允許指令/資料 Cache（可設定 Cacheable/Non-cacheable，及 Write-Back / Write-Through）；
  - 允許**亂序執行、預取、投機存取、位址合併/拆分（如把非對齊存取拆成多次匯流排傳輸）**；
  - 可進一步指定內部/外部（Inner/Outer）Cache 屬性與 Shareability（見第 16 題）；
  - 又細分為 **Normal-Cacheable** 與 **Normal Non-cacheable**。
- **Device Memory（裝置記憶體）**：用於 MMIO 暫存器等周邊位址空間，**不可 Cache**，並按嚴格程度進一步分為四種（`nGnRnE`/`nGnRE`/`nGRE`/`GRE`，G=Gathering 是否允許合併多次存取、R=Reordering 是否允許重排、E=Early Write Acknowledgement 是否允許提前應答寫完成）：
  - **Device-nGnRnE**（最嚴格）：不允許合併、不允許重排、不允許提前應答——即傳統「強序（Strongly-ordered）」記憶體，每次存取都必須嚴格按程式順序、獨立地完成，常用於對時序極敏感的暫存器；
  - **Device-nGnRE**：不合併、不重排，但允許提前應答寫完成；
  - **Device-nGRE**：不合併，允許重排與提前應答；
  - **Device-GRE**（最寬鬆）：允許合併、重排、提前應答，是裝置記憶體中限制最少的一種。

Linux 核心透過 `ioremap()` 系列介面（`ioremap()` 預設 nGnRE、`ioremap_wc()` 對應 Normal Non-cacheable 類似寫合併、`ioremap_np()` 對應更嚴格的 nGnRnE）來選用合適的屬性，MMIO 驅動程式碼需要根據周邊時序要求選擇正確的映射類型。

### 16. 在 ARMv8 架構中，快取共享屬性有內部共享（inner shareable）和外部共享（outer shareable），它們有什麼區別？

Shareability（共享屬性）描述的是「當一個處理器/代理修改某記憶體位置後，系統中哪個範圍內的其它代理需要透過 Cache 一致性協定自動看到這次修改」，分為幾個層級（由頁表項的 `SH[1:0]` 欄位設定）：

- **Non-shareable（不可共享）**：該記憶體位置隻被該處理器自身存取，不需要與任何其它代理保持一致（通常用於私有、未共享的記憶體，如某核獨佔使用的暫存緩衝區）；
- **Inner Shareable（內部共享域）**：一致性範圍涵蓋「內部共享域」內的代理，典型對應**同一 Cache 一致性互連（如 CCI/CCN/DSU）連接的所有 CPU 核心**——即晶片上的多核 CPU 叢集會自動透過硬體監聽協定保持一致，軟體無需手動 flush；
- **Outer Shareable（外部共享域）**：一致性範圍進一步擴大到「外部共享域」，通常涵蓋 **CPU 叢集之外、但仍在系統級一致性範圍內的代理**，如系統中的 GPU、其它 Socket 上的 CPU（多路伺服器場景）等，只要它們也被納入同一 Outer Shareable 域，也能自動獲得一致性。

簡言之：**Inner Shareable 是「CPU 叢集內部自動保持一致」的範圍，Outer Shareable 是把這個一致性範圍擴展到叢集之外更多種類/更大規模的代理**——外部共享域包含（是超集於）內部共享域。具體劃分是 SoC 設計者根據互連拓樸（哪些代理接入了硬體一致性互連）來定義的實現細節。記憶體屏障指令的 `ish`/`osh`/`sy` 後綴（如 `dmb ish`）正是用於精確指定屏障需要同步到哪一個共享域，域越小屏障開銷越低。

### 17. 在 ARMv8 架構中，支援哪幾條記憶體屏障指令？它們都有什麼區別？

同第 1 章第 5 題：ARMv8（A64 指令集）提供 **DMB、DSB、ISB** 三條基礎記憶體屏障指令：

- **DMB（資料儲存屏障）**：保證屏障前後的記憶體存取相對順序，但不要求屏障前的存取已經「完成」（資料到達終點），也不影響非記憶體指令；
- **DSB（資料同步屏障）**：更強，要求屏障之前的所有記憶體存取都**已經真正完成**才允許執行屏障之後的**任何**指令（不只是記憶體指令）；
- **ISB（指令同步屏障）**：清空管線，保證之後取指得到的都是本條指令之前所有系統狀態變更（如頁表、系統暫存器、Cache/TLB 維護操作）生效後的指令流。

三者都可以附加**作用域後綴**（`OSH`=外部共享域、`ISH`=內部共享域、`NSH`=不可共享域、`SY`=全系統，預設）以及**方向後綴**（`ST`=只針對 Store、`LD`=只針對 Load，DMB/DSB 支援，ISB 不支援），如 `dmb ishst`（僅針對內部共享域內的 Store 操作定序）比全量 `dmb sy` 開銷更低，Linux 核心會依據具體同步需求（如自旋鎖 vs. 跨核 IPI vs. 修改頁表）選用不同強度/範圍的屏障變體（對應 `arch/arm64/include/asm/barrier.h` 中的 `dmb()`、`dsb()`、`isb()` 巨集封裝）。

### 18. 載入-獲取屏障原語與儲存-釋放屏障原語有什麼區別？分別有什麼作用？

這是 ARMv8 引入的**單指令級記憶體屏障**（相比 DMB/DSB 是獨立的屏障指令，Load-Acquire/Store-Release 是**內建在具體的 Load/Store 指令語意中**），用於高效實現臨界區/無鎖資料結構的同步：

- **載入-獲取（Load-Acquire，如 `LDAR`）**：該 Load 指令保證——**在程式順序上位於它之後的所有記憶體存取，都不能被重排到這條 Load-Acquire 之前執行**（即它對後面的存取設定了一個「下界」，像獲取鎖一樣，獲取之後的操作不能「提前」到獲取鎖之前發生）；
- **儲存-釋放（Store-Release，如 `STLR`）**：該 Store 指令保證——**在程式順序上位於它之前的所有記憶體存取，都必須在這條 Store-Release 完成之前完成/可見**（即它對前面的存取設定了一個「上界」，像釋放鎖一樣，釋放之前的所有操作必須先完成，才能讓「釋放」這個訊號對外可見）。

兩者常常配對使用來實現**無需完整 DMB 的輕量級同步**：例如自旋鎖的 `unlock` 用 Store-Release 寫標誌位元（保證臨界區內所有存取都先於解鎖可見），而其它 CPU 用 Load-Acquire 讀該標誌位元判斷鎖是否可用（保證讀到「鎖已釋放」之後才能開始執行臨界區程式碼），從而只需一對 Acquire/Release 指令即可正確同步，而不必像 DMB 那樣對屏障兩側所有存取做全域排序，開銷更低、粒度更細，也是 C11/C++11 `memory_order_acquire`/`memory_order_release` 語意在 ARM64 上的天然硬體映射（Linux 原子操作 `arch/arm64/include/asm/atomic_lse.h` 與 `smp_load_acquire()`/`smp_store_release()` 巨集即基於此實現）。

### 19. 什麼是一個段的載入位址和運行位址？

- **載入位址（Load Address，LMA, Load Memory Address）**：該程式碼/資料段**實際被存放（燒錄/複製）到的實體儲存位置**——比如 bootloader 把核心映像從 Flash/eMMC/網路載入到 DRAM 中的某個實體位址，這個「被放到哪裡」的位址就是載入位址。
- **運行位址（運行時位址 / VMA, Virtual Memory Address，連結位址）**：該程式碼在**運行時被 CPU 實際存取、按照程式碼裡的絕對跳轉/取數位址所期望**的（虛擬或實體）位址——即連結器（`ld`）在連結腳本中為這段程式碼設定的位址，程式碼內部的分支目標、全域變數參照等都是按這個位址編譯生成的。

兩者**通常一致**（直接在最終位址執行），但在很多嵌入式/核心啟動場景中**不一致**：例如核心映像被 bootloader 載入到某個實體位址（載入位址），但核心程式碼本身是按照 `KIMAGE_VADDR`（虛擬位址，運行位址）連結的；類似地，很多 bootloader 自身也是先被載入到一處實體位址執行一段「位置無關程式碼」，再重定位/複製自身到最終運行位址。當兩者不一致時，必須先建立好從「當前執行位置」到「運行位址」的位址轉換（如 ARM64 head.S 中的恆等映射，見第 21 題），或者程式碼本身寫成位置無關程式碼（PIC/PIE），才能正確完成從載入位址到運行位址的跳轉/切換。

### 20. 從 U-Boot 跳轉到核心時，為什麼指令快取可以開啟而資料快取必須關閉？

- **指令 Cache 可以開啟**：核心映像的程式碼（.text）在被載入完成、且執行入口之前是**靜態不變的唯讀資料**，U-Boot 與核心對同一段實體指令內容的理解是一致的，即使開啟 I-Cache 提前快取了部分指令，也不會因為「誰在什麼時候寫了什麼」而產生歧義/不一致問題（不涉及寫操作、也不存在多個執行者對同一位址產生不同資料版本的風險）；開啟 I-Cache 能加速核心入口這段程式碼本身的取指執行，沒有正確性風險。

- **資料 Cache 必須關閉**：因為此時 **MMU 通常還未啟用**（或者即將建立/切換頁表），如果開啟 D-Cache，會出現兩個層面的風險：
  1. 核心啟動早期需要對**頁表、`memstart_addr`、`kimage_voffset` 等關鍵資料結構**做寫入並被後續程式碼（可能運行在另一個核心，或 MMU 啟用之後用不同位址存取）正確看到，若 D-Cache 開啟且這些寫入停留在 Cache 中未及時寫回，一旦發生 Cache 無效化/被其它路徑以非 Cache 一致的方式（如 MMU 關閉時對同一實體位址的直接存取，或其它尚未參與一致性域的核心）讀取記憶體，就會讀到過期資料；
  2. 在 MMU 關閉階段，D-Cache 若是 VIPT/PIPT 但當前又沒有有效的位址轉換語境，容易與「MMU 啟用後、以虛擬位址正常存取同一實體記憶體」產生的 Cache 狀態發生衝突（比如同一實體位址在 MMU 開關前後被不同「虛擬尋址上下文」下的 Cache 行記錄，產生別名問題，見第 1 章第 9~12 題）；
  3. 多核啟動（Secondary CPU 透過 `spin-table`/PSCI 等待、被喚醒後跳轉執行）場景下，各核之間在 MMU/Cache 尚未統一建立前，如果各自的 D-Cache 快取了不一致的資料且未強制同步，會導致核間資料不一致。

因此 ARM64 啟動約定（Booting AArch64 Linux Kernel 文件）明確要求：跳轉核心入口時 **MMU 關閉、D-Cache 關閉（或已妥善 clean/invalidate）、I-Cache 狀態不限（可開可關，架構上是安全的）**，只有等核心自身在 `head.S`/`__enable_mmu` 中正確建立好頁表、開啟 MMU 之後，才會顯式開啟 D-Cache。

### 21. 在 Linux 核心啟動組合語言程式碼中，為甚麼要建立恆等映射？

**恆等映射（identity mapping）**指虛擬位址等於實體位址的映射（`VA == PA`）。原因在於：核心啟動組合語言程式碼執行「開啟 MMU」這個動作本身是有**因果時序問題**的——在 MMU 開啟之前，處理器正在以實體位址直接取指執行（PC 就是實體位址）；一旦執行了開啟 MMU 的那條指令，從下一條指令起，處理器立刻開始把 PC 當成**虛擬位址**去查頁表轉換。如果核心程式碼本身是按照最終的運行虛擬位址（`KIMAGE_VADDR` 等，通常與當前實體執行位址不同）來連結的，那麼「開啟 MMU 那條指令」和「緊隨其後的下一條指令」之間就會出現位址不連續的斷裂：CPU 還在按實體位址執行到開啟 MMU 那一條，下一條卻要按照一個尚未生效、或者生效了但還沒跳轉過去的虛擬位址取指，會導致取指錯亂/異常。

解決辦法是：在建立「核心真正想用的映射」（核心映像映射到高位址虛擬空間、線性映射涵蓋全部實體記憶體）**之外**，額外建立一個小範圍的**恆等映射**——把「當前正在執行、開啟 MMU 那幾條指令所在的實體位址」也映射為**相同數值的虛擬位址**（即 `VA == PA`）。這樣開啟 MMU 前後，緊鄰開啟 MMU 指令的那一小段程式碼（通常只是 `head.S` 裡緊挨著寫 `SCTLR_ELx.M` 那幾條指令）無論按實體位址還是按虛擬位址解釋，取到的都是同一份指令/同一個位址，執行流不會中斷；隨後程式碼再顯式跳轉（長跳轉 `br`/絕對位址跳轉）到核心映像真正的高位址虛擬空間，切換到最終的運行位址繼續執行，之後才不再需要這段恆等映射（可以在後續被丟棄/覆蓋）。

### 22. 在 ARMv8 架構中，在 L0～L2 頁表項中包含了指向下一級頁表的基位址，那麼這個下一級頁表基位址是實體位址還是虛擬位址？

**實體位址**。這與第 1 章第 27 題的道理完全一致：MMU 硬體頁表巡覽器（Table Walk Unit）本身的職責就是完成「虛擬位址 → 實體位址」的轉換，若頁表項中存的是下一級頁表的虛擬位址，就需要「先轉換位址才能拿到轉換位址所需的輸入」，形成邏輯死循環。因此 TTBR0/TTBR1 存放頂層（L0）頁表的實體基位址，L0~L2 每一級的**表描述符（Table descriptor）**中，指向下一級頁表的欄位（bit[47:12] 左右，取決於具體位址寬度）存的也都是**實體位址**，硬體全程用實體位址直接存取記憶體完成整個 4 級頁表的巡覽，只有最終 L3 葉子頁表項中的「實體頁頁幀號」才是本次轉換要求的最終答案，與頁內偏移拼接後得到目標資料的實體位址。

### 23. MMU 可以巡覽頁表，Linux 核心也提供了軟體巡覽頁表的函數，如 walk_pgd()、__create_pgd_mapping()、follow_page() 等。從軟體的視角，Linux 核心的 pgd_t、pud_t、pmd_t 以及 pte_t 資料結構中並沒有儲存一個指向下一級頁表的指標（即從 CPU 角度來看，CPU 存取這些資料結構時是以虛擬位址來存取的），它們是如何巡覽的呢？pgd_t、pud_t、pmd_t 以及 pte_t 資料結構是 u64 類型的變數。

關鍵在於：這些資料結構（`pgd_t`/`pud_t`/`pmd_t`/`pte_t`，本儲存庫 `arch/arm64/include/asm/pgtable-types.h` 定義均為單個 `u64` 值的包裝結構，如 `typedef struct { pteval_t pte; } pte_t;`）裡存的數值確實是（硬體要求的）**實體位址**（即下一級頁表的實體基位址，對應第 22 題），但**核心軟體本身要去存取「下一級頁表」這塊記憶體、讀取其中內容時，走的是核心自己的虛擬位址存取路徑**（因為 CPU 執行核心程式碼時，MMU 已經開啟，所有存取指令的位址都要經過 CPU 當前的位址轉換）——軟體巡覽函數並不是「直接把頁表項里的實體位址塞給 CPU 存取」，而是先把該實體位址透過 **`__va()`（基於 `PAGE_OFFSET` 線性映射的固定偏移，見第 5 題）轉換為對應的核心虛擬位址**，再用這個虛擬位址去解參考（dereference）、讀取下一級頁表的內容。

具體到函數命名與實現（如 `pgd_offset()`、`pud_offset()`、`pmd_offset()`、`pte_offset_kernel()`）：
1. `pgd_offset(mm, addr)` 回傳 `mm->pgd`（這本身已經是一個核心虛擬位址指標，因為 `pgd` 頁在分配時就記錄了其虛擬位址）加上由 `addr` 計算出的索引，從而**直接以虛擬位址**解參考得到 `pgd_t` 的值（該值內部存的是實體位址）；
2. 要往下一級走時，`pud_offset(pgd, addr)` 內部會先用 `pgd_val(*pgd)`（或 `pgd_page_paddr()`）取出該 `pgd_t` 儲存的實體位址，再呼叫 `phys_to_virt()`/`__va()` 把它轉換為核心虛擬位址，才能對這個虛擬位址做指標運算、加上 `pud` 索引，進而解參考讀出 `pud_t` 的內容；
3. 如此逐級重複：每次都是「讀取當前級頁表項的值（實體位址）→ `__va()` 換算成虛擬位址 → 用虛擬位址存取，取得下一級頁表項的內容」，直至到達葉子 `pte_t`。

因此結論是：**MMU 硬體頁表巡覽始終使用實體位址，而 Linux 軟體頁表巡覽函數在每一步「讀取頁表項內容」時用的是虛擬位址（當前行程/核心正常的存取路徑），但在「從上一級頁表項的值計算下一級頁表的存取位址」這一步，都要先做一次 `實體位址(頁表項裡存的值) → 虛擬位址(透過 __va()/phys_to_virt())` 的轉換**，這正是軟體巡覽與硬體巡覽「看起來都在做同一件事，但工作在不同位址視角」的關鍵銜接點。
