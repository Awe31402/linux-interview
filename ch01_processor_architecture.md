# 《奔跑吧 Linux內核》（第二版）卷1 第1章 處理器架構 高頻面試題

> **說明**：收錄自第 1 章開篇「本章的高頻面試題」，共計 34 題。
> 每題答案除書本理論外，均以實驗設備 **Radxa ROCK 5B（RK3588，8× Cortex-A55/A76，SSH: radxa@192.168.68.57）** 上取得的實測數據佐證。

---

### 1. 请简述精简指令集RISC和复杂指令集CISC的区别。

| | CISC（如 x86） | RISC（如 ARM） |
|---|---|---|
| 指令集 | 指令数量多、寻址方式多、单条指令可完成复杂操作（如内存操作数直接参与运算） | 指令数量少、格式规整，多为定长指令 |
| 执行方式 | 微码（microcode）翻译执行，单指令可能耗费多个周期 | 大部分指令一个时钟周期内完成，易于流水线化 |
| 访存 | 运算指令可直接访问内存（register-memory / memory-memory） | 采用 Load/Store 架构，只有 Load/Store 指令可以访问内存，运算指令只操作寄存器 |
| 硬件复杂度 | 译码/控制逻辑复杂，晶体管更多用于指令翻译 | 硬件简单，更多晶体管可用于流水线、乱序执行、Cache |
| 编译器角色 | 编译器只需选用高级指令 | 编译器需要做更多指令调度、寄存器分配等优化工作 |

实测设备的 CPU 均为 ARM64（AArch64）指令集，`cat /proc/cpuinfo` 中 `CPU architecture: 8` 即 ARMv8-A，是典型 RISC 设计：Load/Store 架构、32 个通用寄存器、定长 32 位指令（Thumb 除外）。

### 2. 请简述数值0x1234 5678在大小端字节序处理器的存储器中的存储方式。

设起始地址为 `0x1000`：

- **大端（Big-Endian）**：高位字节存于低地址。`0x1000:0x12 0x1001:0x34 0x1002:0x56 0x1003:0x78`
- **小端（Little-Endian）**：低位字节存于低地址。`0x1000:0x78 0x1001:0x56 0x1002:0x34 0x1003:0x12`

ARMv8 上电复位后默认按小端方式访问（也可通过 `SCTLR_ELx.EE` 配置大端），Linux 内核也可编译为 `CONFIG_CPU_BIG_ENDIAN`，但绝大多数发行版（含本设备）都使用小端。实测：

```
$ lscpu | grep "Byte Order"
Byte Order: Little Endian
```

### 3. 请简述在你所熟悉的处理器（如双核Cortex-A9）中一条存储读写指令的 执行全过程。

以 store 指令 `str r0, [r1]` 为例（多核，含 Cache 一致性）：

1. **取指/译码**：CPU 流水线取出并译码 store 指令，计算出有效地址（EA）。
2. **TLB 查找**：用虚拟地址查一级 TLB（micro-TLB），命中则得到物理地址与访问属性（Cacheable/Shareable 等）；未命中则触发页表walk（硬件 Page Table Walker 逐级查询 L0~L3 页表），并将结果填入 TLB。
3. **访问一级 Cache（VIPT）**：用虚拟地址的 index 位查 Cache set，用物理地址 tag 比较是否命中。
4. **一致性处理（MESI/MOESI）**：若是共享的 Cache 行，要先通过总线监听协议（如 CCI-400 上的 ACE 协议）确认其他核该行状态，将其失效或降级，本核获得 Exclusive/Modified 权限后才能写。
5. **写入 Cache 行**：命中则更新该 Cache line 数据，若采用 write-back 策略，只标记该行为 Dirty，不立即写回主存；未命中则先分配（write-allocate）一行，从下一级 Cache/主存搬入整行数据再写入（或以 write-around 策略绕过 Cache 直写缓冲）。
6. **写缓冲/内存屏障语义**：数据先进入 Store Buffer/Write Buffer，异步写回，除非遇到 DMB/DSB 等屏障指令或该地址为 Device 内存，才强制排空写缓冲。
7. **退休（Retire）**：指令在重排序缓冲（ROB）中按序退休，异常处理精确对齐这一步。

### 4. 请简述内存屏障（memory barrier）产生的原因。

现代处理器为了提升性能，会对访存指令做**乱序执行**、**写缓冲（Store Buffer）**、**Cache 的非阻塞访问**等优化，使得程序中指令实际到达内存系统的顺序可能与程序顺序（program order）不一致；此外编译器也会为了优化重新排列指令。在单核单线程环境下这通常无害（处理器保证自身看到的结果符合顺序一致性假象），但在多核系统中，一个核对内存的写入顺序，另一个核可能以不同顺序观察到，导致依赖数据同步的算法（如自旋锁、生产者-消费者的 flag/data 模式）出错。因此需要显式的内存屏障指令，强制规定屏障前后的访存操作的完成/可见顺序，弥合"处理器实际执行顺序"与"程序员期望的顺序"之间的差异。

### 5. ARM有几条内存屏障指令？它们之间有什么区别？

ARMv8（A64）提供 **3 条**内存屏障指令：

- **DMB（Data Memory Barrier，数据存储屏障）**：保证 DMB 之前的所有存储器访问（读/写）都先于 DMB 之后的存储器访问完成（对同一 Shareability domain 可见），但不影响非存储器指令的顺序，也不保证屏障前指令本身已经"完成"（如写缓冲可以仍未排空，只是顺序被保证）。
- **DSB（Data Synchronization Barrier，数据同步屏障）**：更强，要求 DSB 之前的所有存储器访问都**真正完成**（写操作数据已到达其目标、读操作数据已返回）之后，才允许执行 DSB 之后的任何指令（包括非存储器指令）。
- **ISB（Instruction Synchronization Barrier，指令同步屏障）**：清空流水线（flush pipeline），保证 ISB 之后取指得到的都是 ISB 之前所有对系统寄存器/页表/Cache 等的修改**生效之后**的指令流，常用于修改 MMU/Cache 配置、写 TTBR 或 ASID 之后。

三者还可加 `ISH/OSH/NSH/SY`、`ST/LD` 后缀细分作用域（Shareability domain）和方向（只针对 store 或 load），如 `dmb ish`（内部共享域）、`dsb sy`（全系统）。

### 6. 请简述高速缓存（cache）的工作方式。

Cache 利用**局部性原理**（时间局部性、空间局部性），在 CPU 与主存之间插入一块由 SRAM 构成的高速小容量存储器。数据以固定大小的**Cache 行（line，本设备为 64 字节）**为单位在 Cache 与主存之间搬移。CPU 访存时先查 Cache：

- **命中（hit）**：直接在 Cache 中完成读/写，速度远快于访问主存；
- **缺失（miss）**：需要从下一级 Cache 或主存加载整个 Cache 行到 Cache 中（可能需要先淘汰一个旧行，若被淘汰行为脏（dirty）则要先写回主存），再完成本次访问。

Cache 采用组相联映射，将地址划分为 tag/index/offset 三部分查找对应的行；写策略上有 write-through/write-back，替换策略上多用 LRU 近似算法。实测本机三级 Cache 结构：L1（每核私有）、L2（每核/簇私有）、L3（全核共享）。

### 7. 高速缓存的映射方式有全关联（full-associative）、直接映射（directmapping）和组相联（set-associative）3种方式，请简述它们之间的区别。为什么 现代的处理器都使用组相联的高速缓存映射方式？

- **直接映射（direct-mapping）**：每个主存块只能放到唯一固定的一个 Cache 行（`index = 地址 mod 行数`）。硬件简单、查找快，但当多个经常访问的地址恰好映射到同一行时会频繁发生**Cache 抖动/冲突缺失（conflict miss）**，命中率低。
- **全相联（full-associative）**：任何主存块可放到任意一个 Cache 行，需要用地址的 tag 与**所有**行的 tag 同时比较（CAM，内容寻址存储器）。命中率最高，但比较器数量随容量线性增长，硬件代价与功耗过大，只适合极小容量（如 TLB 的部分结构）。
- **组相联（set-associative）**：将 Cache 分成若干组（set），每组内有 N 路（way），地址的 index 位决定落入哪一组，组内 N 路全相联比较 tag。是直接映射（1 路）与全相联（1 组）的折中。

现代处理器普遍采用组相联（如本设备 L1 4-way、L2 4~8-way、L3 12-way），是因为它用**可控的硬件代价（每组只需比较 N 路而非全部）**换取了**接近全相联的命中率**，在功耗、面积、时延与命中率之间取得最佳平衡点；同时组相联也便于用软件/硬件的 Cache 着色（cache coloring）、路预测等技术进一步优化。

### 8. 在一个32KB的4路组相联的高速缓存中，其中高速缓存行为32字节，请 画出这个高速缓存的高速缓存行（line）、路（way）和组（set）的示意图。

计算：总容量 32KB，4 路，行大小 32B → 每路容量 = 32KB / 4 = 8KB；组数 = 8KB / 32B = 256 组（set）。

```
                    Way0        Way1        Way2        Way3
Set 0   [Tag|32B line] [Tag|32B line] [Tag|32B line] [Tag|32B line]
Set 1   [Tag|32B line] [Tag|32B line] [Tag|32B line] [Tag|32B line]
  ...          ...            ...           ...            ...
Set 255 [Tag|32B line] [Tag|32B line] [Tag|32B line] [Tag|32B line]
```

地址划分（假设物理地址）：`offset` 占 5 位（2^5=32B），`index` 占 8 位（2^8=256 组），其余高位为 `tag`：

```
| ... tag (高位) ... | index[12:5] (8bit) | offset[4:0] (5bit) |
```

对照本设备实测的 A55 L2（`size=128K ways=4`）：每路 32KB，行 64B → 组数 = 32KB/64B = 512 组，index 占 9 位、offset 占 6 位，原理相同，只是参数不同。

### 9. 高速缓存重名问题和同名问题是什么？

这两个问题都源于使用**虚拟地址**的部分位来做 Cache 的 index/tag，而虚拟地址与物理地址之间是多对一或一对多的映射：

- **别名/重名问题（Cache aliasing，又称同义 synonym）**：**多个不同的虚拟地址映射到同一个物理地址**（如共享内存、mmap 同一文件），若这些虚拟地址的 index 位不同，会在 Cache 中产生该同一份物理数据的**多个副本**，分处不同的 Cache 行；一旦其中一份被修改而未同步，就会造成数据不一致（多进程看到不同内容）。
- **同名问题（也称歧义 homonym）**：**同一个虚拟地址在不同进程（不同页表/地址空间）中，映射到不同的物理地址**。若 Cache 只用虚拟地址做 tag（如 VIVT），发生进程切换后，新进程可能命中了上一个进程遗留、tag 相同但内容完全不相关的 Cache 行，读到错误数据。

### 10. ARM9处理器的数据高速缓存组织方式使用虚拟索引虚拟标签（Virtual Index Virtual Tag，VIVT）方式，而在Cortex-A7处理器中使用物理索引物理标签 （Physical Index Physical Tag，PIPT），请简述PIPT与VIVT相比的优势。

PIPT 的 index 和 tag 都使用物理地址，天然不存在别名（aliasing）和同名（homonym）问题：因为 Cache 中的一行始终只与唯一的物理地址对应，不同虚拟地址映射同一物理地址时会命中同一 Cache 行，进程切换也不会读到旧进程遗留的错误行。因此 PIPT 不需要软件在进程切换、mmap/munmap、fork 共享内存等场景下额外维护 Cache 一致性（无需软件按虚拟地址主动 flush/invalidate），大幅简化了操作系统（Linux）Cache 管理代码，正确性更好；代价是需要先做地址转换（TLB 查找）才能索引 Cache，通常需要将 TLB 访问置于 Cache 访问的关键路径上（VIPT 的一种是可以让 index 部分与 offset 一样在页内、不需要先转换地址，从而兼顾速度与 PIPT 的正确性）。

### 11. VIVT类型的高速缓存有什么缺点？请简述操作系统需要做什么事情来 克服这些缺点。

**缺点**：
1. 存在第 9 题所述的**别名（aliasing）**和**同名（homonym）**问题；
2. 进程切换、地址空间变化（如 `mmap`/`munmap`、写时复制、`exec`）后，Cache 中残留的旧虚拟地址数据可能不再有效或产生歧义。

**操作系统的应对**（以早期 ARM Linux 为例）：
- 进程切换（`switch_mm`）时，若新旧进程 ASID/地址空间不同且硬件无 ASID 支持，需要**flush 整个 Cache**（如 `flush_cache_mm`），代价高昂；
- 对共享映射（多个 VMA 映射同一文件/物理页）需要检测**虚拟地址是否满足"Cache 一致性同余"条件（即两个虚拟地址对 Cache 组大小取模相同，才允许共享而不冲突）**，Linux 的 `SHMLBA`、`flush_dcache_page()`、`flush_cache_range()` 等接口正是为此设计；
- 修改可执行页（如动态链接、`ptrace`、JIT）后需要显式 `flush_icache_range()`，保证指令 Cache 与数据一致；
- 因此内核充满了大量与 VIVT 平台相关的 Cache 维护回调（`cpu_cache.dcache_clean_area` 等），这也是 ARMv7 之后处理器逐渐转向 PIPT/VIPT（且限制 VIPT 的 index 落在页内）的原因——从根本上减少软件维护负担。

### 12. 虚拟索引物理标签（Virtual Index Physical Tag，VIPT）类型的高速缓存 在什么情况下会出现高速缓存重名问题？

VIPT 用虚拟地址查 index、物理地址比较 tag。若 **index 所用的位数超出了页内偏移（page offset）范围**，即 `index` 位跨越了虚拟地址到物理地址转换不变的"页内偏移"部分，那么两个映射到同一物理地址的不同虚拟地址（别名）就可能落在**不同的 index（不同的组）**，产生该物理数据同时存在于多个 Cache 组的重名（aliasing）问题——因为它们的物理 tag 相同、但位于不同的 set，Cache 硬件按 tag 比较时只在各自的 set 内查找，无法侦测到彼此，导致同一份数据出现多个不同步的副本。

判断条件：当 `Cache 总容量 / 路数（每路大小） > 页大小（4KB）` 时，index 位会超出页内偏移范围，就有别名风险；这也是为什么很多处理器把**每路（way）容量限制为不超过一个页大小**（如 4KB×N 路），使 VIPT 的 index 完全落在页内偏移中，从而在效果上等价于 PIPT，同时保留了"无需地址转换即可先行索引"的速度优势。

### 13. 请画出在二级页表架构中虚拟地址到物理地址查询页表的过程。

以典型 32 位、二级页表（如 ARMv7 short-descriptor 或经典 x86 二级页表）为例，虚拟地址被划分为 `PGD index | PTE index | Offset` 三段：

```
虚拟地址(32bit)
+----------------+----------------+------------------+
| PGD index(高位) |  PTE index     |  Page Offset(低位) |
+----------------+----------------+------------------+
        |                 |                  |
        v                 v                  |
   [TTBR 指向 PGD 基址]                       |
        |                                    |
        v                                    |
  PGD[index] --> 指向二级页表(PTE表)基址        |
                     |                        |
                     v                        |
              PTE[index] --> 页帧号(PFN，物理页基址)
                     |                        |
                     +----------+-------------+
                                v
                        物理地址 = PFN << 12 | Offset
```

流程：MMU 取虚拟地址高位段作为一级页表（PGD）的索引，从 TTBR 寄存器指向的 PGD 基址加上该索引查得一个页表项，该项指向二级页表（PTE 表）的基址；再用虚拟地址中间段作为二级索引查得 PTE，其中包含物理页帧号（PFN）及访问属性（可读写、可执行、Cache 属性等）；最后将 PFN 与虚拟地址最低位的页内偏移拼接，得到最终物理地址。若某一级页表项无效则触发缺页异常（page fault）。

（本设备的 ARM64 实际使用 4 级页表，48 位虚拟地址、4KB 页，详见第 2 章第 2 题。）

### 14. 在多核处理器中，高速缓存的一致性是如何实现的？请简述MESI协议 的含义。

多核下每个 CPU 都有私有 Cache（本设备每核私有 L1、A55 私有 L2/A76 私有 L2），当多个核缓存了同一物理地址的数据副本、其中一个核修改后，必须保证其它核不再使用过期数据——这就是**Cache 一致性（coherency）**问题。常见解决方式是**总线监听（snooping）协议**：所有核的 Cache 控制器都监听共享总线/互连（本设备为 CCI/AMBA ACE 互连）上的读写事务，根据这些事务动态调整自己 Cache 行的状态。

**MESI** 协议是最常用的一种监听协议，为每个 Cache 行维护 4 种状态（用 2 bit 编码）：

- **M（Modified，已修改）**：该行只存在于本 Cache 中，且与主存不一致（脏），本核可任意读写；
- **E（Exclusive，独占）**：该行只存在于本 Cache 中，但内容与主存一致（干净），可直接转为 M 而无需总线通知；
- **S（Shared，共享）**：该行可能同时存在于多个核的 Cache 中，内容与主存一致，只能读，写入前须先广播使其它副本失效；
- **I（Invalid，无效）**：该行数据无效，必须从主存/其它 Cache 重新加载。

当某核要写一个 S 或 I 状态的行时，会在总线上发出 `BusRdX`（或 Invalidate）信号，其它核收到后将各自副本置为 I；当某核要读一个在其它核为 M 状态的行时，拥有者需要先把数据写回（或直接转发），并将自身状态降为 S。ARM/x86 处理器广泛采用 MESI 或其变种（如 MOESI，增加 Owned 状态避免写回主存，本设备 CCI-500/DSU 互连即支持类似语义）来实现 Cache 一致性。

### 15. 高速缓存在Linux内核中有哪些应用？

- **DMA 一致性管理**：`dma_map/unmap_single()`、`dma_sync_*` 系列接口，在设备 DMA 访问前后主动 flush/invalidate Cache，保证 CPU 与外设看到一致数据（尤其在非 Cache 一致的 DMA 场景）；
- **可执行代码的自修改场景**：`flush_icache_range()`（模块加载、内核态代码patch、JIT、ptrace 写断点）保证 I-Cache 与 D-Cache 一致；
- **用户态/内核态映射同一物理页时的 flush_dcache_page()**：如文件页 cache 被内核写入后，用户态尚未映射的可执行文件需要同步；
- **写时复制（COW）与迁移页面**：`copy_user_highpage()`、页迁移（`migrate_page`）时需要维护目标页的 Cache 状态；
- **CPU 热插拔/低功耗（cpuidle）**：核下电前要 flush 该核私有 Cache（如 `flush_cache_all()`），防止数据丢失；
- **`cache_line_size()`/`____cacheline_aligned` 等对齐宏**：内核数据结构按 Cache 行对齐，避免伪共享（见第 29 题）；
- **`slab`/`slub` 分配器的着色（cache coloring）**：让不同对象在 Cache 中错开起始偏移，减少组冲突；
- **NUMA 感知的调度/内存分配**：结合 Cache 拓扑（`sched_domain`、`cacheinfo`）做负载均衡与就近内存分配。

实测：`cat /sys/devices/system/cpu/cpu*/cache/index*/*` 就是内核 `drivers/base/cacheinfo.c` 把每个 CPU 的 Cache 拓扑（level/size/ways/line size/shared_cpu_list）导出到 sysfs 供用户态与调度器使用的接口。

### 16. 请简述ARM big.LITTLE架构，包括总线连接和高速缓存管理等。

big.LITTLE 是 ARM 提出的异构多核架构：将一组高性能大核（如 Cortex-A76/A75/A72）与一组低功耗小核（如 Cortex-A55/A53）封装在同一芯片上，根据负载动态调度任务到合适的核簇，兼顾性能与功耗。典型连接方式：

- **总线互连**：大、小核簇各自通过 ACE（AXI Coherency Extensions）接口连接到 **CCI（Cache Coherent Interconnect，如 CCI-400/CCI-500/CCI-550）**或更新的 **DSU/CMN** 互连上，CCI 负责跨簇维护 Cache 一致性（监听/转发 MESI 相关总线事务）并提供簇间共享内存访问；
- **GIC（Generic Interrupt Controller）**：统一管理跨核簇的中断路由，使调度器可以把中断/任务迁移到任意核；
- **Cache 层级**：每个核有私有 L1（及部分架构每核或每簇私有 L2），跨簇一致性由 CCI 保证；也可能存在跨簇共享的 L3/系统缓存（SLC）。
- **调度模式**：早期有 cluster migration（整簇切换）、CPU migration（大小核一一对应切换）、**全局任务调度（Global Task Scheduling / HMP）**——现代 Linux 用 **EAS（Energy Aware Scheduling）** 结合 `sched_domain`、`arch_scale_cpu_capacity()` 实现任意核间任务迁移。

实测本设备 RK3588 即为 4×Cortex-A55（小核）+ 4×Cortex-A76（大核）的 big.LITTLE：

```
$ cat /proc/cpuinfo | grep -E "processor|CPU part"
processor 0~3 → CPU part: 0xd05 (Cortex-A55, variant 0x2)
processor 4~7 → CPU part: 0xd0b (Cortex-A76, variant 0x4)
```

Cache 拓扑（`/sys/devices/system/cpu/cpuN/cache/`）显示：A55 每核私有 L1D/L1I 各 32KB(4-way)、私有 L2 128KB(4-way)；A76 每核私有 L1D/L1I 各 64KB(4-way)、私有 L2 512KB(8-way)；而 **L3 3MB(12-way) 由全部 8 个核共享**（`shared_cpu_list: 0-7`），说明 RK3588 用 DSU 把 8 核纳入同一一致性域并共享一份系统级 L3。

### 17. 高速缓存一致性（cache coherency）和一致性内存模型（memory consistency）有什么区别？

- **Cache 一致性（coherency）**：针对**同一个**内存地址，保证所有处理器核在任意时刻看到的（最终会看到的）都是该地址**最新写入的唯一值**——即多个 Cache 副本不能同时呈现不同数据。这是"单地址"的正确性问题，由 MESI 等总线监听协议解决，对程序员通常透明。
- **内存一致性模型（consistency model）**：针对**不同地址**之间的访存操作，规定这些操作对其它核而言"可被观察到的顺序"，即多个处理器同时对不同变量读写时，各处理器观察到的**全局顺序**是否与程序顺序一致。它关心的是"多地址、多操作之间的顺序"，由处理器的内存模型（强序 x86 TSO、弱序 ARM/RISC-V weak ordering）决定，程序员需要用内存屏障指令来显式约束顺序。

简言之：coherency 保证"同一份数据不会分裂"，consistency 保证"多份数据/多次访问的相对顺序符合预期"；一致性协议是一致性内存模型的必要非充分条件——即使 Cache 完全一致，弱序处理器仍可能因为乱序执行/写缓冲让其它核看到"错误顺序"的多变量更新，这正是需要内存屏障的原因（见第 4、5 题）。

### 18. 请简述高速缓存的回写策略。

指 Cache 中的数据被修改后何时/如何写回主存，主要两种：

- **写通过（write-through）**：CPU 每次写 Cache 命中时，同时把数据写回下一级/主存，保证 Cache 与主存随时一致；实现简单、不会丢数据，但每次写都要访问较慢的主存（通常配合写缓冲 write buffer 缓解），带宽压力大。
- **写回（write-back）**：CPU 写命中时只更新 Cache 行内容，并将该行标记为"脏（Dirty）"，暂不写回主存；只有当该行被**替换淘汰**、或被其它核/DMA 请求、或显式 flush 时才一次性写回。优点是大幅减少对主存的写次数（多次写同一行只需最后写回一次），是现代处理器（含本设备 Cortex-A55/A76）L1/L2/L3 普遍采用的策略；缺点是实现复杂（需要额外 Dirty 位与写回逻辑），且掉电/异常时若未及时写回可能丢失数据（因此设备驱动、文件系统在断电场景要考虑 Cache 落盘/`flush`）。

配合写策略还有**写分配（write-allocate）**：写未命中时先把整行数据从主存加载进 Cache 再写入（常与 write-back 搭配）；以及**非写分配（no-write-allocate/write-around）**：写未命中时直接写主存、不占用 Cache 行（常与 write-through 搭配）。

### 19. 请简述高速缓存行的替换策略。

当发生 Cache 缺失、且该组已存满（组相联情况下 N 路已用完）时，需要挑选一路淘汰腾出空间，常见策略：

- **随机替换（Random）**：随机挑选一路替换，硬件实现最简单，性能中规中矩，避免了某些规律访问模式下的病态最坏情况。
- **最近最少使用（LRU, Least Recently Used）**：淘汰最长时间未被访问的一路，命中率通常最好，符合时间局部性原理；但真 LRU 需要为每路维护访问时间戳/排序链表，路数越多硬件代价越高，实际处理器多用**伪 LRU（Pseudo-LRU，如树形 PLRU、位计数近似）**折中实现。
- **先进先出（FIFO）**：按照进入 Cache 的先后顺序淘汰最早进入者，实现比 LRU 简单，但不一定反映真实的访问热度。
- **轮询/时钟（Round-Robin / Clock）**：介于 FIFO 与 LRU 之间的近似算法。

替换策略只与组相联（或全相联）Cache 有关；直接映射 Cache 由于每个地址唯一对应一路，没有替换策略可言（新数据直接覆盖旧数据）。ARM Cortex-A 系列（含本设备 A55/A76）L1/L2 通常采用硬件伪随机或伪 LRU 策略，具体算法由实现定义（IMPLEMENTATION DEFINED），软件通常无法配置。

### 20. 多进程间频繁切换对转换旁视缓冲（Translation Look-aside Buffer， TLB）有什么影响？现代的处理器是如何解决这个问题的？

TLB 缓存的是"虚拟地址→物理地址"的转换结果，而不同进程拥有**不同的页表**（同一虚拟地址在不同进程中可能映射到不同物理地址，即"同名/homonym"问题，参见第9题）。若 TLB 项不区分进程，进程切换后旧进程残留的 TLB 项就可能被新进程错误命中，导致地址转换错误；早期做法是**每次进程切换（`switch_mm`）时无条件flush 整个 TLB**，这样虽然正确，但新进程运行初期会频繁发生 TLB miss（需要重新走页表），如果进程切换非常频繁（如高并发、容器/线程密集场景），TLB miss 带来的页表 walk 开销会显著拖累性能。

现代处理器（包括 ARMv8）引入**ASID（Address Space ID）**方案：为每个进程地址空间分配一个 ID，TLB 项除了虚拟地址外还携带该 ASID（本设备 TLB 项中 ASID 存于 Bit[63:48]），查找 TLB 命中条件变为"虚拟地址匹配 **且** ASID匹配"。这样进程切换时无需 flush 整个 TLB，只需切换 TTBR/ASID 寄存器，不同进程各自的 TLB 项可以共存、互不干扰，大幅减少了进程切换后的 TLB miss，只有当 ASID 数值空间耗尽发生"回卷（rollover）"时才需要做一次全局 flush 与重新分配。x86 也有类似的 PCID（Process-Context Identifier）机制。

### 21. 请简述NUMA架构的特点。

NUMA（Non-Uniform Memory Access，非统一内存访问）从 SMP 演化而来：系统由多个"节点（node）"组成，每个节点包含一组 CPU 和一块本地物理内存（以及可能的本地 I/O），节点间通过高速互连（如 QPI/UPI、AMD Infinity Fabric）连接。特点是：

- CPU 访问**本节点本地内存**延迟低、带宽高；访问**其它节点的远程内存**需要经过节点互连，延迟明显更高、带宽更低——即"内存访问延迟不均一"；
- 逻辑上仍是一个统一的共享地址空间（区别于纯粹的分布式/消息传递系统），软件可以像 SMP 一样编程，但为了性能，操作系统和应用需要做 **NUMA 感知的调度与内存分配**（尽量让任务在本地节点分配内存、就近调度，如 Linux 的 `numactl`、`mbind()`、`autonuma`）；
- 常见于多路服务器 CPU（如 Intel 至强多路互联、AMD EPYC、ARM 的 ThunderX2 等）。

实测本设备为**单芯片、单一 DDR 控制器域的 SoC**，`numactl --hardware` 返回 `No NUMA available on this system`，内核配置 `# CONFIG_NUMA is not set`，`/sys/devices/system/node/` 不存在——即典型的 **UMA（统一内存访问）**架构：所有 8 个核访问同一块 DDR 内存的（近似）延迟一致，不存在节点划分，这与多路服务器的 NUMA 形成鲜明对比。

### 22. ARM从Cortex系列开始性能有了质的飞跃，如CortexA8/A15/A53/A72，请指出Cortex系列在芯片设计方面的重大改进。

- **流水线深度与超标量/乱序执行**：从 Cortex-A8 的顺序双发射，到 A9/A15 引入乱序执行（Out-of-Order），A72/A76 在保持高效能的同时进一步加深流水线、提升发射宽度与执行单元数量（本设备 A76 为 4 发射乱序核）；
- **多核与 Cache 一致性互连**：引入 SMP 多核簇结构，配合 CCI/CoreLink 互连实现跨核 Cache 一致性；
- **big.LITTLE 异构计算**：A7/A15、A53/A57、A55/A76 等大小核组合，兼顾性能与功耗（见第16题）；
- **64 位架构（ARMv8-A）**：从 A15/A7 的 32 位 ARMv7-A 过渡到 A53/A57/A72/A76 的 64 位 ARMv8-A，扩展寄存器数量（31个通用寄存器）、虚拟地址空间（48/52位）、新增 NEON/加密指令扩展（AES/SHA/CRC32，本设备 `Features` 中可见 `aes pmull sha1 sha2 crc32`）；
- **更深/更大的 Cache 层级与 TLB**、硬件预取器改进；
- **更精细的电源管理**：DVFS（动态电压频率调节，本设备 `cpufreq` 显示 A55 408MHz~1.8GHz、A76 408MHz~2.256GHz）、细粒度电源域/功耗门控；
- **原子操作与内存模型增强**：ARMv8.1 引入 LSE（Large System Extensions）原子指令（本设备 Feature 含 `atomics`），减少多核自旋锁/原子操作开销。

### 23. 若对非对齐的内存进行读写，处理器会如何操作？

不同架构处理方式不同：
- 部分严格架构会直接触发**对齐异常（Alignment Fault）**，交由软件（内核）模拟完成非对齐访问后再恢复执行，代价高昂；
- ARMv7 及以后（包括 ARMv8-A）**普通内存（Normal Memory）区域默认支持非对齐访问**：处理器（在 `SCTLR_ELx.A` 位未置位时）会自动将一次非对齐访存拆分成两次（或多次）对齐的总线传输，硬件内部处理拼接，程序员通常无感知，但相比对齐访问会有额外的性能开销（多一次总线周期，且如果跨越 Cache 行/页边界还可能触发额外的 Cache 缺失或两次页表查询）；
- 但对 **Device / Strongly-ordered 内存类型**（如 MMIO 寄存器空间）通常**不允许非对齐访问**，会直接触发 Data Abort 异常；对某些原子的 Load/Store-Exclusive、SIMD 大数据宽度访问，架构也可能要求必须对齐，否则触发异常。

Linux 内核可以通过 `SCTLR_EL1.A` 关闭非对齐访问自动处理来 debug（或用 `/proc/cpu/alignment` 之类的接口在旧架构上统计/处理未对齐异常）。

### 24. 若两个不同进程都能让处理器的使用率达到100%，它们对处理器的功 耗影响是否一样？

不一样。CPU 利用率达到 100% 只说明"核在忙、没有空闲进入 idle"，但功耗还取决于具体在执行**什么样的指令/微架构行为**：

- 若进程主要执行**密集型运算（如大量整数/浮点/SIMD 指令，尤其是高功耗的 NEON/SVE 向量运算或频繁触发 Cache miss、DRAM 访问）**，会激活更多功能单元、总线/内存控制器活动更频繁，动态功耗（与开关活动 activity factor 成正比）显著更高；
- 若进程主要是**忙等待/自旋（如空循环、频繁但简单的分支跳转、命中 Cache 的小工作集）**，虽然利用率也是 100%，但活跃的电路规模、内存带宽占用小得多，功耗相对更低；
- 此外，不同利用率 100% 的负载可能触发不同的 **DVFS 频率/电压**（如本设备 governor 会根据负载特征选择不同 OPP 点），频率越高动态功耗随频率、电压平方近似增长（`P ∝ f·V²`），因此即使两者都跑满 CPU，若被调度到不同频率点，功耗也会不同；
- 还要看是被调度到大核（A76）还是小核（A55）——同样 100% 利用率，A76 满载功耗远高于 A55。

因此"CPU 利用率"是调度/时间维度的指标，不能直接等价于功耗，需要结合具体指令组合、微架构活动、运行频率/核类型综合评估（这也是 Linux **EAS（Energy Aware Scheduling）**引入 per-CPU 能耗模型的原因）。

### 25. 为什么页表存放在主内存中而不是存放在芯片内部的寄存器中？

- **容量原因**：页表需要覆盖整个虚拟地址空间到物理地址空间的映射，条目数量庞大（现代 64 位系统页表可能达到 GB 级），芯片内部寄存器（触发器）成本高、面积/功耗代价极大，不可能集成如此大容量的存储；而主内存（DRAM）单位成本低、容量大，天然适合存放这种"大而稀疏访问"的数据结构。
- **可分页式的层级结构**：多级页表设计允许按需分配（未使用的地址区间不必分配页表页），进一步利用了主存"按需付费"的特性；
- **芯片内**只保留**最近使用过的少量转换结果**——也就是 TLB（作为页表的 Cache），用少量高速寄存器/CAM 存放热点转换，兼顾容量与速度：miss 时才去主存中查完整页表（页表遍历，可由硬件 Page Table Walker 或软件完成）。这本质上和"为什么数据不能全放寄存器、要用 Cache+主存的层级结构"是同一个道理。

### 26. 为什么页表要设计成多级页表？直接使用一级页表是否可行？多级页 表又引入了什么问题？

**直接使用一级（线性/单级）页表的问题**：假设 32 位地址空间、4KB 页，一级页表需要 `2^32/4KB = 2^20` 个页表项，若每项 4 字节，则需要 4MB 连续内存；到了 64 位地址空间（如 ARM64 的 48 位虚拟地址）则需要 `2^48/4KB` 个项，页表本身就要占用 TB 级空间——而且**每个进程都要有一份完整页表**，即使该进程实际只用到很少一部分地址空间，也必须提前分配整个页表，浪费巨大。

**多级页表的思路**：把线性页表按虚拟地址高位切分成"页目录 → 页表 → 页帧"的树状结构，只有真正被该进程使用到的地址区间才需要分配对应的下级页表页，未使用区间对应的上级页表项直接标记为无效（不存在），从而把页表所占空间从"和地址空间大小成正比"降低为"和实际映射的内存量成正比"，节省大量内存。

**多级页表引入的问题**：
1. **地址转换需要多次访存**（本设备 ARM64 4 级页表最坏情况需要连续访问 4 次页表 + 1 次实际数据，共 5 次内存访问），比单级页表慢，因此必须依赖 TLB 缓存命中来弥补这一开销；
2. 增加了硬件页表遍历（或软件 walk）逻辑的复杂度；
3. 多级结构下缺页处理、页表本身的分配/回收管理（如何创建/销毁中间级页表页）也比单级复杂。

### 27. 内存管理单元（Memory Management Unit，MMU）查询页表的目的是找 到虚拟地址对应的物理地址，页表项中有指向下一级页表基地址的指针，那它 指向的是下一级页表基地址的物理地址还是虚拟地址？

是**物理地址**。因为 MMU 硬件页表遍历器（Page Table Walker）本身工作在"MMU 尚未（对这次转换而言）完成"的语境下——它的职责就是把虚拟地址转换为物理地址，如果页表项里存的是下一级页表的虚拟地址，那就成了"需要先做一次地址转换才能拿到做地址转换所需的输入"的先有鸡还是先有蛋的死循环。因此架构规定：TTBR 寄存器存放顶级页表的**物理**基地址，页表中每一级页表项（如 ARM64 的 Table descriptor）里存的下一级页表基地址也都是**物理地址**，硬件页表遍历器全程直接用物理地址访存（通常还会绕过/直接访问物理内存，不经过一般数据访存的 Cache 一致性域），只有最终查到的叶子页表项中的"物理页帧号"才是最终答案。

软件层面（Linux 内核）操作页表时是通过内核的线性映射虚拟地址去读写页表内容的（比如 `pgd_offset()` 返回的是虚拟地址指针），但页表项**里面存的内容**（即写入到那些内存单元的值）永远是物理地址，这是软件视角与硬件视角的区别（详见第2章第23题，Linux 如何用虚拟地址遍历、页表项内容却是物理地址）。

### 28. 假设系统中有4个CPU，每个CPU都有各自的一级高速缓存，处理器内 部实现的是MESI协议，它们都想访问相同地址的数据A，大小为64字节，这4个 CPU的高速缓存在初始状态下都没有缓存数据A。在T0时刻，CPU0访问数据A。 在T1时刻，CPU1访问数据A。在T2时刻，CPU2访问数据A。在T3时刻，CPU3想 更新数据A的内容。请依次说明，T0～T3时刻，4个CPU中高速缓存行的变化情 况。

| 时刻 | 事件 | CPU0 | CPU1 | CPU2 | CPU3 |
|---|---|---|---|---|---|
| 初始 | — | I | I | I | I |
| T0 | CPU0 读 A（缺失，其它核都无该行）| **E**（独占，从主存加载，无人共享） | I | I | I |
| T1 | CPU1 读 A（发出 BusRd，CPU0 侦听到并响应/共享） | **S**（由 E 降级为共享） | **S**（从 CPU0 或主存获得数据） | I | I |
| T2 | CPU2 读 A（发出 BusRd，CPU0/CPU1 均已是 S，继续共享） | **S** | **S** | **S**（加入共享） | I |
| T3 | CPU3 要写 A（发出 BusRdX/Invalidate，使其它所有副本失效，自己获得数据并修改）| **I**（被 CPU3 的 BusRdX 使无效） | **I** | **I** | **M**（独占且已修改，与主存不一致） |

要点：第一个读者直接进 E（因为读之前该行在所有 Cache 中都是 I，没有其他持有者），后续读者的加入会把已持有该行的核（们）从 E/S 降级为 S 并共享；任何一个核要写（更新数据）时都必须先通过总线广播使其余所有副本失效，自己独占该行并置为 M，写回主存前该行内容仅在 CPU3 私有 Cache 中是最新的。

### 29. 什么是高速缓存伪共享？请阐述高速缓存伪共享发生时高速缓存行状 态变化情况，以及软件应该如何避免高速缓存伪共享。

**高速缓存伪共享（false sharing）**：多个 CPU 各自频繁读写**不同的变量**，但这些变量在内存布局上恰好落在**同一条 Cache 行**内。由于 MESI 协议是以"整条 Cache 行"为一致性维护粒度而非按字节，即使两个 CPU 操作的是行内互不相干的字节，只要其中一方写了该行，就会触发另一方持有的副本失效，造成大量不必要的 Cache 一致性流量与缺失，性能显著下降——"共享"其实并未发生在逻辑数据上，只是物理上共享了同一条 Cache 行，故称"伪共享"。

**状态变化举例**（CPU0 频繁写变量 a，CPU1 频繁写同一行内的变量 b）：
1. CPU0 写 a：该行由 I/S 转为 CPU0 独占 **M**；
2. CPU1 要写 b：即使 b 与 a 无关，因为同行，CPU1 发出 `BusRdX`，CPU0 的该行被迫写回并置为 **I**，CPU1 获得数据后写入变为 **M**；
3. CPU0 再写 a：又要发 `BusRdX` 使 CPU1 该行失效为 **I**，自己转为 **M**；
4. 如此在 CPU0、CPU1 之间反复"乒乓"（ping-pong），行不断在 M↔I 之间跳动，产生大量总线流量与延迟。

**软件规避方法**：
- 让不同线程/CPU 频繁读写的（逻辑上无关的）变量在内存中**分开存放到不同 Cache 行**，常用**按 Cache 行大小对齐/填充（padding）**，如 Linux 内核的 `____cacheline_aligned_in_smp`、C11/C++17 的 `alignas(std::hardware_destructive_interference_size)`；
- 将 per-CPU 频繁写的数据组织为**per-CPU 变量**（如内核的 `DEFINE_PER_CPU`），从数据结构层面天然隔离到不同 Cache 行/不同内存区域；
- 调整结构体成员顺序，把只读、跨核共享的字段与各自私有、高频写的字段分组隔离，避免它们混排进同一 Cache 行。

### 30. CPU和高速缓存之间，高速缓存和主存之间，主存和辅存之间数据交 换的单位分别是什么？

- **CPU ↔ Cache**：以**字（word）**为单位（通常等于寄存器宽度，如 32/64 位），CPU 的 Load/Store 指令按字（或字节/半字，具体到指令粒度）读写；
- **Cache ↔ 主存（DRAM）**：以**Cache 行（line）**为单位（本设备为 64 字节），一次缺失/写回都是整行搬运；
- **主存 ↔ 辅存（磁盘/Flash 等外存）**：以**页（page）**为单位（如 Linux 常见 4KB，本设备 `getconf PAGE_SIZE` = 4096），换页/交换（swap）、文件页缓存的读写都是整页操作。

三者体现了存储层级中"离 CPU 越近，交换粒度越小、速度越快、容量越小；离 CPU 越远，交换粒度越大、速度越慢、容量越大"的一般规律。

### 31. 操作系统选择大粒度的页面有什么好处？选择小粒度页面有什么好 处？

**大页（如 2MB/1GB 大页，或 64KB 页）的好处**：
- 同样大小的地址空间需要的**页表项数量更少**，页表本身占用内存更小、遍历层级更浅；
- **TLB 覆盖的地址范围更大**（TLB 项数量有限，每项覆盖的内存更大意味着更少 TLB miss），显著降低大内存工作集（如数据库、大型内存计算）程序的地址转换开销；
- 减少缺页异常次数（一次缺页处理的数据量更大，摊薄了异常处理的固定开销）。

**小页（如 4KB）的好处**：
- **内存碎片/内部碎片更小**：进程实际请求的内存往往不是页大小的整数倍，页越小，最后一页浪费的空间（内部碎片）越小；也更容易凑出连续大页所需的物理连续性；
- **粒度更细，便于精细化的内存管理**：写时复制（COW）、按需换页（demand paging）、内存回收（reclaim）、权限设置（读/写/执行）都可以以更小单位操作，减少不必要的数据复制/换入换出，适合内存紧张、多任务、内存映射粒度要求高的场景（如加载可执行文件的不同段）；
- 更容易在物理内存紧张时找到可用的空闲页（大页要求物理连续，容易因碎片而分配失败，小页要求低）。

因此现代操作系统（含 Linux/本设备）通常以小页（4KB）为默认粒度，同时提供**大页（HugePage/Transparent HugePage）**作为可选优化，让程序按需在两者之间取舍。

### 32. 引入分页机制的虚拟内存是为了解决什么问题？

- **地址空间隔离与保护**：每个进程拥有独立的虚拟地址空间和页表，天然隔离进程之间的内存访问，一个进程无法直接读写另一个进程的物理内存，提高系统安全性与稳定性；
- **简化编程模型/重定位问题**：进程可以假设自己独占从 0 开始的连续地址空间，无需关心程序实际加载到物理内存的哪个位置，链接器/编译器可以使用固定的虚拟地址，避免了早期"程序需要根据加载地址重定位"的复杂性；
- **突破物理内存容量限制**：通过按需调页（demand paging）与磁盘交换（swap），进程可以使用的虚拟地址空间总量可以超过实际物理内存大小，操作系统按需把暂不使用的页换出到磁盘；
- **高效的内存共享与去重**：多个进程可以将各自的虚拟地址映射到同一物理页（共享库、`mmap` 共享内存、写时复制 `fork`），既节省物理内存又实现进程间通信；
- **细粒度的访问权限控制**：页表项可以为不同页设置只读/读写/不可执行（NX/XN）等属性，实现内存保护（如防止代码段被修改、数据段被执行）；
- **减少外部碎片**：以固定大小的页为单位分配物理内存，不要求进程占用连续物理内存，避免了早期"分段式"内存管理容易产生的外部碎片问题（代价是页内的内部碎片，见第31题）。

### 33. 缺页异常相比一般的中断存在哪些区别？

- **同步 vs 异步**：一般外部中断是**异步**的，与当前执行的指令流无关，随时可能到来；缺页异常是**同步异常（synchronous exception）**，由当前正在执行的具体指令（访存指令）直接触发，与该指令强相关。
- **精确定位与可恢复性（restartable）**：缺页异常发生时，处理器必须精确保存"引发异常的那条指令"的上下文（如 ARM64 的 `ELR_ELx` 指向该指令本身，而不是下一条），使得内核在缺页处理程序（`do_page_fault`/`handle_mm_fault`）分配好物理页、建立好映射之后，可以**从异常前那条指令重新执行**，仿佛缺页从未发生过；而一般中断处理完成后是从**被中断的下一条指令**或独立的中断服务流程返回，不需要"重新执行原指令"。
- **处理内容依赖 CPU 现场**：缺页异常处理需要读取导致异常的**具体地址（Fault Address Register，如 `FAR_ELx`）**及**访问类型（读/写/取指、权限错误还是页不存在）**等 CPU 相关的现场信息来决定如何修复（分配页、COW、换入 swap、还是触发 SIGSEGV），而普通中断处理通常只需知道中断号，不依赖被中断指令的具体语义；
- **可能涉及阻塞与调度**：缺页处理可能需要等待磁盘 I/O（换入页面）等耗时操作，处理过程中可以睡眠、被调度切换；而中断上下文（尤其是硬中断处理部分）通常不允许睡眠，必须尽快返回或转交给可睡眠的下半部（softirq/tasklet/workqueue）处理。

### 34. 高速缓存设计中，如何实现更高的性能？

- **提高命中率**：更大容量、更高相联度（组相联路数）、更优的替换算法（伪LRU）、软件/硬件预取（prefetch，根据访存模式提前取入即将使用的行）、Cache 着色减少冲突缺失；
- **降低访问延迟**：多级 Cache 层级设计（L1 小而快、L2/L3 逐级增大但延迟增加），让绝大多数访问在最快的 L1 命中；关键路径上采用 VIPT（可与 TLB 查找并行）而非串行的先转换地址再查 Cache；
- **提高带宽/并发度**：非阻塞 Cache（non-blocking / lockup-free cache，缺失后不阻塞后续无关请求，用 MSHR—Miss Status Holding Register 跟踪多个在途缺失）、多端口/多存储体（bank）设计允许同周期多次访问、写缓冲（write buffer）与合并写（write combining）减少写操作对流水线的阻塞；
- **降低一致性开销**：更高效的互连（CCI/CCN/DSU/CMN 等）、目录式（directory-based）一致性协议（大规模多核下比全广播监听更省带宽）、MOESI 的 Owned 状态减少不必要的写回；
- **减少软件维护成本换取硬件正确性**：采用 PIPT/受限 VIPT 避免别名问题（第10~12题），减少软件 Cache flush 的频率与范围；
- **针对负载做定制策略**：如根据访问模式选择合适的替换/预取策略、结合大页减少 TLB miss 从而间接提升 Cache 访问效率（TLB miss 期间无法继续访存流水线）。

综合来看，Cache 性能优化是"命中率 × 单次访问延迟 × 并发吞吐"三者的整体权衡，实际处理器设计（如本设备 Cortex-A76 相比 A55 有更大 L1/L2、更高相联度、更复杂的乱序执行与预取器）正是围绕这一权衡在成本、功耗与性能之间做出的取舍。
